# 待办（按版本记录）

> 约定：写明「v1.0.X 待办」的东西，**等主人说"开始做 v1.0.X"再动**；
> 没说之前的所有改动都算在 **v1.0.9** 这批更新里。

## v1.1.2（进行中：设置窗口不用重启就更新 + 气泡风格 + 动效）

主人 2026-09-14 原话（第一条带设置窗口「桌宠形象」页的截图）：

> 「这个我的形象库后面三维0挂件0这个数据更新刷新机制改一下，每次都需要我重启才能更新」
> 「之前说的动效做了吗」
> 「按照简易程度和风险等级来做，最简单低风险的开始，那个气泡风格切换改成一个单独的选项在桌宠外观里」
> （切页动效第一版做完之后）「bug严重，不是淡入的，是很生硬的卡顿错位」
> 「这个形象库如果使用了自己的形象，现在是谁的名称是不是也要同步一下
> （括号的三视图和单张是全局配置）」
> 「天气添加了城市不能立即更新」
> 「背景轻微浮动 / 视差」→（划掉旁边批注）**这个不做先**
> 「设置项搜索、空状态插图、图标风格统一」→（批注）**先开始这三个**

### 改了什么

- **根因**：设置窗口的页面是**建一次就一直留着**的（`ConsoleWindow._build_main` 里
  每个 `builder` 只跑一次），而「我的形象库…（三维 N · 挂件 N）」这几个字是
  `_page_appearance` 建页面时**算死的**字符串 —— 数据本身（`pet.skin_library()`）
  一直是现读的，只是没人去动那颗按钮的文字，所以非得重启才更新。
- 加了一套**页面级的"现读一遍"挂点**：`_hook_page(key, fn)` 挂回调，
  `_refresh_page(key)` 在**切页**和**窗口重新露出来**（`showEvent`）时跑一遍。
- 「桌宠形象」页：条目数改成现读（`_skin_lib_text`），**关掉形象库窗口立刻刷一次**，
  「现在是谁」也一起跟着实际形象走；「全部恢复默认形象」之后同样刷一遍。
- 同一类毛病顺手一起修了：音效名单（自己加 / 删的）、「台词内容」那行「已改 N 类」、
  「回收内存」那行「上次：…」。

### 「之前说的动效」这一轮的账（2026-09-14 对过旧会话）

- 旧会话（09-14 08:29 那份计划）里「六、动效」写的是：**页面切换动画开关**、
  背景轻微浮动 / 视差、「减少动效」总开关；当时被归到"太复杂的先不做、等你点名"。
- 主人说按**简易程度和风险等级**排着做，所以这版先做风险最低的那两条：
  1. **气泡风格**（主人点名要单独一条选项，放在「桌宠形象」页）—— 做完了；
  2. **页面切换动效 + 「减少动效」总开关** —— 做完了（两颗开关都在「通用 → 动效」）。
- **先没做**（等主人点头）：**背景轻微浮动 / 视差**（每帧要重画整张底图，跟视频背景、
  省电冲突，得单独设计 —— 比如只给静态图做慢速漂移，视频和模糊时不走）；
  以及 v1.1.0 列在第三期的 **设置项搜索、空状态插图、图标风格统一**。

### 气泡风格（主人点名）

- 落点：**「桌宠形象」页 → 「外观」卡片里单独一条「气泡风格」**（跟随界面 / 浅色 / 深色）。
  主人原话是"在桌宠外观里" —— 现有分类里管桌宠外观的就是「桌宠形象」这一页，
  没有另开新分类（要新分类的话主人说一声）。
- 默认「跟随界面」：深色界面下气泡自动变深底浅字（原来写死白底，暗色下很扎眼）。
- 语录 / 余额 / 歌词**三种气泡一起换**；心声（括号那种）也有单独一套底和字色。

### 「现在是谁」的名字同步（主人点名，2026-09-14 追加）

- 用着库里的自定义形象时，那一排档位文字换成**那个形象的名字**：
  「三姐妹（三视图）」「小粉（单张）」；没自定义过还是「大肥鱼（三视图）」
  「小鲸鱼挂件（单张）」。
- **「（三视图）」「（单张）」是按本来的（全局），永远跟着那一档走** ——
  名字太长只截名字（截到 12 字 + …），括号里那四个字不会被截掉；悬停看全名。
- 两本各算各的：三维那档不会串成挂件的名字。换形象 / 改名字 / 删条目 / 恢复默认之后
  都是靠切页或关掉形象库窗口那一下重读（同一套 `_sync_skin_controls`）。

### 切页动效返工（主人报的 bug）

- 主人原话：**「bug严重，不是淡入的，是很生硬的卡顿错位」**。
- 第一版：`QGraphicsOpacityEffect` 让新页面淡入 —— 它会把整页塞进离屏图重画一遍，
  配这个"无边框 + 半透明"（分层窗口）就会闪、会错位，而且一帧贵。
  **结论：这条路别再走。**
- 第二版（现在这版）：切页前先 `shell.grab()` 拍一张"上一屏"，切成新页之后把这张图
  铺在壳上淡掉 —— 新页面自然浮出来。一帧只是贴一张图，不重画整页。
  逐帧验过：第 1 帧与切页前逐像素一致，之后单调过渡，卡片左边 x 全程 232（没错位）。

### 城市不能立即更新（主人报的 bug，2026-09-14）

- 两个原因，都得治：
  1. 城市那一排是**建页面时算死的**（`Segmented` 建完就不管了）—— 跟形象库那颗按钮一个毛病；
  2. 更要紧的一条：**加城市是后台联网跑的**。搜索走 `_city_pick_queue`、按 IP 定位走
     `_city_queue`，都要等 `tick()` 把结果取出来才 `_apply_city()` ——
     所以窗口那边"关掉重开 / 切页重读"都不够，**加完那一刻没人通知窗口**。
- 改法：
  - `Segmented.set_options()` 整排重建；`_sync_weather_controls()` 现读城市名单
    （条目变了才重建、没变只改选中）+ 按城市个数显隐「从列表里删掉城市…」；
  - `桌宠._notify_console()`：`_apply_city()` / `remove_city_dialog()` 末尾各叫一声
    → `ConsoleWindow.refresh_pages()` → 重读当前那页。窗口没开就叫了也白叫（什么都不做），
    下次打开自己现读。
- 顺手把同一个毛病的**余额来源那一栏**也治了：来源名单、Key 状态、按钮文字都现读。
- 还没动、但同一类的地方：**设置窗口里那些"一次性建出来"的开关状态**
  （透明度 / 吸附 / 层级 / 性能模式…）目前不会因为桌宠那边改了就变 ——
  它们都是"点了才变"，暂时没发现会被别处改的场景；真碰上了按同一套挂点加就行。

### 第三期这三件（2026-09-14 做完）

主人批注：**背景轻微浮动 / 视差「这个不做先」**，其余三件「先开始这三个」。

#### 图标风格统一 ✅

- 根子上的问题不是素材乱：49 个图标本来就是一套 Lucide（24 网格、线宽 1.75、
  单色、圆头圆角），**乱的是用法** —— 有些卡片图标的含义对不上（「显示」「开机」都挂
  `nav.general` 齿轮、「最小化动画」挂 `page.resize`、语录挂"加载中"那种放射图标），
  按钮一个图标都没有，**右键菜单干脆是纯文字**。
- 做的事：① 三档尺寸常量 `ICON_SM/CARD/MD`，全界面统一；② 修正含义对不上的卡片图标；
  ③ **按钮也能带图标**（`Page.buttons` 第 4 项 / `_text_button`）；
  ④ **两版右键菜单每条都配图标**（前缀表 `MENU_ICONS` + 子项继承 `MENU_ICON_CHILD`）；
  ⑤ 补 9 个缺口、删 7 个没人用的；⑥ 加了 `test_icon_family.py` 把规矩钉住
  （以后新加的图标也得守 24 网格 / 线宽 1.75 / 单色 / 圆头圆角）。
- **别踩**：菜单图标颜色必须取 `menu.palette().WindowText` —— 右键菜单是系统画的，
  写死一个颜色在亮 / 暗系统主题下必有一边看不清。
- **别踩**：给按钮 / 分段控件配图标时，颜色要跟着主题重画（`repaint_cards()` /
  `apply_theme()`），不然切主题后图标颜色会跟文字对不上。

#### 空状态插图 ✅

- 三处：搜索无结果（侧边栏）、形象库那一本空着（对话框右侧换成插图）、
  「应用联动」一个应用都没配过（卡片里）。
- `EmptyState` 是通用件：插图 + 标题 + 说明，颜色取主题的淡字色、跟着主题重画。
- 还能往哪儿用（等主人提）：余额没配 Key、音效没自己加过、天气断网取不到数。

#### 设置项搜索 ✅

- 侧边栏顶上「搜设置…」（Ctrl+F 聚焦、Esc 清空），敲字把分类换成结果列表：
  12 页共 66 条，跨标题 / 说明 / 页名做**子串 AND** 匹配；点一条跳过去 + 滚到那一行 +
  闪一圈主色边框（`FLASH_MS = 900`）。
- **没做**的：拼音 / 首字母（打「qp」找「气泡」）和模糊匹配 —— 现在是纯子串，
  中文直接打字就够用；真觉得不够再加。
- 索引是 `Page._index` 累积、`_build_main` 收口成 `_search_index`：以后新加设置项
  只要走 `page.row()` / `page.slider()` / `page.buttons()` 就自动能搜到，
  别自己 new 控件往 `card.addWidget()` 里塞（那样搜不到）。

#### 背景轻微浮动 / 视差 ❌（主人说"先不做"）

- 记在这儿免得以后又提：每帧重画整张底图，跟视频背景 / 省电冲突；
  真要做只能"静态图慢速漂移 + 视频 / 模糊时不走"。

### 还没做 / 待定

- 桌宠本体的**右键菜单**不用改：那份菜单是每次弹之前现攒的，条目数本来就新。
- 设置窗口里其它"建页面时读一次"的文字（比如窗口层级 / 吸附开关的当前值）都是**控件自己**
  带着状态，不在这套挂点里；以后再有类似的"要重启才更新"的反馈，先看 `_page_hooks`。
- 气泡风格**没进右键菜单**（主人要的是设置里的选项）；以后想顺手换，菜单里加一条即可。
- 动效这两颗开关只管**设置窗口自己**；桌宠本体的动作快慢还是「性能和工具 →
  怎么跑（性能模式 / 休闲模式）」那一档，没混在一起。

## v1.1.1（已发布：设置窗口能自定义）

> **版本号规矩（主人 2026-09-14 明确）**：没推送就不算开新版本 ——
> 这个会话里的返工全都算在 **1.1.1** 上，等推送那天再说下一版。
> 上半场（背景 / 标题 / 图标）记在本文下面的「v1.1.1 上半场」一节，同一个版本号。

主人 2026-09-14 原话：

> 「继续做，把你说的实现出来，但注意精简和bug，太复杂的先不做和我说，我来决定」
> 「优化一下控制台的选项内容，我看过来操作有点太复杂了，强调色加一项可以让用户自己
> 选择任意颜色（色卡也有吸色功能）优化一下说明，更加精炼高效」
> （在我写的 v1.1.2 上划了重点）「这一个会话里都还是1.1.1，只要没有推送就不算开始新版」
> 「背景设定有点小bug，不用和清除区别不大，我想的是只有导入文件这一个选项，
> 可以导入视频或者图片，如果是视频就是循环播放，然后加一个是否自动适配窗口大小的开关，
> 关闭则显示完整视频或图片，打开可以选择截取的位置，还有把滚轮修改数值或者选项关了
> 或者在通用里加一个开关，我老是误触，数值类型的调整加入点击数字可以自己输入数值，
> 如果输入超出上限或者下限则就只提供到上限数字或者下限数字（输入均为整数）」

### 改了什么

- **强调色改成"一排色卡 + 自定义… + 吸管"**（原来是一个下拉框 + 单独一行按钮，
  主人说操作太复杂）：色卡点一下就是它，少一层展开。
  - 「自定义…」用的是 Qt **非原生**取色盘（`DontUseNativeDialog`），
    自带 `Pick Screen Color` 那颗**吸管，能吸屏幕上任意一点** —— 这是主人这次要的。
  - 「自定义…」那颗自己会变成用户挑的颜色并被点亮，不用再单占一行说明现在用的是哪颗。
  - 最后一颗吸管图标 = 从背景图取色（`pick_accent`）。
  - 用户只挑**一个**颜色，hover / 选中底 / 主色上的字都是算出来的（`_accent_into`）。
- **卡片**：不透明度 40~100%、圆角 0~18。**侧边栏宽度删掉了**（主人说操作太复杂，
  这个性价比最低，要的话三行就能加回来）。
- **背景入口只剩一个**（主人报的 bug：「不用」和「清掉」干的是同一件事，分不清）：
  - 只有一颗 **「导入文件…」**（选过之后变成「换个文件…」）+ 一颗 **「移除」**。
  - **图片还是视频看后缀**（`bg_kind_of`），所以 `console_bg_type` 这个键**整个删掉**了。
  - 平铺也删了（主人没要，而且视频平铺一帧要贴几百次）。
- **自动适配窗口**（`console_bg_fit_window`，默认开）：
  - 开 = 铺满，多出来的裁掉，**旁边九宫格挑留哪一块**（`console_bg_focus`，默认 cc 正中）。
  - 关 = 完整显示，图 / 视频一点都不裁，空的地方露主题底色。
- **窗口**：记住大小和位置（默认开）、打开时先看哪一页。
- **修了个老 bug**：`__init__` 里 `switch_page("home")` —— "home" 这一页 v1.1.0
  就删了，所以设置窗口打开时标题栏一片空白。现在按 `console_start_page` 切，
  一打开就有标题。
- **整页文案精简**：所有说明砍到一行以内（原来一段一段解释）。举例：
   「亮暗度 / 负数压暗、正数提亮；图太晃眼就往左拖」→「亮暗 / 负数压暗，正数提亮」；
  「遮罩 / 在底图上蒙一层底色，越大底图越淡、字越清楚」→「遮罩 / 越高底图越淡」；
  「用什么样的背景」+「现在用的文件」两行合成一行「用哪个」+ 选文件 / 清掉。
  行数从 19 行降到 13 行左右。
- **滚轮不再改数值 / 选项**（`_WheelGuard`，应用级事件过滤器）：
  滑块和下拉收到滚轮时**不自己处理，转交给外面的滚动区** —— 页面照常滚，值不变。
  （主人说"我老是误触"，所以直接全局关掉，没做成"通用"里的开关；想留开关也容易加。）
  滚动条自己的滚轮不拦。
- **数值可以点着直接填**：滑块右边那颗数字改成按钮，点一下 `QInputDialog.getInt()`，
  只收整数，超上下限只给到上下限（`QInputDialog` 自己夹 + `setValue` 再夹一道）。

### 特意没做的（怕复杂 / 怕出 bug，等主人点名）

- 主题包（保存 / 导入导出 / 分享码）：牵扯文件复制 + 另一台机器上路径不一样，
  得一整套"打包 + 解包 + 路径重写"，是独立的一摊活。
- 导航项改名 / 隐藏 / 排序：要配一个小编辑器，容易把这一页撑爆。
- 全局字号缩放 / 换字体：QSS 里字号是逐个写死的（`QLabel#brand` 那些），
  改一处不动全身，得先把整套字号参数化，布局也要跟着重算。
- 侧边栏放右边：圆角和品牌行都要镜像，性价比低。
- 页面切换动画：现在压根没有动画，做"开关"等于没东西可控。

### 踩的坑

- **卡片半透明不能拿 `$surface` 开刀**：下拉弹窗、列表、按钮都用它，
  半透明的弹出菜单会把桌面透出来。所以新开一个 `$card` 只给卡片用。
- **重下样式表 45.4 毫秒**（实测）：卡片那几个滑块要是每一格都重下，拖起来会顿。
  改成 60 毫秒的攒一下（`_style_timer`），连续拖的时候只在**松手之后**重下一次。
- **最小化动画会改窗口几何**（缩到任务栏那么一粒）：`_geo_save` 必须跳过
  "动画中 / 还原中 / 已最小化 / 还留着最小化截图"这几种状态，
  不然下次打开窗口就是一粒芝麻。用例专门验了这条。
- 几何存的是 `saveGeometry()` 的 base64；回来时按可用屏幕夹一次
  （拔掉显示器 / 分辨率变小的兜底）。

### 验证

- `F:\Codex\work\test_console_style.py`：**全过** —— 打开就有标题、
  强调色（调色板 / 样式表 / hover / 选中底 / 主色上的字）、背景取色（#af613a）、
  **真点色卡**（点绿 → #22a06b；点「默认」→ 回主题主色）、自定义颜色点亮「自定义…」、
  恢复默认、卡片 rgba + 圆角、
  **自动适配开关（九宫格跟着能点 / 灰掉）、九宫格换位置画面真的变、完整显示不裁图、
  「移除」之后底图没了并且按钮写回「导入文件…」、导入之后变「换个文件…」**、
  记住窗口（900×660 @(120,90) 关掉重开一致）、起始页停在「控制台外观」、
  恢复默认清干净、最小化动画不污染几何、重开窗口能自己套上存下来的强调色、
  **滚轮划过滑块值不变 + 页面照常滚（0 → 60）+ 下拉选项不变**、
  **点数字能填数（上下限交给输入框）+ 填超上限只到上限**。
  截图：`shot_style_accent.png` / `shot_style_card60_r18.png` /
  `shot_style_tile.png` / `shot_style_bottom.png`。
- 取色盘的吸管单独验过：`F:\Codex\work\probe_color_dialog.py` 打出按钮列表，
  非原生那套里有 `&Pick Screen Color`（吸管）、色轮 / RGB / HSV / HTML 都在，
  挂在设置窗口底下也照样有（截图 `shot_colordialog_styled.png`）。
- 回归：`test_console_ui.py`（**49** 图标 / 12 页 / 24 张图）**PASS**、
  `test_console_backdrop.py` 12 项全过、`test_console_click.py` 全过、
  `test_console_page.py`（最小尺寸 + 12 页全切）DONE、
  `test_console_perf.py`：重下样式表 45.4 毫秒、换底图 4.0 毫秒、
  模糊 7.7~7.9 毫秒、模糊+压暗 8.4 毫秒、视频帧 5.5 毫秒。
- 没验的：手感（拖滑块跟不跟手、哪个配色好看）。这条等主人自己点。

## v1.1.1 上半场（背景 / 标题 / 图标，同一个版本，先做的）

主人 2026-09-14 原话：

> 「这个界面我想要可以自定义，有什么选择，我目前只想到可以自定义图片或者视频当作背景，
> 然后图片和视频可以调整亮暗度和模糊值，还有大肥鱼桌宠这几个字可以自己改，
> 边上那个图标也可以自定义」
> （我给了七类可自定义的清单之后）「先开始做v1的，版本1.1.1」
> （在我建议分类名拆开的那句上划了重点）「这个」

### 改了什么

- 左侧第 12 类 **「控制台外观」**（`ui_console.py` 的 `_page_console`），三张卡：
  1. **背景**：分段选「不用 / 图片 / 视频」→「选文件…」（还没选过文件时会自己弹
     文件框）、亮暗度 -100~+100、模糊 0~40、遮罩 0~100%。
  2. **标题和图标**：标题随便改（清空回默认）、图标换成自己的图（自动缩到 26px）。
  3. **恢复默认**：三样一起清。
- 按主人划的那句，「形象和外观」**拆成两类**：「形象和外观」→ **「桌宠形象」**，
  新的 **「控制台外观」** 排在它后面。
- 底图画在 `ShellFrame.paintEvent` 里（窗口最外层的子控件），
  样式表在玻璃模式下把壳 / 侧边栏 / 页面底变透明，**卡片保持实心**（字不会糊）。
- 视频：`QMediaPlayer` + `QVideoSink`，循环、静音、**15 帧/秒**上限，
  窗口隐藏 / 最小化时 `pause()`。
- 配置：`console_bg_type` / `console_bg_path` / `console_bg_bright` /
  `console_bg_blur` / `console_bg_scrim` / `console_brand_title` /
  `console_logo_path`，在 `桌宠.py` 的 `cfg_defaults` 里登记，老配置自动补。
- 最小高度 560 → **620**：分类多了一条（12×36 + 11×4 + 品牌 + 边距 ≈ 558），
  560 会把左下角那行版本号挤出去。

### 踩的坑 / 定下来的做法

- **不用 `QGraphicsBlurEffect`、也不逐像素卷积**：125% 缩放下 1150×800 的图卷一遍
  高斯要几百毫秒，拖滑块会一顿一顿的。改成"缩到 1/s 再放大回来"做两遍 ——
  Qt 在大比例缩小时走的是面积平均，等效一次盒式模糊，7.7 毫秒就算完（实测）。
  亮暗同理：整张蒙一层黑 / 白，不逐像素算。
- **样式表只在"真的换了玻璃状态"时重下**（`_glass_state`）：只在 `apply_theme`
  里换会漏掉"用户把底图文件删了"那条路 —— 底图没了、样式表还是透明的那套，
  窗口会变成一片空白。现在 `_apply_backdrop` 开头就比一下，不一样就回去重下样式表。
- **视频第一帧之前不切玻璃**：`_glass_on()` 只看"类型 + 文件在不在"，
  不看"这一帧有没有画面"。有画面之前先按平的底色画（看着就是原来的窗口），
  不然那几百毫秒里底色透明、又没人画底图，会闪一下。
- 图片做了**两级缓存**（原图按 路径+修改时间、处理完的按 尺寸+参数），
  拖滑块只重算"处理"这一步。

### 验证

- `F:\Codex\work\test_console_backdrop.py`：图片 / 模糊 / 亮暗 / 遮罩 / 暗色下底图 /
  自定义标题 / 自定义图标 / 视频出帧 / 恢复默认 / 配置落盘 —— **12 项全过**，
  截图 `shot_console_01_image.png` ~ `08_reset.png`。
- `F:\Codex\work\test_console_perf.py`：940×660 的壳，原图 4.0 毫秒、模糊 7.7 毫秒、
  模糊+压暗 8.3 毫秒、视频帧（模糊 40）5.4 毫秒；**文件删掉后 glass 回到 False、
  壳的底色回到实色**。
- `F:\Codex\work\test_console_page.py`：这一页本身的亮 / 暗 / 带底图三张图，
  最小尺寸 820×620 下 12 条分类和版本号都放得下，12 页挨个切一遍没崩。
- `F:\Codex\work\test_console_ui.py`（上一版的老用例，已改成 48 图标 / 12 页 / 24 张图）：
  **PASS**。
- `F:\Codex\work\test_console_click.py`：发真的鼠标 / 键盘事件点这一页的控件
  （分段两档、模糊滑块 0→40、标题框打字、全部恢复默认），**全过**；
  恢复默认后别的页照常。
- 打包：`pyinstaller --noconfirm --clean --distpath F:\Codex\work\pyi-console-test\dist …`
  出包成功，**没有覆盖 `dist/` 里已发的安装包**；顺手对了一下新旧 exe，
  `Qt6Multimedia.dll` / `avcodec-61.dll` 在 v1.1.0 的包里就有，这次只多了 14KB。
- 打包版能不能真解码：`F:\Codex\work\probe_frozen_video.spec` 冻了一个一次性小包
  （`probe_frozen_video.py`），里面 `MEDIA_OK=True`、收到 2560×1440 的视频帧 ——
  **打包版里视频背景能用**。
- 没验的：**手感**（拖滑块跟不跟手、模糊拖到 40 好不好看）—— 用例发的是程序生成的
  鼠标事件，证明得了"接得上、不卡"，证明不了手感。这条等主人自己点一遍。

### 主人点过之后可以再谈的

- 每页单独设背景（现在是一张底图铺满整个窗口）。
- 侧边栏宽度 / 位置、卡片圆角、强调色这些（我当时列在"v3 高级项"里的）。
- 主题包：保存 / 导入导出 / 分享码（v2 那批）。

## v1.1.0（已发布：新的设置界面）

主人 2026-09-14 原话：

> 「现在我想加上真正ui界面，现在有点粗糙，你有什么计划吗，先不要动任何东西，和我说说需要什么图标」
> （谈完计划后）「开工试一下吧，先给我一版看一下效果」
> （定版本号时）「之后迭代就每20一版吧，比如1.2.0的上一版是1.1.19」

### 改了什么

- 新增 `ui_console.py`：左侧 12 个分类 + 右侧内容的设置窗口，主题（亮 / 暗 / 跟随系统）、
  47 个单色图标（`assets/icons/`，Lucide ISC）、一整套控件（开关 / 分段 / 卡片 / 滑块 / 下拉）。
- `桌宠.py`：菜单第一块加「打开设置…」（包 `defer_dialog`）、`open_console()` 懒加载、
  `quit_app()` 里顺手关掉窗口、模块顶部 `try: import ui_console`（失败不影响桌宠）。
- `AGENTS.md` 第 5 条改成新版本规则：**补丁位每 20 一进位**（`.19` 用完进位）。
- 老菜单**一条没删**，这一版只把新界面立起来。

### 分三期

1. **第一期（本次）**：窗口骨架 + 主题 + 图标 + 分类页全部接了真动作（不是占位）。
2. **第二期（本次也做了大半）**：右键菜单已经缩成一条十条的快捷条（零子菜单），
   设置类的入口全挪进窗口。**还没删的**：完整版老菜单留在「经典菜单…」后面兜底，
   连同那 1000 行子菜单补丁，跑稳一版再一起删（删的时候用例要改成验窗口）。
3. **第三期**：设置项搜索、空状态插图、动效、图标风格统一、暗色下桌宠气泡也跟着换。

### 这一版主人提的（2026-09-14 追加）

> 「有了这个菜单感觉有点多余，能优化一下菜单吗，还有设置里一些冗余的功能，
> 比如查看天气，我觉得是不需要在设置里显示的，然后选项名称改的通俗一些，
> 但不要太掉价，我自己觉得别扭的名字是联动和自动化这个名字」

- 菜单：30 条 → 10 条，零子菜单；「打开设置…」提到第一块。
- 设置：**删掉整页「首页」**（余额 / 天气 / 在放什么那几张卡）、
  天气页的「现在的天气 → 查一下」、音乐页的「看一眼在放什么」；
  同一个开关只留一处（原来"首页"和专门页各一个，状态还会不同步）。
- 改名：「联动与自动化」→ **「应用提醒」**；分类统一成"X和Y"这种说法；
  「无级调节」→「精确调节」、「层级」→「窗口层级」、「记账」→「今日已用怎么算」、
  「歌词对时」→「歌词对不上？」、「Agent 名称 / 会话日志目录」→「去哪个文件夹读对话记录」。

### 还拿不准、等主人一句话的

- **「经典菜单…」那条留不留**：现在它是完整版老菜单的入口，用稳一版就该删。
- 「余额和用量」页顶上那张**「现在的余额」卡**留不留 —— 它也算"看一眼"，
  但它能直接验证 Key 配没配对；主人觉得多余就一起去掉。

### 这一版主人又提的（2026-09-14 晚，三张图）

> 「图一这个图标能改变吗好丑，图二这个选中的时候，上边缘似乎被什么东西挡住了一部分
> 不止这一个ui，图三这个形象库好丑能统一风格吗」

- 图一 = 任务栏上那只**空白方块**：全项目一个 `setWindowIcon` 都没有。
  现在 `main()` 和设置窗口都设成 `icon.ico`；`ui_console.bundle_icon()` 负责找图标。
- 图二 = hover 时顶边像被挡：把 hover 从"换细边框颜色"改成**换底色**，
  拿掉 `QGraphicsDropShadowEffect`（配无边框半透明窗口容易留残影）改自己画阴影，
  控件高度对齐整物理像素。**注意：离屏抓图抓不到 `:hover` 状态**（`grab()` 不画、
  `grabWindow()` 抓半透明窗口是全黑），所以这条是推断 + 架构上规避，不是像素级复现过。
  主人再看一眼，还犯的话得换招（比如把边框换成 2px 或干脆去掉边框）。
- 图三 = 形象库等老窗口：`ui_console.style_dialog()` 统一配色，
  形象库 / 上传形象 / 扫描应用 / 台词编辑器 / 小滑块窗口全接上。

### 顺手修 + 踩的坑

- **`_rebuild_sprites()` 少建"当前高度"**：它只建五档标准高度，而 v1.0.18 起大小能无级调。
  主人把大小调到 40%（136px）以后，`test_skin_library.py` 里"换图后侧面是新图"那条就挂了，
  一查是这个 bug。现在当前高度也一起建。
- **我自己的锅**：一个临时复现脚本忘了把 `CONFIG_PATH` 指到临时目录，
  把三条临时图写进了主人的真 `config.json`，已手工清掉（备份
  `%TEMP%\config.before-cleanup.json`）。以后写配置的脚本必须先重定向。

### 又提的两条（2026-09-14 更晚）

> 「能把固定挡位和无极调整联动一下吗，比如选择大，那么精确调整的滑块就在100%，
> 只是打个比方，精确数字你要自己算，然后精确大小最小再小一点可以到20%，大可以到150%」
> 「然后还有就是网易云音乐播放时气泡窗最上方灰色小字显示的为啥是未知应用，修一下」

- **档位 ↔ 滑块联动**：五档改成按"整百分数"排 —— **迷你 30 / 特小 40 / 小 60 / 中 80 /
  大 100**（「大」= 100%，就是主人举的那个例子）；范围放宽到 **20% ~ 150%**
  （倍率 0.18 ~ 1.35）。点档位滑块跟着跳；拖滑块停在哪一档，那一档就点亮。
  顺带把「无级调节」这个叫法统一成「精确调节」（菜单里那条也改了）。
- **网易云显示「未知应用」**：查下来标识本身是能对上的（网易云装在
  `E:\CloudMusic\cloudmusic.exe`），但兜底太薄 —— 现在加了 netease / qq music 等别名，
  而且**会话认不出名字时会用播放器窗口那边认出来的名字**，再退一步还有"把标识收拾成人看的
  样子"（`SomeNewPlayer.exe` → `SomeNewPlayer`），只有连标识都没有才会显示「未知应用」。
- **音乐页说明改实话**（2026-09-14 追加）：删掉「歌词对不上？」里
  "网易云不报播放进度…"那段解释，改在「显示歌词内容」下面直说
  **网易云暂不支持歌词显示**（只有 QQ音乐 能看歌词）。
  主人原话：「歌词对齐那个部分网易云解释可以删掉本来网易云就没有联动歌词功能了，
  但是在显示歌词边上要加说明，网易云暂不支持歌词显示」

### 再追加三条（2026-09-14 06:06）

主人原话：

> 「把设置侧边的顺序改一下，1.余额和用量，2.形象和外观，3动作和互动，4.天气和城市，
> 5.音乐和歌词，6.文案和语录，7.音效，8.应用提醒（改成应用联动），9.性能和工具，
> 10.通用，11.关于，解决一下图一快速滚动会显示掉帧卡顿，图二ok和cancel的汉化」

- **侧边顺序**按主人给的顺序重排，「应用提醒」→ **「应用联动」**。
  顺序断言钉在 `test_quick_menu.py` ⑤（以后谁再动顺序会被这条拦下来）。
- **滚动掉帧**：`ConsoleWindow.paintEvent` 的阴影改成**贴图缓存**（`_shadow_pixmap()`），
  QSS 里滚动区底色 `transparent` → `$bg`，`Page.__init__` 给视口加
  `WA_OpaquePaintEvent`。整窗重画 1.67→0.52ms。
  **注意**：离屏量不出"真机手感"，这条主要是减少每帧工作量；主人再看一眼，
  还卡的话下一步是把圆角/边框换成预渲染的九宫格贴图。
- **OK / Cancel 汉化**：`install_qt_translator(app)` 装 `qtbase_zh_CN.qm` + `qt_zh_CN.qm`，
  翻译器实例挂在模块级 `_QT_TRANSLATORS` 里防回收；顺带 `QLocale.setDefault(zh_CN)`。

### 菜单再瘦两条（2026-09-14 06:2x）

主人原话：

> 「把经典菜单切换按钮放到通用里，然后在菜单里去掉，把显示和隐藏也去掉，在设置里留就可以」

- 拿到手时 **菜单里那条「经典菜单…」已经没了**（上一句改动留下的痕迹，
  但「通用」页里还没接上，`_open_classic_menu()` 当时是没人调用的死代码），这次补完：
  - 「通用 → 排查问题」加了一行 **「右键菜单用经典版」开关**，配置键 `classic_menu`；
    `_make_menu(classic=None)` 现在默认看这个配置（显式传 True/False 仍然照旧，用例在用）。
  - 同一个卡片下面加了「打开经典菜单…（旧版那份长菜单）」按钮，
    直接调 `_open_classic_menu()`，不用开开关也能看一眼。
  - 顺带给 `set_classic_menu()` 加了句回话。
- **「显示/隐藏」也从菜单撤了**，设置「通用 → 显示」里那个按钮照旧。
  现在右键菜单 8 条：查看余额 / 查看天气 / 看一眼在放什么 / 打开设置… /
  锁定位置 / 鼠标穿透 / 救急恢复 / 退出。
  **注意**：托盘右键用的是同一份菜单，所以穿透之后"显示/隐藏"也得去设置里点；
  真觉得不方便的话，给托盘单独留一条也行（`_on_tray_activated` 里可以另建一份）。

### 设置窗口的置顶与最小化（2026-09-14 06:4x）

主人原话：

> 「修复1.设置界面会一直置顶，改一下不要这样和正常窗口一样就可以，不要跑桌宠层级那个代码
> 2.设置点击最小化时任务栏会直接消失，逻辑和点击关闭似乎相同了。增加一套设置的最小化动画，
> 参考windows最小化窗口过度动画」

- **不置顶**：`open_console()` 里那段"`layer == 'top'` 就给设置窗口也加
  `WindowStaysOnTopHint`"删了。设置窗口现在就是普通窗口（打开时 `raise_()` 一下）。
  代价：桌宠置顶时可能盖住设置窗口的一角 —— 主人要的就是这个。
- **最小化**：右上角那颗按钮的 tooltip 从「收起」改成「最小化」，
  接的是新的 `minimize_with_anim()`；「关闭」仍是 `hide()`。
  - 动画：`QParallelAnimationGroup` = `geometry` 从当前矩形收到任务栏附近一小块
    （`InCubic`，190ms）+ `windowOpacity` 淡到 0；
  - 收尾 `_minimize_done()`：先把尺寸还原（此刻全透明看不见）→ `showMinimized()`
    → 再把不透明度放回 1.0。不这么排的话，从任务栏点回来会是小小一只或者还是透明的。
  - 连点有 `_min_anim` 守门，不会叠两个动画。

### 最小化动画的两处返工（2026-09-14 07:0x）

主人批注：

> 「你去读一下windows的最小化逻辑，是有带缩放的，不是向下划拉就完了」
> 追加需求：「加一下点任务栏图标照样可以最小化和唤出设置窗口」

- **"往下划拉"的真凶找到了**：窗口有 `setMinimumSize(820, 560)`（免得被拖太小），
  几何动画被它钳死在 820×560 —— 位置在动、尺寸缩不下去，所以只剩"往下滑"。
  现在动画前 `setMinimumSize(0, 0)`、收尾再还原。
- **真的要缩放**：光松开最小尺寸还不够（窗口真变小 = 里面的字和控件会重新排版）。
  现在是 `self.grab()` 先把整窗拍成一张图、`self.shell.hide()` 藏掉真控件，
  动画期间 `paintEvent` 只把这张图按当前窗口大小铺开（`SmoothPixmapTransform`）——
  整窗连着字一起等比缩小，这才是 Windows 最小化的观感。
- **点任务栏图标**：给窗口加了 `Qt.WindowType.WindowMinimizeButtonHint`。
  Windows 只认 WS_MINIMIZEBOX 这一位（实测样式 `0x960A0000` 里有），
  没有它任务栏按钮点了没反应。加上之后"点一下最小化、再点唤出"就是系统原生行为
  （那条路由走的是 Windows 自己的动画，带缩放的那套）。
- 验的手段：真机探针 `probe_minimize_anim.py` 抓 `console.grab()` 的三帧
  （`shot_min_t1/t2/t3.png`）—— 屏幕截图会被上面的 Codex 窗口挡掉，抓窗口自己画的才对。

### 最小化动画再两处（2026-09-14 07:3x）

主人批注：

> 「这个也要加上动画啊」（点任务栏图标那条）
> 「现在点击最小化，终点是任务栏正中心，应该指向设置的图标」

- **任务栏那条路也走动画**：先确认了系统不给我们的窗口跑动画 ——
  `probe_native_minimize.py` 实测 `showMinimized()` 之后窗口矩形**一采样就是
  (-32000,-32000)**，没有任何过渡帧（无边框 + 半透明 = 分层窗口，DWM 不管）。
  于是加了 `_MinimizeGuard(QAbstractNativeEventFilter)`：拦
  `WM_SYSCOMMAND / SC_MINIMIZE`（任务栏按钮、系统菜单的最小化都是这条），
  吃掉之后 `QTimer.singleShot(0, minimize_with_anim)`；`changeEvent` 里再留一层兜底。
  真机 `probe_minimize_from_taskbar.py` 打印 PASS。
- **落点**：想自动拿到任务栏按钮矩形，**两条路都试过、都不通** ——
  ① .NET UIA（PowerShell 调 `UIAutomationClient`）：全桌面搜 50 秒、
     缩到只搜 `Shell_TrayWnd` 子树也要 15 秒，而且按窗口标题**根本查不到**
     （只列得出托盘那几个：网络/音量/时钟/通知/显示桌面）；
  ② MSAA `AccessibleObjectFromPoint` 沿任务栏扫：返回的是**前台窗口**的元素，
     Win11 任务栏按钮是 XAML 画的，MSAA 桥不过去。
  所以做成**可校准**：`cfg["minimize_target"] = [x, y]`，
  「通用 → 最小化动画 → 校准…」给 3 秒让主人把鼠标停在自己的任务栏图标上。
  没校准就还是落任务栏中间。
  **如果以后想要全自动**：得给项目加 UI Automation 依赖（`comtypes` + `uiautomation`，
  纯 Python、打包能带上），但按上面②③两条的实测，能不能查到那个按钮还得再验一次。

### 「校准功能无效」的修复（2026-09-14 07:5x）

- **根因是个低级错误**：`ui_console.py` 里用了 `QCursor.pos()`，但**从来没 import 过
  `QCursor`** —— 倒计时最后一拍直接 `NameError`，异常又被 Qt 的事件回调吞掉，
  所以界面上一点反应都没有（配置里永远没有 `minimize_target`）。
  修法就是补上 `from PySide6.QtGui import ... QCursor ...`。
- 顺手把校准做得不别扭（原来只给 3 秒，手慢一点就记错地方）：
  提示实时显示鼠标坐标，**按空格/回车立刻记**，不按键就 8 秒后自动记，
  再点一次那颗「校准…」按钮也等于"就记这儿"。
- 验的手段：真机 `probe_calibrate.py` 两轮（空格记 / 自动记）都 PASS。

### 还原动画（2026-09-14 08:0x）

主人：「点击任务栏图标变回正常窗口也给个动画」

- 拦的还是同一条原生消息：`WM_SYSCOMMAND / SC_RESTORE`（最小化着再点一次任务栏图标
  发的就是它），改调 `restore_with_anim()`。
- 素材复用最小化那张整窗截图：所以 `_minimize_done()` 里**不再清 `_min_pm`、
  也不急着把 shell 显示回来**，留着给还原动画用；`_finish_restore()` 才收尾复位。
- 踩了个坑：`restore_with_anim()` 内部要 `showNormal()`，而 `changeEvent` 里那条兜底
  正是"没在放动画又脱离最小化 → 收拾干净"，一上来就把我刚建好的动画对象清掉了
  （表现：还原没动画）。加了个 `_restoring` 牌子挡住它。
- 顺带：`open_console()` 改成调 `restore_or_show()` —— 最小化着点「打开设置…」
  也会先播还原动画。
- 验：真机 `probe_minimize_from_taskbar.py` 两条路都 PASS；
  离屏 `test_console_minimize.py` 第 ⑧ 组（还原动画起没起、几何/透明度/素材/shell 复位）。
  注意探针里**不能**用阻塞 `Sleep` 等动画（收尾回调不会跑），要用"边跑事件循环边等"。

### 还原动画的闪烁卡顿（2026-09-14 08:2x）

主人：「打开动画有不正常的闪烁卡顿」

- 原因①**顺序**：原来 `restore_with_anim()` 是先 `showNormal()` 再挪到落点 ——
  中间会有一帧停在"原来那么大"（那一帧就是闪烁）。改成
  `hide() → setWindowState(WindowNoState) → setWindowOpacity(0) → setMinimumSize(0,0)
  → setGeometry(小) → show()`，第一次露面就是"小 + 全透明"。
  **注意**：`hide()` 不会清掉"最小化"状态，漏了 `setWindowState(WindowNoState)`
  的话 `show()` 回来还是最小化的（这个坑当场踩了一次，离屏用例直接挂）。
- 原因②**贵**：动画每帧都在把上千像素的截图做平滑缩放（十几毫秒/帧），
  改成 `SmoothPixmapTransform=False`。整窗重画 1.11 ms/帧。
- 验：真机逐帧采样「起手拍 = 宽 115 + 透明 0.00 + 可见」，全程无非合理帧；
  `probe_minimize_from_taskbar.py` 两条路 PASS；离屏 `test_console_minimize.py` PASS。

### 动画打断（2026-09-14 08:4x）

主人：「快速点击动画会出bug，有没有和windows原生一样的打断动画」

- 原来 `minimize_with_anim()` / `restore_with_anim()` 开头都是
  `if self._min_anim is not None: return` —— 动画跑着的时候新请求**被丢掉**，
  于是卡在错的状态（想最小化却开着、想还原却还是最小化）。
- 改成"接管"（跟 Windows 一样）：`_start_anim(going_min)` 里先把在跑的
  `stop()` 掉，起点取**当前**的 `geometry()` / `windowOpacity()`，往新方向走；
  用 `_anim_target`（"min" / "restore"）判断"是不是已经在往那儿走了"来防重复。
- 反向的两种情形分别处理：
  · 最小化跑到一半 → 还原：窗口还没真最小化，`_restore_geo` 还在，直接从当前位置长回去；
  · 还原跑到一半 → 最小化：素材（`_min_pm`）还在，`shell` 也还藏着，顺着缩回去。
- 验：离屏连点 6 轮状态自洽、进程不崩；半路反向两条都 PASS。

### 大事故（已恢复）

- 我贪快，用 `Set-Content -NoNewline` 批量替换 `桌宠.py` 里的两处默认值，
  结果**所有换行被吃掉**，6822 行的主程序变成了一行（首行是 `#` 注释，
  `py_compile` 还假通过）。`git checkout -- 桌宠.py` 恢复后，把本轮所有改动
  重新打了一遍（菜单瘦身、设置窗口接线、对话框换肤、图标、精灵高度、大小档位全部重打并复验）。
  **以后整文件改写只用 `apply_patch`。**

### 还留着的（第二期一起弄）

- **桌宠自己的气泡**颜色还是写死的（白底深字），暗色壁纸下没问题，但没跟主题联动。
  要做的话是 `桌宠.py` 里那 14 处 `QColor(...)` 换成取主题变量。
- 几个老的 `QDialog`（形象库 / 上传形象 / 扫描应用 / 台词编辑器）还是 Qt 默认灰样式，
  没套新主题；这一版先把窗口本体做好。
- 设置窗口现在**不支持缩放**（固定 920×640，可拖）；窗口大小记忆没做。
- 分类图标现在是单色 + 主题色底片，主人如果看腻了可以换成定制的双色一套。

### 验证

- 真机真鼠标：`F:\Codex\work\probe_console.py`（右键 → 菜单里有「打开设置…」→ 点开窗口 →
  点「音效」换页 → 点「亮色」换主题 → config 写 `ui_theme=light` → 关窗口后桌宠还在），PASS。
- 离屏：`F:\Codex\work\test_console_ui.py`，47 个图标无空白 / 12 页 / 切页 / 亮暗两套 /
  系统判定，全过；出图 24 张。
- 回归：`F:\Codex\work\test_*.py` 29 套里 27 套过（`test_new_features.py`、
  `test_still_front.py` 是上一版就有的同样两条，无关）。
- **跑回归的口诀**（容易忘）：`QT_QPA_PLATFORM=offscreen` + `PYTHONIOENCODING=utf-8`，
  少一个要么弹真窗口卡住、要么中文编码崩。

## v1.0.19（已完成：百宝箱 → 回收内存）

主人 2026-09-14 原话：

> 「现在我想加一个缓存优化的功能，类似pcl2启动器里百宝箱里的功能，但具体原来我不知道，
> 先看看能不能实现，再决定加不加，稍等我想要就是全局内存优化，可以在不影响应用的情况下减少内存占用」
> （看完可行性报告后）「开始吧」

### 先查到的（PCL2 那个功能到底是什么）

- PCL2 的「更多 → 百宝箱」里有内存优化，方法名 `MemoryOptimize` / `MemoryOptimizeInternal`
  （同一个类里还有 `RubbishClear` 垃圾清理、`Jrrp` 今日人品、`StartCustomDownload`、随机洞穴/提示）。
- 但**开源仓库（Hex-Dragon/PCL2）里这块是空的**：函数体只有一句
  「为便于维护，开源内容中不包含百宝箱功能……」，拿不到原实现。
- 所以按 Windows 通用手段实测（RAMMap / Mem Reduct 那一套），结论写在下面的"验证"里。

### 改了什么

- 右键菜单「流畅度」和「音效」之间新增二级菜单 **「百宝箱」**：
  **回收内存** / **回收时不动前台程序（防卡顿）**（可勾，默认勾上）/
  灰字「不弹管理员提示，系统进程碰不到」；回收过一次之后多一行「上次：x 个程序腾出 y」。
- 回收 = 对**能碰到的进程**调 `EmptyWorkingSet`（工作集还给系统），**不提权**：
  浏览器 / 聊天 / 播放器这些自己开的程序都在范围内，系统进程（system / lsass / dwm …）
  本来也打不开句柄；`MEM_TRIM_MIN_MB = 20` 以下的进程不动。
- 默认**跳过前台程序**（主人要的是"不影响应用"），勾掉才是"连前台一起收"。
- 干完冒泡报数：「收拾完啦：22 个程序腾出 2.6 GB，可用内存多出 251 MB」。

### 验证

- 离屏：`F:\Codex\work\test_mem_trim.py`（11 组全过，含一组对照实验：跳过自己时工作集一根毛都没少、
  直接收自己则立刻掉到 3.2 MB）。
- 真机真鼠标：`F:\Codex\work\probe_mem_box.py`（右键 → 悬停「百宝箱」→ 子菜单 → 点「回收内存」→
  报数 → 再开菜单看到「上次…」→ 点勾选项 → config 跟着变），打印 `PASS`；截图
  `shot_mem_box_sub_box.png`、`shot_mem_after.png`、`shot_mem_box_sub_last.png`、`shot_mem_toggle.png`。
- 本机回收能力：`F:\Codex\work\probe_mem_optimize.py` —— 311 个进程里 141 个能碰到，
  工作集 12.2 GB → 0.29 GB，整轮 150 ~ 850 ms；170 个（系统/提权）连句柄都打不开。
- 回归：`F:\Codex\work\test_*.py` 全套 29 套里 27 套通过；`test_new_features.py`、
  `test_still_front.py` 这两套在**改动前的干净版本上就是同样的断言失败**（`git stash` 回退复跑确认），
  跟这次改动无关。

### 留给以后的

- **深度回收（清空备用列表 + 清系统文件缓存）**：实测 `NtSetSystemInformation(0x50, 4)` 返回
  `STATUS_PRIVILEGE_NOT_HELD`、`SetSystemFileCacheSize(-1,-1,0)` 返回 `GetLastError=5` ——
  **两条都必须管理员**。要做的话得单起一个提权 helper（`ShellExecuteEx` runas），
  代价是**会弹一次 UAC 框**、而且会把整机磁盘缓存扔掉（之后读文件重新走盘），
  跟"不影响应用"是直接冲突的，所以这一版没做。真要做，得先问主人要不要弹 UAC。
- **自动回收**：比如内存占用超过 xx% 时自己收一把（或者只在检测到大型游戏启动前收）。
  现在只有手动点，主人没提这个需求。
- 回收出来的量是"一次性"的：同一台机器上第一把收得最多（12.2 GB），紧接着再点往往只剩零头
  （实测第二把 1.3 GB）。要不要在气泡里也提一句，看主人反馈。

## v1.0.18（已完成：大小无级调节 + 锁定位置）

主人 2026-09-13 原话：

> 「更新功能，大小调节增加无级调节模块，原本5档模块不要取消，增加锁定功能，启用后会把桌宠固定在原地，
> 可以正常交互只是无法拖动，可以有效防止误触」

### 改了什么

- **大小无级调节**：菜单「大小」里五档原样保留（迷你 / 特小 / 小 / 中 / 大），后面加一条
  **「无级调节…（现在 xx%）」**，点开是滑块小窗口（`_slider_dialog` 那套，跟透明度/音量一样），
  范围 30% ~ 120%，拖着当场生效。滑块停在档位上时菜单里那一档照旧打勾（五档的倍率都是整百分数，
  滑块 1% 一格，正好对得上）。
- **锁定位置**：一级菜单下半部分（「回到屏幕内」下面）加了可勾选的
  **「锁定位置（拖不动·防误触）」**；勾上后按住鼠标拖也不动，松手那一下也不算点击/双击；
  单击、双击、右键菜单、按压 Q 弹全部照常。状态存进配置，重启还锁着。
  它只管"拖不动"：**想让它连散步都停 →「模式 → 原地待着」**（没合并成一条，两件事分开）；
  「回到屏幕内」「救急恢复」照旧能把它挪回来（锁在屏幕外就真叫不回来了）。
- **变大小改成原地缩放**：脚底和中心不动（以前是窗口左上角不动，从大换到小会整只往上跳）。
- 顺手把两套旧用例改了：`test_size_levels.py` ⑦（写死五条菜单）、`test_lines_dialog.py` ④b
  （写死 10 类，v1.0.17 加了「快速双击说的话」之后该是 11 类）。

### 验证

- 离屏：`F:\Codex\work\test_size_lock.py`（12 组全过）、`F:\Codex\work\test_size_levels.py`、
  `test_lines_dialog.py`，以及 `F:\Codex\work\test_*.py` 全套 28 套用例。
- 真机真鼠标：`F:\Codex\work\probe_size_lock.py`（子菜单五档+无级调节 → 点「小」→ 弹窗真拖滑块
  55→97（拖动中）→114（松手）→ 勾锁定 → 真拖 (+64,+40) 一动不动 → 单击/按下照常有反应 →
  解锁 → 同样拖动能走），打印 `PASS`；截图 `shot_size_submenu.png`、`shot_size_dialog.png`、
  `shot_size_dragged.png`、`shot_lock_menu_only.png`、`shot_lock_drag.png`、`shot_unlock_drag.png`。
- 手感：`F:\Codex\work\probe_size_diag.py` 每步 0.52 ~ 0.55 ms；`probe_sprite_crop.py` 确认
  自带素材裁透明边前后一模一样（255x340 / 222x340 / 253x340 都没有边可裁）。
- 合成鼠标拖拽的坑记在 `probe_drag3.py`：窗口是 `WS_EX_LAYERED`，透明像素上的消息会被送给下面窗口
  → 4px 一小步 + 起点取精灵正中；`SendInput` 后必须 `app.processEvents()`。
- 真机：主人这只桌宠已重启跑上新代码（窗口还在原地、42% 不变，`config.json` 自动多出 `locked: false`）。

### 留给以后的

- 锁定目前不挡"自由散步"（主人原话是"固定在原地……只是无法拖动"，理解成"防误触拖走"；
  真要连散步一起停，说一声就在锁定里一起管）。
- 无级档位和五档是同一份配置（`size` 存倍率），没有"记忆几个自定义尺寸"。

### v1.0.18 说明文案（主人 2026-09-13 追加）

主人原话：

> 「更新一下，这些解释说明以我的身份去发送就好，修改一下大小：原来那五档照旧，另外加了个"无级调节"」

- 发出去的说明（Release 说明 + CHANGELOG 的用户部分）改成第一人称：标题写成
  「修改一下大小：原来那五档照旧，另外加了个「无级调节」」，「主人 2026-09-13 原话：」
  改成「我 2026-09-13 提的需求：」（原话照旧保留）。第二段「锁定位置」的标题没动。
- 接着主人又说「我 2026-09-13 提的需求：> 「…」这种话就删掉就好了」→ **那段引用整块删了**，
  现在「修改一下大小」这条下面直接就是说明（Release 和 CHANGELOG 都去掉了）。
  **以后发出去的说明都别带"某某说的/需求原话"这类引子，直接写改了什么。**
- 没重发新版、没动安装包：v1.0.18 的 Release 只把说明换了一版（`gh release edit --notes-file`）。
- 顺手写了 `F:\Codex\work\make-release-notes.ps1`：以后只改说明、不重发版时，用它按发布脚本
  同一套规则（去实测行、去维护者那节）重出一份 Release 说明，再用 `gh release edit` 更新
  —— 注意脚本存成 UTF-8 **带 BOM**，不然 Windows PowerShell 5.1 读中文会乱码。

## v1.0.17（已完成：修「今日已用」把余额变动算成用量）

主人 2026-09-13 原话：

> 「现在修一个余额显示bug，今日显示余额异常，我今天最多用了15元，但显示了200多」

### 查到了什么

- 桌宠的「今日已用」是**本地按余额差值记账**（`usage.json`），没有平台用量接口。
  主人这台机 9-13 早上：账本 `todayUsage 270.67`、`lastBalance 21.57`；
  余额接口同一时刻报 `21.48`、`赠送余额 0.00`、`充值余额 21.48`。
- **主人 06:14 贴了平台用量页，真相坐实**：今天真实消费 **¥14.83**（1,438 次请求 / 344,272,679 tokens），
  充值余额 ¥21.11；余额从 35.94 掉到 21.11（对得上）。账本里那个 `292.24` = **赠送 271.13 + 充值 21.11**
  → 是**赠送额度整块到期/被收回**，平台不计入消费，旧版桌宠却把这段下降记成了 269.95 元用量。
  9-12 那笔 304.27 是同一个成因。
- 排除了"换账号"：全机（环境变量 / Codex config.toml / 桌面 api key.txt / 各备份）**只有一把 Key**，
  指纹 `cf2a9bed`，前后一致。
- 也试过官方用量接口（`/user/usage`、`/user/billing/usage`、`/user/bill`、`/usage`）→ 全 404，
  只有 `/user/balance` 能用。所以「今日已用」只能继续按余额差值算，判据做扎实才是关键。

### 改了什么

- `record_balance_usage()` 现在会先判断这次下降像不像用量：
  单次掉超过阈值（60 秒 20 元起，按采样间隔每小时 5 元放宽）或**赠送额度整块消失** → 记成
  「余额变动」（`todayAdjust`），**不计入今日已用**；真发生就冒一句解释。
- 账本按 **Key 指纹** 记账：换 Key / 换币种只重置基准，不把差额记成用量。
- 右键菜单「查看余额」下面加了一行灰色说明 `今天另有 ¥x 余额变动，没算进今日已用`（有变动时才出现）；
  **主人 2026-09-13 看过之后要求删掉**：「把这个灰色小字删了，奇奇怪怪的」→ 已去掉
  （真发生变动时气泡照旧解释一句）。验证见下面「v1.0.17 追加·删掉那行灰色说明」。
- 账本落盘改成临时文件 + `os.replace`，避免半截 JSON 被当空账本。
- 新增 `F:\Codex\work\calibrate_usage_today.py`：需要按平台数手工校准时用（这次把今天校准成
  `¥14.83`，账本里留 `calibrateLog`）。写回改成"老文件为底 + 覆盖自己管的字段"，不会被抹掉。
- **桌宠里加了同款功能**（主人 06:20 要求「加一个校准功能吧在余额菜单里」）：
  `calibrate_balance_usage()` + `PetWindow.calibrate_usage_dialog()`，
  菜单挂在 **余额 → 校准今日已用…（按平台用量页）**（用 `defer_dialog` 下一拍再弹，跟其它弹窗一致）。
  只改 `todayUsage`，不动余额基准和 `todayAdjust`，每次留 `calibrateLog`。
- 主人这只桌宠今天的账本已改回来：`270.67` 改判成余额变动，`todayUsage` 归零重新累计
  （旧账备份 `F:\Codex\work\usage-bak-20260913-060534.json`），并重启让它跑上新代码。
- 顺手修了自测的卫生问题：`test_double_click_peek.py` 会喂假余额 9.87 给 `_apply_balance`，
  以前**直接写主人的 usage.json**（这次真多出 11.42 元假账，按
  `F:\Codex\work\usage-bak-20260913-061004.json` 备份后改判）→ 现在这个用例把 `pet.USAGE_PATH`
  指到临时目录。以后要喂假余额的用例都得这么干。

### 验证

- 离屏：`F:\Codex\work\test_balance_logic.py`（10 组断言全过）、`F:\Codex\work\probe_balance_guard.py`
  （界面层：大跳变不计用量 + 会解释一句 + 之后小额照常累计）。
- 真机真鼠标：`F:\Codex\work\probe_menu_balance_info.py` → 菜单那行说明正常显示，
  截图 `F:\Codex\work\shot_menu_balance_info.png`。
- 真机在跑的这只：余额 21.57 → 21.29 期间，账本记 0.28，一分不差。
- 跑完 25 套自测再对账：账本只多了 0.04 元（真实消耗），`todayAdjust` 不再被自测改动。
- 和平台对账：按平台 `¥14.83` 校准后，桌宠显示 `今日已用 ¥14.87 / 余额 ¥20.90`
  （多出来的 0.04 是校准之后真实用掉的），截图 `F:\Codex\work\shot_today_after_fix.png`。
- 校准窗口（真机真鼠标，`F:\Codex\work\probe_calibrate_menu.py`）：子菜单里点到那一条 → 窗口弹出、
  带出当前值 → Ctrl+A 输 `12.34` → 回车 → 账本 `todayUsage=12.34` + `calibrateLog` 多一条 + 内存/气泡同步，
  跑完脚本自己把 `usage.json` / `config.json` 还原。截图 `shot_calibrate_dialog.png`。

### 还没弄明白 / 待观察

- 那 270 元到底是"赠送额度到期"还是"接口抽值"，平台侧查不到（余额接口不给明细）。
  下次再遇到，账本会在 `adjustLog` 里留一笔（时间 + 金额 + 判据），可直接拿这个去对照平台的余额明细。
- `history` 里 9-11（26.63）/ 9-12（304.27）两个老数字没动，成因大概率同上面一条。

## v1.0.17 追加（已完成：快速双击想弹什么，一级菜单里自己挑）

主人 2026-09-13 原话：

> 「更新一个功能，如果用户未设置apikey则快速双击不会触发余额窗口，在一级菜单加一个选项可以选择快速双击触发的窗口
> （天气，余额（未配置api则不可以选择），看一眼在放什么，或者自定义语录）」

### 做了什么

- 一级菜单最上面那块"快速查看"里多一条 **快速双击（现在：…）**，四项：看一眼余额（5 秒）/ 看一眼天气 /
  看一眼在放什么 / 说一句我写的台词；点一下即生效并写进 `config.json` 的 `double_click`。
- **没配 Key 时余额这一项不给选**（菜单里灰掉、后面写「要先配 Key」；硬选也不给存）；
  这种状态下快速双击**不会弹余额窗口**，还是原来的"换姿势"，只在本次启动提醒一次怎么配。
- 「说一句我写的台词」双击冒的是用户自己写的句子（新增台词类 `DOUBLE_CLICK_LINES`，
  默认 4 句）；子菜单里有 **改写这几句…** 直接开编辑窗口，**台词内容…** 窗口里也有这一类。
- 配套两处小改：双击之后那几秒**闲话让位**（`_dc_hold_until`，别把刚弹的那一眼盖掉）；
  双击不看余额时，闲着那句「双击我一下，余额马上给你看」不再冒（`DOUBLE_CLICK_HINT_LINE`）。
- 默认行为没变：没改过设置的用户，双击还是"看 5 秒余额"。

### 验证

- 离屏：`F:\Codex\work\test_double_click_choice.py`（15 组：四种选择各自走对窗口、没配 Key 不弹余额、
  菜单里余额项灰掉且没勾、没配 Key 选余额不给存、配置写坏退回默认、那句余额闲话会被过滤、
  双击后闲话不插嘴）。
- 回归：`F:\Codex\work\test_double_click_peek.py`（原来 6 组：5 秒余额 / 放歌不被歌词盖 / 待办冒泡）全过。
- 真机真鼠标：`F:\Codex\work\probe_double_click_menu.py` → 截图 `shot_double_click_menu_nokey.png` /
  `shot_double_click_lines.png` / `shot_double_click_menu_key.png` / `shot_double_click_balance.png`，结论 PASS。
- 真机真鼠标：`F:\Codex\work\probe_dc_edit_lines.py` → 子菜单「改写这几句…」点开台词窗口，
  「改哪一类」停在「快速双击说的话（自己写）」，截图 `shot_dc_edit_lines.png`，结论 PASS。
- 踩坑记录：写探针时 `mapToGlobal`（Qt 逻辑像素 2048×1152）没乘 DPR 1.25 就喂给 `SendInput`（物理 2560×1440），
  表现成"子菜单点着没反应"；用 `F:\Codex\work\probe_dc_menu_geom.py` 量的尺寸。以后写真鼠标探针先乘 DPR。

## v1.0.17 追加·删掉那行灰色说明（已完成）

主人 2026-09-13 原话（还配了一张右键菜单的照片）：

> 「把这个灰色小字删了，奇奇怪怪的」

### 做了什么

- 一级菜单「查看余额」下面那行灰色的 `　今天另有 ¥281.99 余额变动，没算进今日已用` 整条去掉
  （原来只在"今天有余额变动"时才出现，所以不是每个人都能看到那条）。
  「钱去哪了」照样看得到：真发生这种变动时，桌宠会冒一句
  `余额少了 ¥x，看着不像用掉的（…），这次没算进今日已用`。

### 验证

- 真机真鼠标：`F:\Codex\work\probe_menu_no_adjust_line.py` —— 先用主人的账本（`todayAdjust` 281.99，
  也就是会出现那条的条件）起一只桌宠，右键截屏：一级菜单里**没有任何带「余额变动」的条目**，
  第一块只剩 查看余额 / 查看天气 / 看一眼在放什么 / 快速双击…，截图 `shot_menu_no_adjust_line.png`。
- 离屏：`F:\Codex\work\test_menu_no_adjust_line.py` —— 同一个账本条件下 `_build_menu()` 的条目里
  没有 "余额变动" 字样；余额对象里的 `adjust` / `adjust_reason` 字段照旧（只是不再上菜单）。

## v1.0.16（已完成：外观自定义 → 「我的形象库」两本 + 上传整合）

主人 2026-09-13 的两次要求（原话）：

1. 「更新一下外观自定义功能，可以选择上传过的形象，上传形象时会让用户命名，
   记得提醒用户不要把上传的源文件删除，也不要随意更改路径不然会找不到」
2. 「三维的形象库和挂件的形象库分开来，三维的需要照片都上传了才会建立一个形象，
   形象库里三维形象展示默认为正面图片，把两个形象库和上传功能整合一下」
3. 「把形象的二级菜单变简洁一点，就留大鲸鱼和挂件还有全部恢复默认和形象库的四个选项，
   前两个的三级菜单可以取消掉了，和形象库和恢复默认功能重复了」

### 做了什么

- **两本库分开**（`config.json → skin_library`）：
  - `{"pet": [...], "widget": [...]}`；三维形象 = `{"id","name","front","side","back","added"}`，
    挂件形象 = `{"id","name","path","added"}`；正用着的记在 `custom_skins.pet` / `custom_skins.widget`。
  - **三维形象必须三张齐**（`SkinEditDialog` 的 `missing()` 卡着「保存」按钮）：
    少一张保存是灰的、也不会收录；换上时三张整套换（`add_pet_skin` / `apply_skin`）。
  - **库里门面默认是正面**（`skin_thumb_path`：列表图标 + 大预览都用 `front`，正面不在才顺延）。
- **上传 / 换图 / 改名 / 删掉整合进一个窗口**：`SkinLibraryDialog` 顶部一行切换两本库
  （下拉里带数量，默认先开你正用着的那本），共用一套列表 + 预览 + `用这个形象 / 换图… / 改名… /
  从库里删掉 / 上传新形象…`；上传和换图走同一个 `SkinEditDialog`（三维三行、挂件一行）。
- **菜单精简**（第 3 条要求）：`形象` 二级菜单**就四条** —— 大肥鱼（三视图）/ 小鲸鱼挂件（单张）/
  我的形象库…（三维 N 个 · 挂件 M 个）/ 全部恢复默认形象，**三级菜单整个删掉**；
  前两条点一下就切皮肤（打勾显示当前用哪套），上传 / 换图 / 单套恢复都去形象库窗口里做。
- **形象库里加了「这套恢复默认」小按钮**（主人批注「加上」）：只把**这一本**正用着的自定义形象
  退回自带的（`clear_custom_skin(kind)`），另一本和库里的条目都不动；这一本用的是自带形象时按钮是灰的
  （悬停有说明）。菜单里那条「全部恢复默认形象」照旧两本一起退。
- **提醒照旧写在窗口里**：橙色两行「别把源文件删掉」「别改它的路径 —— 改名、挪文件夹、换盘都会让桌宠找不到」；
  上传完再冒一句「（别删原图哦）」。名字在上传窗口里起（挑完图自动填文件名，可改）。
- **没挑完不白挑**：三维挑了一两张取消 → 记进 `skin_draft`，下次点上传自动带上接着挑。
- **缺图处理**：只让缺的那张退回自带（另外两张照旧是主人自己的图）；菜单标「（这套里有图找不到了）」、
  库里标 `⚠ 侧面那张找不到了` 并灰掉「用这个形象」、启动冒泡提醒一次、硬要换上会弹说明窗口
  （`_warn`：自己搭的置顶 QMessageBox —— 静态 `QMessageBox.warning` **不认 WindowStaysOnTopHint**，
  传进去会 TypeError，这个坑踩过一次）。
- **老配置自动升级**（`_migrate_skin_library`）：挂件那张 → 挂件库；三张都有的 → 一个三维形象；
  只有一两张 → `skin_draft`；老库里没人用的图 → 兜底进挂件库。
- 验证：离屏 `F:\Codex\work\test_skin_library.py`（12 组全过）、真机
  `F:\Codex\work\probe_skin_library.py`（真鼠标点开窗口 + 真字体截图）、
  `F:\Codex\work` 下 **25 套**自测全过。截图：`shot_skinlib_menu.png` / `shot_skinlib_dialog.png` /
  `shot_skinlib_widget.png` / `shot_skinlib_edit.png` / `shot_skinlib_missing_warn.png`
  （另有离屏版 `shot_skinlib_pet_offscreen.png` / `shot_skinlib_widget_offscreen.png`）。

### 还等主人定 / 真机确认

- **要不要"复制一份到桌宠目录"这个选项**：现在按主人的要求**只记原图路径 + 提醒别删别挪**（不复制文件）。
  以后想"原图删了也不怕"，可以在上传窗口加个勾选「复制一份到桌宠的 skins/ 目录再用」——主人说加再加。
- **发版规矩**（主人 2026-09-13 原话）：「之后如果我说推送等词，就把绿色安装版发送到 github 里，
  微信 zip 包留在之前一直放的文件夹里，在回复里给我和以前一样写出来就可以，
  桌面命令可能因为项目创建原因好像不管用了」→ 写在 `AGENTS.md` 的「发版」一节：
  **主人说「推送」= Codex 直接跑 `F:\Codex\work\publish-pet.ps1`**（提交 → 推送 → 打包 → 发新版），
  Release 只放绿色版 + 安装包，微信 zip 只做在本地 `outputs\` 并在回复里写出文件名 + 路径。
- **本地微信 zip 的内容**（主人 2026-09-13 原话）：「本地给我留的 zip 包里就是把 windows 安装版和教程
  打包就可以，其他不需要」+ 批注「对换回安装版，之后的也都是只保留安装版」→
  zip 里**就两样**：**Windows 安装版 exe（`大肥鱼桌宠-安装版-vX.Y.Z.exe`）+ `使用说明.txt`（教程）**，
  不放绿色版、不放源码（`New-WechatZip` 已改成这么打包，说明模板也改成了"先装再用"的写法）。
  回复里给 zip 要**写成能直接点的 Markdown 链接**（主人要求「之后都给我一个可以直接点的链接」）。
- **GitHub 上只留安装版**（主人 2026-09-13 原话）：「查一下 GitHub 里 exe 文件就给 windows 安装版就可以了，
  有这个版本的就只留这个，没有的不用变更」→ 已执行：v1.0.13~v1.0.16 删掉 `dafeiyu-pet.exe`（绿色版）
  只留 `dafeiyu-pet-setup.exe`，发版说明里那行也删了；v1.0.12 及更早只有绿色版 → 原样不动。
  以后 `publish-pet.ps1` 也只传安装版（没编出安装包才兜底发绿色版 + 警告）。
  README 顶部"下载最新版"的按钮跟着换成 `dafeiyu-pet-setup.exe`。
- **Release 说明要简洁**（主人 2026-09-13 原话）：「现在就改，减少不必要的文案 1.0.15 的就很好」→
  v1.0.16 的说明已重写成 v1.0.15 那种写法：标题 → `## 这一版改了什么`（用户视角小节）→ `## 下载` →
  最后一句「每一项的实测数据都写在仓库的 CHANGELOG.md 里。」（从 100+ 行砍到 44 行）；
  CHANGELOG 那节也重排成「用户部分 → `- 实测` / `- 回归` → `### 维护者相关`」；
  `publish-pet.ps1` 生成说明时会自动丢掉实测/回归行和"维护者相关"那一节（规矩写进 AGENTS.md）。
- 真机确认：主人平时怎么换形象就怎么走一遍（上传三维形象：三张都要挑 → 起名；
  挂件形象：一张 → 起名；然后在库里切两本、换图、改名、删掉）。
- 待办（小）：三维形象现在只能"整套换"（正面/侧面/背面就是这个形象的三张），
  如果想"同一张正面 + 两套侧面/背面"混搭，需要另加"单张替换"的入口 —— 等主人提。

## v1.0.10（第二批：音效 / 高亮 / 重叠再减）

### 已完成：自己加音效 + 重叠再减 25%

- **自己加音效**（`add_custom_sound_dialog` / `_add_sound_from_path` / `_register_sound` / `_remove_sound`）：
  - 菜单入口：「音效 → 添加我的音效…（自己挑 wav）」；有自定义音效时才出现「删掉我加的音效…」
  - `make_click_wav(src, dest, max_seconds)`：立体声→单声道、任意采样率→44100Hz、位深→16 位
    （常开流 `ClickPlayer.load_clip` 只吃 `1ch/2B/44100`，不转就会没声或开头被吞）；
    有 `audioop` 就用它，没有（以后 Python 会删掉）退回手写：16 位手动合单声道，其它位深直接拒绝并提示
  - **时长建议区间 `SOUND_IDEAL = (0.15, 0.60)` + `SOUND_TIP`**：太短问"要用吗"，太长问"裁到 0.60 秒吗"
  - 文件存 `USER_DIR/sounds/`，路径记进 `config.json → custom_sounds`，启动时自动加载
  - 测试：`F:\Codex\work\test_custom_sound.py`（转换 / 裁剪 / 注册 / 常开流 / 备用池 / 菜单 / 删除 全过）
- **重叠 12px → 9px**（两次各减 25%）：3 个位置 × 20 条 = 60 次实测，重叠 9~10px、顶部错位 0、
  实际落点和算的完全一致
- **菜单高亮跟着手走**（主人反馈"一级菜单快速上下移动时选择动画卡住"）：
  Qt 在这台机器上收不到菜单里的鼠标移动 → 高亮不更新。现在
  `_highlight_menu_item(menu, pos)` 由我们按光标位置点选中项：原生 WM_MOUSEMOVE（零点几毫秒）
  和 40ms 对账各来一次。实测一级菜单快速上下走 10 次 0 次卡住
- **换条时高亮不再被拽回旧条目**（主人反馈"从添加音效移到同级其它按钮难选中"）：
  branch ② 里加了判断 —— 光标如果落在同一个菜单的另一条上，就按"换条"处理
  （收掉旧子菜单、高亮给新的一条、清掉补弹计数）。实测二级菜单 8 次移动 0 次失败

## v1.0.10 待办

### 已完成（v1.0.10）：用"重叠"这一招修好二三级菜单

主人在 2026-09-12 提了两个方案，**选方案 2**，方案 1 弃用：

1. ~~加大一级菜单的触发范围~~：代码里留了开关 `MENU_TRIGGER_MARGIN_X`，**现在是 0（不启用）**
2. **让子菜单和一级菜单重叠 16px**：`MENU_OVERLAP = 16`
   —— 子菜单的落点在弹出**之前**就算好（`_submenu_pos`），所以不会"先出现在老位置再往里跳"

光靠重叠还不够，真机测试又揪出三个真问题，一起修了：

- **进三级菜单时二级菜单消失**：原来"换条就收掉上一个子菜单"会把光标脚下这一层也收走
  （三级菜单正是二级菜单打开的那一项的子菜单）。现在加 `_menu_covers` 判断 ——
  要收的那一层如果就是光标现在所在层（或它的上层），就不收
- **子菜单盖住一级菜单的条目**（最关键的一个）：子菜单往左弹时，**三级菜单会正好压在一级菜单
  「层级 / 透明度」那两条上**，于是那两条怎么悬停都不出子菜单。现在"往左弹"会**一层一层传下去**
  （`_dfy_flip_left`）—— 二级往左，三级继续往左，不会再压回一级菜单
- **残留的上一支子菜单还挂在屏幕上**：现在每 120ms 对账一次（`_prune_branches`），
  **一次只留光标所在的那一支**（Windows 原生也是这个规矩）

点击仍然由**原生消息层的点击摆渡**（`MenuClickBridge`）接管
（否则落子菜单上的点击会被送给持有捕获的上层菜单）。

**实测（真鼠标、拟人速度）**：

- 全菜单扫描（20 条带子菜单的项，只悬停不点击）：**右侧 20/20 正常、左侧 20/20 正常，闪烁 0 次** ✓
- 拟人流程：右键 → 悬停「形象」→ 移进二级 → 点「小鲸鱼挂件（单张）」→ 三级打开 → 点「用这个形象」→ **皮肤真的切换** ✓
- 光标停在三级菜单里 2.5 秒（左右各测一遍）：**二级 100% 可见、三级 100% 可见、翻转 0 次** ✓
- 11 个自动化测试套件全过（含菜单性能那套）✓

**托盘菜单（右键任务栏小图标）也一起修了**：

- 托盘这条路的菜单是 Qt 自己弹的，而且每次右键都会重建一份。万一它弹的是**上一份**，
  我们记着的 `_menu_keepalive` 就跟屏幕上的对不上（悬停兜底、收子菜单、点击摆渡全落空）。
  现在每 120ms 认一次"屏幕上真正开着的那棵菜单"（`_adopt_visible_menu`），认错了就换过来
- 子菜单被 Qt 收掉、光标还停在它原来那一片时，分三步往回捞（`setActiveAction` →
  替 Qt 往那一条塞鼠标移动 → 都不行才自己补弹）
- 实测：托盘图标在 Win11 里收进了"隐藏的图标"溢出（Qt 拿不到它的矩形），所以按**真实托盘位置
  （屏幕右下角）**触发同一套菜单代码来扫：**20/20 正常、闪烁 0 次**；
  另外专门模拟了"Qt 弹的是上一份旧菜单"这种最坏情况，同样 20/20

**还需要主人真机确认**：自己按平时的速度左右各走一遍（尤其"斜着冲进三级菜单"）。

### 重叠像素：量过了（2026-09-12，主人问"为啥有时多有时少"）

把桌宠放在 **4 个位置**（屏幕最左 60,200 / 中间 1000,200 / 最右 1900,200 / 靠下 1000,900），
每个位置把**全部 20 条带子菜单的项（含二级、三级）**都走一遍，共 **80 次**测量：

- **横向重叠恒为设定值**（`MENU_OVERLAP`，一开始 16 → 80/80 都是 16；
  主人说"压得有点多"，按 25% 减到 **12** → 60/60 都是 12），没有被屏幕边缘钳制过
- 纵向固定是"子菜单顶边比这一条顶边高 4px"，实测偏差只有 0 或 -1px（DPI 取整）
- **子菜单实际落点和 `_submenu_pos` 算出来的位置 0 处偏差** → 说明不是"我们算得飘"，
  量到的都是真实窗口矩形（`GetWindowRect`，换算成逻辑像素）

那"看着忽多忽少"是怎么来的？顺着量下去揪出三个真问题，都修了：

1. **残留子菜单挡住下一次右键**：Qt 有时不跟着根菜单一起收子菜单（`root.close()` 之后
   二级菜单还留在屏幕上）；这个"孤儿"弹窗会让下一次右键**弹不出菜单**（实测修前 4 轮全失败，
   修后 4/4 正常）。新增 `_close_orphan_submenus()`：根菜单一关就收掉残留子菜单
2. **"认屏幕上那棵树"会把孤儿子菜单误当成根**（`_adopt_visible_menu`）→
   改成在**所有**菜单（含已关掉的）里找父关系，孤儿一律不接管
3. 子菜单被 Qt 收掉、光标还停在它原先那一片时，分三步往回捞
   （`setActiveAction` → 替 Qt 塞一个鼠标移动 → 都不行才自己补弹）

> 想调重叠大小就改 `桌宠.py` 顶部的 `MENU_OVERLAP = 12`（想更贴近就调小，想更"咬住"
> 一级菜单就调大）。

### 主人截图反馈："位置/重叠忽上忽下，出现时全部二级菜单都这样"（2026-09-12 8:17）

截图里二级菜单整体比悬停的那一条**高出一截**，而且主人说一旦出现就是**所有二级菜单**都这样
（两侧同理）—— 这种"整体偏移"说明不是某一条算错，而是**某个全局状态**或者
**屏幕上多出来的那一份旧子菜单**。反复量了 80 次都没复现出偏移，所以改成"兜住"：

1. **位置自愈**：`_menu_hover_watch` 里每 120ms 对账一次 —— 光标压在带子菜单的条目上时，
   先把该在的位置算出来（`_submenu_pos`），**只要屏幕上的子菜单偏了 2px 以上就挪回去**。
   谁摆歪的都一样治（Qt 自己摆的 / 弹出时尺寸没算准 / 被别的动作挪过）。
   实测：故意挪歪 37/63 像素 → 0.45 秒内回到"横向重叠 16px、顶部错位 0"，一级和三级都验过 ✓
2. **残留子菜单清理**（上一轮的 `_close_orphan_submenus`）：根菜单一关就把残留子菜单收掉，
   避免屏幕上同时存在"上一份树的子菜单"（它的位置是按旧菜单算的，看起来就是整体偏了一截）
3. **排查开关**：右键菜单里加了「记菜单日志（菜单出问题时打开）」。
   打开后 `menu-debug.log` 会记：每次展开子菜单算出的位置、这一条的 rect、
   以及每次"自愈挪回"的 前→后 坐标。**再遇到就直接开它复现，把日志给我**。

4. **"先小部分重叠、再往里移动"这是真的，而且找到原因了**（2026-09-12 晚）：
   Qt 自己弹子菜单时用它那套摆法（贴着条目边、不重叠、还可能高一截），
   我们的对账要等到下一拍（原来最多 120ms）才发现位置不对再挪过去 —— 眼睛看到的就是两段位移。
   现在：
   - 给每层子菜单装了 `SubmenuPlaceFix`（事件过滤器），**Show 的那一帧就把位置摆正**
     （实测：故意按 Qt 的摆法弹出来，8/8 都是同一帧摆到位，看不出挪动）
   - 菜单开着时对账间隔 120ms → **40ms**（`MENU_HOVER_FAST_MS`），菜单关掉自动回 120ms
   - 光标还停在原地、子菜单却被 Qt 误收时**自动原地弹回来**（以前 0.35 秒宽限后放弃，
     20 条里偶尔会中一条"进去就没了"）
   - 重叠按主人要求从 16px 减到 **12px**（`MENU_OVERLAP`）：3 个位置 × 20 条 = 60/60 都是 12px

### 备选（还没做，v1.0.10 之后如果还想更稳再上）：把右键菜单换成"原生 Win32 菜单逻辑"

**为什么**：这台 Windows 上，Qt 的**弹出式子菜单**（`QMenu` 的 SubMenu）拿不到输入：

- 鼠标**移动**消息不会送给弹出子菜单（全被上层菜单或桌宠窗口收走）→ Qt 以为"鼠标离开了"，
  每隔约 300ms 把子菜单收一次 → 看着就是"闪一下 / 来不及点"
- 鼠标**点击**消息会被送给**持有鼠标捕获的上层菜单**（不是被点的那一层）→
  Qt 一看坐标在自己外面，就把整个菜单关掉 → 二级、三级菜单"点了没反应"
  （抓原生 `WM_LBUTTONDOWN/UP` 确认过：点击落在子菜单上，消息却发给了上层菜单）
- 表现还跟屏幕位置有关：**桌宠在右边时往左弹、在左边时往右弹，各自出问题**

**主人在 2026-09-12 给出的关键观察**：对比左右两种弹出方式，
**左边的二级菜单和一级菜单重叠更少**（子菜单被 Qt"翻转 + 贴边"挪开后，离被悬停的那一项更远），
于是鼠标从一级移到二级的路上**离开了"触发二级菜单的范围"** → 子菜单被收掉。
也就是说：**触发范围不能只按"菜单项矩形"算，必须包含"从这一项到子菜单的整条路径"**。

**怎么做（原生 Win32 的做法）**：

1. 菜单窗口自己 `SetCapture()` 持有鼠标捕获 —— 之后所有移动/点击都直接送给菜单，
   不再出现"消息送给别人"的问题（这正是 Qt 在这台机器上做不到的地方）
2. 子菜单自己画（一个无边框 `Qt.Tool` / `WS_POPUP` 窗口 + 自绘列表），
   不再用 `QMenu` 的 SubMenu 层级；展开/收起全部由我们自己控制
3. 收起规则按 Windows 原生那套：
   - 光标在这一项上 → 保持
   - 光标在子菜单里 → 保持
   - 光标在"**这一项 → 子菜单**"的走廊里 → 保持（走廊按两点之间的矩形带算，
     而不是简单按像素放大 —— 这样斜着走、绕一点路都不会误关）
   - 移到同级另一项 → 立刻换（旧子菜单马上收）
   - 点菜单外面 / Esc → 全部收起
4. 展开延时用系统值（`MenuShowDelay`，默认 400ms）；已经展开过时立即展开

**验收方式（真鼠标，拟人速度）**：

- 桌宠分别在**屏幕左侧 / 右侧**，各走一遍二三级菜单（斜着走过去点）
- 记录：子菜单可见比例、是否出现"两个子菜单同时开着"、能否点中最后一项
- 参照物：Windows 桌面右键菜单（`#32768`）的手感

### 参考：已经试过、都没能彻底解决的方案（别再重复踩）

悬停兜底（自己盯着 `QCursor.pos()` 代弹子菜单）、拦截/转发鼠标事件、
延时 0.5 秒才收、被误关自动补弹、`setActiveAction` 展开、`popup()` + `grabMouse()` 抢捕获、
原生消息层拦截点击并转发、把子菜单改成"点进去"式、改成独立浮窗、改成选择框对话框。

结论：这些都是在 Qt 的弹出菜单框架里绕，绕不过"没有捕获"这个根；
所以 v1.0.10 走"自己实现菜单窗口 + 自己持有捕获"的路。

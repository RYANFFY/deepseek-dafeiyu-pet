# 更新日志

## v1.1.2

### 设置窗口里的数字，不用重启就跟着变

- 「桌宠形象」页那颗 **「我的形象库…（三维 N · 挂件 N）」**：在库里 **上传 / 换图 / 改名 /
  从库里删掉** 之后，后面的条目数**当场**就更新 —— 不用关掉设置窗口再打开，更不用重启桌宠。
- **「现在是谁」**那一排（大肥鱼 / 小鲸鱼挂件）也跟着实际用着的那套走：在别的地方换过形象，
  回到这一页会跳到正确的一档上，不会停在旧的那一档。
- **音效**页的「音效选择」名单：自己加过 / 删过的音效，回到这一页就在名单里，
  下拉里选中的也是正在用的那套。
- **「台词内容」**那行的「已改 N 类」、**「回收内存」**那行的「上次：N 个程序腾出 … MB」：
  回到那一页读的都是最新的数。
- **「现在是谁」**那一排现在写**实际在用的那个形象的名字**：用着库里的自定义形象，
  档位上就是它的名字（比如「三姐妹（三视图）」）；没自定义过还是「大肥鱼（三视图）」、
  「小鲸鱼挂件（单张）」。**「（三视图）」「（单张）」是按本来的**，一直跟着这一档走；
  名字太长只会截名字（悬停能看到全名），不会把括号里那四个字挤掉。
- 做法统一成一套"现读一遍"：**每次切回某一页、或者把设置窗口重新露出来**
  （收起来过一阵再打开、从任务栏点回来）都会重读该页上会变的数据，
  而不是建页面的时候读一次、之后再也不管。

### 气泡风格自己挑：深色界面下不再是刺眼的白气泡

- **「桌宠形象 → 外观」里多了一条「气泡风格」**（跟随界面 / 浅色 / 深色）：
  **语录、余额、歌词三种气泡一起换**。
- **默认「跟随界面」**：设置窗口选亮色，气泡就是白底深字；选暗色，
  气泡换成深底浅字 —— 深色主题下原来那块白气泡太扎眼，这条就是冲着它来的
  （「跟随系统」也跟着系统的亮暗走）。
- 想固定一种也行：选「浅色」/「深色」之后跟界面主题脱钩，界面怎么切它都不变。

### 动效：切页时上一屏淡淡化开，不想看就关掉

- 设置窗口**切分类时，上一屏会淡淡地化开**（150 毫秒，只动透明度、不动位置、不挪位置）——
  原来是一下子"啪"地换页。
- **修掉第一版那个"很生硬的卡顿错位"**：第一版是让新页面淡入（Qt 的
  `QGraphicsOpacityEffect`），它会先把整页塞进一张离屏图里重画一遍，
  配这个"无边框 + 半透明"的窗口会闪、还会错位。现在改成**拍一张"上一屏"铺在窗口上、
  按自己的拍子淡掉**，新页面自然就浮出来了 —— 一帧只是贴一张图，不重画整页。
- **「通用 → 动效」**里两颗开关：
  - **页面切换动效**：上面那条淡出的开关，不想看就关掉。
  - **减少动效**：一开，这个窗口的动画**全都不播**（切页直接换，
    最小化 / 从任务栏点回来也直接到位，不拍截图、不插帧）—— 省电，
    也照顾不喜欢画面动来动去的情况。

- 实测：离屏脚本 `F:\Codex\work\test_console_live_refresh.py`（10 组，22 项全过）——
  空库时按钮写「三维 0 · 挂件 0」；在形象库里收一条、关掉窗口**当场**变「三维 0 · 挂件 1」；
  切到别的页再切回来读到「三维 1 · 挂件 1」；删掉一条变「三维 1 · 挂件 0」；
  在别处换回大肥鱼后「现在是谁」跟着跳；设置窗口收起来、在别处加了形象，
  重新露出来写的就是最新数；音效名单切回来多了自己加的那套、下拉选中它；
  「已改 1 类」和改回默认都对得上；回收内存那行再进来读的是最新那次
  （「上次：7 个程序腾出 512 MB」）。截图 `F:\Codex\work\shot_skinlib_count_live.png`、
  `F:\Codex\work\shot_console_appearance_live.png`。
- 回归：设置窗口那几支老脚本照旧全过 —— `test_console_page.py`（12 页切一遍 + 最小窗口）、
  `test_console_ui.py`、`test_console_style.py`（45 项）、`test_console_click.py`（12 项）、
  `test_console_perf.py`、`test_console_backdrop.py`（12 项）；
  `test_skin_library.py`（形象库本体）也照旧 ALL PASS。
- 实测：气泡风格 `F:\Codex\work\test_bubble_style.py`（22 项全过）——
  默认 auto；界面切暗色 → 气泡自动用深色那套；手动选浅色时界面暗着也还是浅色；
  **把桌宠真画出来取气泡那一带的底色**：浅色 (255,255,255)、深色 (31,35,49)，
  心声 (232,232,238) → (45,50,68)，余额和歌词气泡同样换；歌词那块的**排版缓存**
  也跟着重算（字色从 #262c42 变 #e9ebf4）；「桌宠形象 → 外观」里那一排点一下就写进配置；
  老配置里存着「暗色」时，**刚启动、还没开过设置窗口**气泡就已经是深色。
  截图 `F:\Codex\work\shot_bubble_dark_auto.png`（深色界面下的深气泡）、
  `shot_bubble_dark_light.png`（同一界面下强选浅色）、`shot_bubble_style_row.png`。
- 实测：动效 `F:\Codex\work\test_console_motion.py`（30 项全过）——
  切页确实铺上了那张"上一屏"、跑完自己撤干净、12 页连着快速切一圈没有残留、
  关掉开关就不播；**一页上都不许出现 `QGraphicsOpacityEffect`**（第一版那个坑）；
  **「减少动效」开着时最小化走系统的直接最小化
  （不拍截图、不留素材），还原之后真控件都在**；关掉之后最小化 / 还原照旧走动画；
  两个键都写进 `config.json`。
- 实测：切页动效**逐帧**看过（`F:\Codex\work\probe_page_fade2.py`，28 张图在
  `F:\Codex\work\fade2\`）—— 第 1 帧跟"切之前那一屏"逐像素一样（差 0.00），
  之后 16 / 31 / 63 / 78 / 109 / 125 / 141 毫秒一路平滑过去
  （跟旧页的差 0 → 12.38、跟新页的差 12.38 → 0.00，单调、没有跳变），
  **每一帧里第一张卡片的左边都在 x=232**（没有错位）；帧间隔稳定在 15~16 毫秒。
- 回归：这条动效是**返工过一次**的 —— 第一版用 `QGraphicsOpacityEffect` 让新页面淡入，
  实测出来是"很生硬的卡顿错位"（离屏重画 + 分层窗口，会闪会偏）。现在这版不再用任何
  QGraphicsEffect。
- 回归：桌宠本体那批用例一起跑了一遍 —— 歌词折行 / 歌词卡住与位置记忆 / 音频设备跟随、
  峰谷显示、余额逻辑、自定义台词与音效、大小五档与无级、锁定位置、吸附翻面、
  鼠标不动模式、音量透明度窗口、菜单性能、应用扫描、天气、回收内存：全过。
  顺手修了三支早就过期、跟这次改动无关的用例：`test_quick_menu.py`
  （侧边分类少了 v1.1.1 新增的「控制台外观」一页）、`test_size_levels.py`
  （菜单里那条早就从「无级调节…」改名成「精确调节…」）、`test_new_features.py`
  （应用搜索那条假定这台机器上只有一个名字带 steam 的应用，实际有两个）。

### 维护者相关（发版 / 仓库，不写进 Release 说明）

- 「数字不用重启」那一批没动桌宠本体：那几处一直是设置窗口"读晚了"，
  数据源（`pet.cfg` / `pet.skin_library()` / `pet._sound_names()` / `pet._mem_last`）
  本来就是现成的。气泡风格那批才动了 `桌宠.py`（见下面几条）。
- `ui_console.py`：
  - `ConsoleWindow._page_hooks` + `_hook_page(key, fn)` / `_refresh_page(key)` /
    `_refresh_current_page()`：页面级的"现读一遍"挂点。页面是建一次留着的，
    凡"会变的数据"都往这儿挂；`switch_page()` 和 `showEvent()` 各调一次。
  - `Page.buttons()` 返回的那行挂 `wrap.buttons = [...]`，外面改按钮文字不用再
    `findChildren` 赌顺序。
  - 「桌宠形象」页：`_skin_lib_text()`（条目数现读）、`_sync_skin_controls()`
    （条目数 + 「现在是谁」）、`_open_skin_library()`（关掉形象库窗口立刻刷一次）、
    `_reset_all_skin()`；`_skin_name()` / `_skin_option_text()` / `_shorten()`
    负责"现在是谁"那一排的名字（`Segmented.set_label()` 是这次新加的，
    换文字不会动那一排的选中逻辑；值仍然是 `SKIN_PET` / `SKIN_WIDGET`）。
  - 「音效」页：`_sync_sound_controls()`（名单变了才重建下拉，重建时 `blockSignals`）、
    `_add_sound()` / `_remove_sound()` 各包一层；「文案和语录」页 `_sync_lines_controls()`；
    「性能和工具」页 `_sync_mem_hint()`（回收是后台跑的，点完那一下数还不准，
    回到这一页再读）。
  - `APP_VERSION`：1.1.1 → **1.1.2**。
- 气泡风格（`桌宠.py`）：`BUBBLE_STYLES`（auto / light / dark）、`BUBBLE_INK`
  （两套颜色：气泡底 / 正文字 / 心声 / 余额三行 / 歌词三行）、`BUBBLE_PEAK_COLORS`
  （峰谷那行在深色底上要亮一点）；`_bubble_theme()` / `_bubble_ink()` /
  `set_bubble_style()`；`_lyric_rows()` 多收一个 `ink`（不给就按现在生效的那套，
  老用例不用改），歌词**排版缓存的 key 里带上了 theme** —— 颜色是烘在缓存里的，
  不带就会"换了风格字色不变"。`_bubble_theme()` 在「跟随系统」那档才缓存 1 秒
  （画气泡每帧都要问一次，而那条路要读注册表）；亮 / 暗是定死的，直接算不缓存。
- `ui_console.is_dark()`：给桌宠用的"现在实际是亮还是暗"。另外 `PetWindow.__init__`
  现在**一启动就 `ui_console.set_mode(cfg["ui_theme"])`** —— 以前要等设置窗口建出来
  才 set_mode，所以"刚启动、还没开过设置"时气泡会按系统亮暗走，老对话框取主题色也偏一档。
- 动效（`ui_console.py`）：`PAGE_ANIM_MS = 150`；`_page_anim_on()` / `_reduce_motion()` /
  `_set_motion()`；新类 `_PageFade`（铺在壳上的一张截图，`paintEvent` 里
  `setOpacity` + `drawPixmap`，`WA_TransparentForMouseEvents` 不吃鼠标）+
  `_start_page_fade()` / `_page_fade_tick()` / `_stop_page_fade()`；
  `switch_page()` 里**先 `_start_page_fade()` 再 `setCurrentWidget()`**
  （截图必须在切页之前拍），拍之前先 `_stop_page_fade()` 撤掉上一张
  （否则会把半透明的旧图拍进去）。拍的是**整个壳**（含侧边栏和底图），
  这样淡出时只有内容区在变、侧边栏看着不动，也不用换算坐标。
  **别再回到 `QGraphicsOpacityEffect` 那条路**：整页离屏重画，分层窗口下会闪、会错位
  （主人 2026-09-14 报的"很生硬的卡顿错位"就是它），而且一帧就贵得多。
  `minimize_with_anim()` / `restore_with_anim()` 开头加了「减少动效」分支：
  最小化前要 `self._min_self = True`（不然 `changeEvent` 的兜底会把窗口拉回来再播一遍），
  还原走 `showNormal()` + `_finish_restore()`（它本身是幂等的收尾）。
  `console_page_anim` / `console_reduce_motion` 加进 `BACKDROP_DEFAULTS`，
  所以「全部恢复默认」也会把动效复位（`_sync_motion_switches()`）。

## v1.1.1

### 设置窗口能换背景了：图片 / 视频都行

- 左侧多了一类 **「控制台外观」**，专门改**这个设置窗口自己**的样子：
  选一张图或一段视频铺在窗口最底下，左边分类和卡片浮在上面。
- 只有 **「导入文件…」** 这一个入口：图片和视频一起给你挑，是哪一种**看文件后缀**
  （不用先选一遍类型）。旁边一颗 **「移除」**，不想要了点它就回纯色。
- **自动适配窗口**：开着 = 铺满整个窗口，旁边那个**九宫格**挑"留哪一块"
  （竖图想留上半身点上面、想留人脸点中间）；关掉 = **完整显示**，图 / 视频一点都不裁，
  空出来的地方是主题底色。
- **亮暗 / 模糊 / 遮罩**三个滑块，边拖边生效。亮暗 **-100 ~ +100**（负数压暗、
  正数提亮），模糊 **0 ~ 40**，遮罩 **0 ~ 100%** —— 遮罩就是往底图上蒙一层底色，
  调高一点底图淡下去、字更清楚，图太花的时候用它。
- 视频背景**循环播放、不带声音**；窗口收起来或者最小化的时候会**自己暂停**，不白烧电。
- 底图文件被删了 / 挪走了也不怕：窗口自动退回原来的纯色样子，不会变成一片空白。

### 滚轮不再改数值，数字可以直接填

- **滚轮划过滑块 / 下拉，值不再跟着变**（鼠标顺手一滚就把参数改了，那是误触）。
  页面该滚还是照滚 —— 滚轮会转给外面的滚动区，不会"卡住不动"。
- **每个滑块右边的数字都能点**：点一下弹出小窗口，**直接填数字**（只收整数）。
  填超了上限 / 下限也没关系，它只会给到上（下）限那个数。

### 强调色：整个界面换一个主色

- 色卡一排，**点一下就是它**：默认 / 蓝 / 青 / 绿 / 紫 / 橙 / 玫红。
  选中态、主按钮、滑块、开关全都跟着变。
- **「自定义…」打开取色盘**：色轮、RGB / HSV、HTML 色号都行 ——
  **右边那颗吸管能直接吸屏幕上任意一点**（想让主色跟着壁纸，吸一下就完事）。
- 色卡最后那颗**吸管图标** = 从背景图里挑一个颜色：挑的是图里最鲜艳的那个，
  不是随便取一点。
- hover 和选中底色**是跟着这个颜色算出来的**，不用一个个配。

### 名字和图标也能换成自己的

- 左上角「大肥鱼桌宠」这几个字随便改（清空就回默认），旁边那个小图标也能换成自己的图。
  方形图最好，会自动缩到 26 像素，不会拉变形。
- **「全部恢复默认」**一把把背景、颜色、标题、图标清干净。

### 卡片和窗口

- **卡片不透明度 40% ~ 100%**：调低一点，背景图就从卡片里透出来；**圆角 0 ~ 18**。
- **记住窗口大小和位置**（默认开）：下次打开还是你上次摆的样子；
  换了显示器、拔了屏也会自己拉回看得见的地方。
- **打开时先看哪一页**：想让它一开就停在某一页上也行。

### 顺手做的几处

- 「形象和外观」这一类**拆成了两个**：**「桌宠形象」**管桌宠自己
  （换成谁 / 多大 / 多透明 / 在哪一层），**「控制台外观」**管这个设置窗口的样子。
- 开着背景时，左侧分类和页面底会**透出一层**、卡片仍然是实心的：底图看得见，字不会跟着糊。
- **修好一个一直没人发现的老毛病**：打开设置窗口时，标题栏那片是**空的** ——
  代码里切的是 v1.1.0 就删掉的「首页」，等于什么都没切。现在一打开就是当前页的名字。
- 分类从 11 条变成 12 条，窗口最小高度 560 → 620（不然底下那行版本号会被挤出去）。
- 这一页的说明文字**全部改短**：一行以内说完，不再一段一段解释。

- 实测：底图为 940×660 时，换一次底图 4.0 毫秒；模糊 10 / 25 / 40 都是 7.7 毫秒；
  模糊 40 再压暗 40 是 8.4 毫秒；视频每帧（模糊 40）7.3 毫秒 —— 15 帧/秒的预算
  是 66 毫秒，够用（`F:\Codex\work\test_console_perf.py`）。
- 实测：图片背景、模糊、亮暗、遮罩、暗色主题下的底图、自定义标题、自定义图标、
  视频出帧、恢复默认、配置落盘，12 项一次全过（`F:\Codex\work\test_console_backdrop.py`）。
- 实测：强调色 / 色卡点击 / 从背景图取色（取到 #af613a）/ 卡片 rgba + 圆角 /
  **自动适配开关 + 九宫格截取位置（左上和右下画出来不一样）+ 完整显示不裁图** /
  **点「移除」之后底图没了、按钮写回「导入文件…」** /
  记住窗口（900×660 @(120,90)，关掉重开一模一样）/ 起始页 /
  最小化动画不污染窗口大小 / **滚轮划过滑块值不变、页面照常滚、下拉选项不变** /
  **点数字能填数、填超上限只到上限** —— 全过（`F:\Codex\work\test_console_style.py`）。
- 实测：这一页上的控件**真点一遍**（发真的鼠标 / 键盘事件，不是直接调函数）——
  点「自动适配窗口」开关、点九宫格右下那一格、把模糊滑块从 0 拖到 40、
  往标题框里打字、点「全部恢复默认」。色卡也点了一遍：点绿 → 主色变 #22a06b，
  点「默认」→ 回到主题自带那个色（`F:\Codex\work\test_console_click.py`）。
- 实测：取色盘用的是 Qt **非原生**那一套，`Pick Screen Color`（吸管）那颗按钮在，
  色轮 / RGB / HSV / HTML 也都能用（`F:\Codex\work\probe_color_dialog.py`）。
- 实测：12 个分类页在最小窗口（820×620）下放得下，色卡那一排在最窄的窗口里也放得下，
  底下那行版本号没被挤出去（`F:\Codex\work\test_console_page.py`）。
- 实测：打包版里视频背景真能解码 —— 冻了一个一次性小包，`MEDIA_OK=True`、
  收到 2560×1440 的视频帧（`F:\Codex\work\probe_frozen_video.py`）。
- 回归：49 个图标全都画得出来、12 个分类页都在、亮暗主题样式表真的换、
  老对话框（形象库 / 上传形象）配色照旧 —— 全过（`F:\Codex\work\test_console_ui.py`）。
- 回归：设置窗口的最小化 / 还原动画、右键菜单、桌宠本体的行为都没动过一行。

### 维护者相关（发版 / 仓库，不写进 Release 说明）

- `ui_console.py`：
  - 新常量 `BACKDROP_DEFAULTS`（14 个 `console_` 键）、`BG_FOCUS`（九宫格）、
    `ACCENT_PRESETS`、`IMAGE_FILTER` / `VIDEO_FILTER` / `MEDIA_FILTER`、`VIDEO_EXTS`、
    `VIDEO_FRAME_S`（视频 15 帧/秒上限）。
  - 新函数 `load_image` / `scale_cover` / `brighten_image` / `blur_image` / `pick_accent`。
    **模糊走的是"缩到 1/s 再放大回来"，做两遍**，不是逐像素卷积：
    125% 缩放下 1150×800 的图卷一遍高斯要几百毫秒，拖滑块会一顿一顿的；
    缩放法等效一次盒式模糊，几毫秒就完事。亮暗同理，整张蒙一层黑 / 白。
    `pick_accent` 缩到 64×64、按色相分 12 桶、按 饱和度×亮度 加权取最鲜艳那桶，
    太暗的提亮到 V=175。
  - `scale_cover(img, w, h, focus)` 多了一个九宫格参数；`bg_kind_of(path)`
    按后缀判图片还是视频（**不再有"类型"这个设置项**，也就没有
    「不用」和「清掉」做同一件事的坑）。平铺整个删掉了。
  - 新类 `_WheelGuard` + `install_wheel_guard(app)`：全程序装一个应用级事件过滤器，
    滑块 / 下拉收到滚轮时**不自己处理，把事件转交给外面的 QScrollArea**
    （页面照常滚，值不变）。滚动条自己的滚轮不拦。
  - 模块级 `_accent_pick` / `_card_alpha` / `_card_radius` + `set_accent` / `get_accent` /
    `set_card_style` / `card_style` / `default_accent`；`refresh_tokens()` 里多一步
    `_accent_into`（主色 / hover / 选中底 / 主色上的字，四个值都由用户挑的那一个颜色算出来）。
    放模块级是因为 `build_qss()` 和一堆老对话框都读同一套调色板。
  - 新类 `ColorSwatches`（一排色卡 + 「自定义…」+ 吸管那颗）。「自定义…」那颗自己也显示颜色：
    当前颜色不在预设里时它会被点亮，就不用再单独占一行说明现在是哪颗。
  - 新类 `AlignGrid`（截取位置的九宫格）。
  - `Page.row()` 左边那栏文字改成"整块居中"：右边控件高（九宫格）时，
    原来标题和说明会被拉开成一个在顶一个在底。
  - `Page.slider()` 右边的数字从 QLabel 换成 `QPushButton#numBox`：
    点一下走 `QInputDialog.getInt(min, max)`，**只收整数、超范围自动夹到上下限**。
  - 样式表新增 `$card`（卡片 rgba 底色）、`$card_radius`，以及 `QPushButton#swatch` /
    `#swatchText` / `#swatchIcon` 三段。**半透明只给卡片**：下拉弹窗、列表、按钮还是
    `$surface` 实色，不然弹出的菜单会把桌面透出来。
  - 新类 `ShellFrame`：窗口壳，底图画在它的 `paintEvent` 里（最外层子控件，
    天然在所有内容底下）。`build_qss(glass=True)` 把壳 / 侧边栏 / 页面底的底色
    换成"透一层"，卡片保持实心。样式表只在**真的换了玻璃状态**时重下（`_glass_state`），
    不然"文件被删掉"那一下会留着透明底 → 窗口一片空白。
  - 背景铺法在 `_paint_backdrop()`；视频永远按 cover 画（平铺的视频一帧要贴几百次）。
  - 窗口几何：`_geo_save` / `_geo_restore` / `_geo_clamp`（存 base64，回来按可用屏幕夹一次）。
    **保存前跳过"最小化动画 / 还原中 / 已最小化 / 还留着最小化截图"这几种状态** ——
    不然会把"往任务栏收的那一粒"记成窗口大小。
  - `Page.row()` 多挂了 `wrap.title_label` / `wrap.desc_label` 两个引用
    （有几行的说明要就地刷成回执）。
  - 视频：`QMediaPlayer` + `QVideoSink`，每帧 `toImage()` 后走同一套亮暗 / 模糊。
    `MEDIA_OK=False`（QtMultimedia 没起来）时「视频」那一档直接不显示，其余照常。
- `桌宠.py`：`cfg_defaults` 加 14 个 `console_` 键（老 config.json 会自动补上）；
  `main()` 里 `PetWindow()` 建完就 `ui_console.set_accent(...)` / `set_card_style(...)`
  各套一次 —— 免得从右键菜单直接开老对话框时用的还是默认配色；
  `main()` 里还要 `install_wheel_guard(app)`，让音量 / 透明度那些老窗口也吃上"滚轮不改值"。
- 新图标 `assets/icons/nav.console.svg`（Lucide `app-window`）、
  `assets/icons/ui.pipette.svg`（Lucide `pipette`），都是 ISC。图标总数 48 → 49。
- 按主人"操作太复杂"的意见**删掉了「侧边栏宽度」**那一项（代码里也一并去掉了
  `console_sidebar_width`）；要回来的话是三行的事。
- 打包：`ui_console.py` 里直接 `import PySide6.QtMultimedia`，PyInstaller 会跟着
  收 `Qt6Multimedia.dll` 和它带的 ffmpeg（`avcodec-61` 等，约 18MB）；
  验收时另起一个 `--distpath` 出包，**没有覆盖 `dist/` 里已发的那几个安装包**。
- 打包版那条路单独验过：`F:\Codex\work\probe_frozen_video.spec`（一次性小包，
  同样剔了 ICU），exe 里 `MEDIA_OK=True` 且收到视频帧。
  顺带比了一下新旧 exe：`Qt6Multimedia.dll` / `avcodec-61.dll` 在 v1.1.0 的包里
  就已经在了，所以这次**包只大了 14KB**，不用动 `桌宠.spec`。

## v1.1.0

### 新的设置界面：所有设置都在一个窗口里

- 右键菜单第一条 **「打开设置…」** → 弹出一个正经的设置窗口，左边竖排 **11 个分类**
  （余额和用量 / 天气和城市 / 音乐和歌词 / 形象和外观 / 动作和互动 / 文案和语录 /
  音效 / 应用提醒 / 性能和工具 / 通用 / 关于），右边是那一类的设置，
  不用再顺着右键菜单一层层往里点。
- **亮色 / 暗色 / 跟随系统**：默认跟着 Windows 的深色模式走 —— 你在系统里切成深色，
  窗口自己跟着变；也可以手动锁死在亮色或暗色（「通用 → 主题」）。
  卡片、文字、按钮、滑块、开关整套配色都有两份。
- 窗口是**无边框的圆角卡片**，拖标题栏能挪、按 Esc 关掉、关掉不影响桌宠。
  大小 / 透明度 / 音量这类直接拖滑块，边拖边生效。
- 设置里**只放设置**：看余额、看天气、看在放什么这些是桌宠自己的活儿，
  菜单和桌宠本体上都有，不在设置里再放一份。

### 右键菜单瘦身：从三十条缩到十条

- 右键桌宠弹出来的菜单从"三十条、套着 21 个子菜单"缩成**十条、一个子菜单都没有**：
  「查看余额 / 查看天气 / 看一眼在放什么」三样看一眼的，加「打开设置…」，
  加「显示/隐藏 · 锁定位置 · 鼠标穿透 · 救急恢复」四条随手要按的，最后「退出」。
  没有子菜单，也就没有"悬停半天展不开、点在子菜单上没反应"那套老毛病了。
- 模式 / 大小 / 形象 / 层级 / 文案 / 音效 / 百宝箱这些**设置类的入口全都挪进设置窗口**，
  菜单里不再重复。真要在菜单里找老位置，倒数第二条 **「经典菜单…」** 是完整版的老菜单
  （一条没少），用稳一版之后就删掉。

### 分类和选项的名字改得更直白

- 分类名：**余额和用量 / 天气和城市 / 音乐和歌词 / 形象和外观 / 动作和互动 /
  文案和语录 / 音效 / 应用提醒 / 性能和工具 / 通用 / 关于**。
  原来那条"联动与自动化"改成 **「应用提醒」**（打开某些应用时它冒泡说话），
  开机自启挪到「通用」。
- 选项名：「无级调节」→「精确调节」、「层级」→「窗口层级」、
  「记账」→「今日已用怎么算」、「歌词对时」→「歌词对不上？」、
  「显示峰谷时段」→「在气泡里标出现在贵不贵」、
  「Agent 名称 / 会话日志目录」→「去哪个文件夹读对话记录」。

### 图标、边框、老窗口：把"看着糙"的地方收一收

- **任务栏 / 标题栏的图标补上了**：以前整个程序一个窗口图标都没设，任务栏上显示的
  是 Windows 找不到图标时那只**空白方块**。现在换成「大肥鱼」自己的图标
  （用 `icon.ico`，16~256 各尺寸都齐，放多大都不糊）。
- **老窗口统一风格**：形象库、上传形象、扫描电脑应用、台词编辑器、音量 / 透明度那种
  小滑块窗口，原来都是 Qt 默认的灰样式，跟新界面放一起像两个软件；
  现在都套上同一份配色，亮色 / 暗色也跟着走。
- **按钮 hover 不再换细边框的颜色，改成换底色** —— 125% 缩放下，1px 的描边在 hover
  重绘那一下容易把顶边画丢，看着就像"上边缘被什么挡了一截"。
- 顺手把控件高度对齐到**整物理像素**（按钮 32 / 开关 24 / 左侧分类 36，
  乘 1.25 都正好是整数）：之前 30 那种高度会落在半像素上，一列控件累积下来，
  细线就会忽隐忽现。
- **修掉一个真 bug**：`_rebuild_sprites()` 原来只建**五档标准高度**的精灵，
  而 v1.0.18 起大小能无级调（比如 40% = 136px，不在五档里）。
  在非标准档位下换形象 / 换图，当前高度上就没图可画。现在当前高度也一起建。
- **音乐页的说明改成实话**：删掉「歌词对不上？」那块"网易云不报播放进度…"的解释，
  改成在「显示歌词内容」下面直接写明白 —— **网易云暂不支持歌词显示
  （只有 QQ音乐 能看到歌词）**，免得拿网易云的人一直等歌词。

### 大小：档位和精确调节联动，范围放宽到 20% ~ 150%

- **「大」这一档 = 100%**。五档现在是 **迷你 30% / 特小 40% / 小 60% / 中 80% /
  大 100%**（乘出来正好是整百分数，滑块拖过去能正好停在这几档上）。
  以前那个百分数是"原图高度的比例"，读着别扭（「大」居然是 90%），现在以「大」为基准。
- **两个控件联动**：点上面某一档 → 下面的滑块**当场跳到同一个百分比**；
  拖滑块停在哪一档上 → 那一档**跟着点亮**，停在档位之间就整排都不亮。
  两处是同一个大小，不会出现"上面点「大」、下面还写着 42%"这种对不上的情况。
- **范围放宽**：20% ~ 150%（原来 30% ~ 120%）。想更小更小、更大更大都行。

### 气泡顶上的应用名：网易云不再显示「未知应用」

- 播放器名字现在是**三级兜底**：认出来的中文名（QQ音乐 / 网易云音乐）→ 拿原始标识
  再认一次 → 把标识收拾成人能看的样子（例如 `SomeNewPlayer.exe` → `SomeNewPlayer`）。
  **只有连标识都拿不到时**才会是「未知应用」。
- 认的写法也多了：`cloudmusic.exe`、完整路径、商店版的
  `Netease.CloudMusic_xxxx!App`、`NeteaseMusic.exe`、`qq music` 都能认出来。
- 媒体会话那条路还会带上 `app_id`（原始标识）；如果某个播放器的标识实在认不出来，
  只要播放器窗口那边认得出是 QQ音乐 / 网易云，**名字就用窗口那边认出来的**，
  不会再空着。

### 侧边顺序、滚动掉帧、按钮汉化

- **侧边分类按常用程度重排**：余额和用量 → 形象和外观 → 动作和互动 → 天气和城市 →
  音乐和歌词 → 文案和语录 → 音效 → **应用联动** → 性能和工具 → 通用 → 关于。
  原来那条「应用提醒」也改回 **「应用联动」**。
- **快速滚动不再掉帧**：阴影改成**画一次缓存成图**（原来每滚一帧都要重画 6 圈抗锯齿
  圆角矩形），滚动区域的底色由"透明"改成实色、视口标成不透明，滚动时就不用连带
  把整个无边框 + 半透明窗口重画一遍。实测整窗重画 **1.67ms → 0.52ms**、
  滚一屏 **1.40ms → 0.72ms**。
- **Qt 自带按钮汉化**：装上 PySide6 自带的 `qtbase_zh_CN` 翻译，
  「触发文字」那种窗口底下的 **OK / Cancel 现在显示「确定 / 取消」**，
  顺带把 `QMessageBox` / `QInputDialog` / 文件选择框里那一套内置按钮一起带上了。
- **右键菜单再瘦两条**：「显示/隐藏」和「经典菜单…」都从菜单里撤掉，
  移进设置窗口的「通用」页 —— 那边新加了一行 **「右键菜单用经典版」** 的开关
  （打开后右键弹的还是旧版那份完整菜单，功能一条没少），
  旁边还有个「打开经典菜单…」按钮，不用开开关也能马上看一眼。
  现在右键桌宠只剩：查看余额 / 查看天气 / 看一眼在放什么 / 打开设置… /
  锁定位置 / 鼠标穿透 / 救急恢复 / 退出。

### 设置窗口：不再一直压在最上面，最小化也有了过渡动画

- **不再一直置顶**：设置窗口现在就是个**普通窗口** —— 你点别的程序它会正常被盖住，
  想找它点任务栏就行（打开的时候仍然会自己抬一下，不至于藏在后面）。
  以前是"桌宠置顶，设置窗口跟着置顶"，一直压在所有窗口上面，很碍事。
- **最小化改回真的最小化**：以前右上角那颗按钮跟「关闭」是同一套 `hide()` ——
  任务栏图标会**直接消失**，想再打开只能去右键桌宠。现在：
  - **最小化** → 走 Windows 那套"往任务栏收"的过渡动画（窗口一边缩小、一边淡出，
    190 毫秒），收完真的最小化到任务栏，**图标一直在，点一下就还原**；
  - **关闭** → 还是收起来（跟以前一样）。
- 动画收尾时会把窗口尺寸和不透明度悄悄还原回去，
  所以从任务栏点回来时不会变成小小一只、也不会还是透明的。
- **最小化动画改成"整窗等比缩放"**（之前只有往下滑）：窗口原来有最小尺寸
  820×560，几何动画被它钳住了 —— 缩不下去，看着就剩"往下划拉"。
  现在动画期间先松开最小尺寸，并且**先把整窗拍成一张图、动画期间只按当前大小画这张图**
  （真控件先藏起来），所以是**整只窗口连着字一起等比缩小**，跟 Windows 最小化一个观感。
  实测窗口尺寸一路 920×640 → 907×631 → 755×525 → 280×195 地缩下去。
- **点任务栏图标能最小化 / 唤出设置窗口了**：窗口加上了
  `WindowMinimizeButtonHint`（Windows 那边就是 WS_MINIMIZEBOX，实测窗口样式
  `0x960A0000` 里有这一位）。没有它，任务栏按钮点了没反应 —— 只有 OS 认到这一位，
  "点一下 = 最小化、再点一下 = 唤出"才是系统原生那套行为（动画也是 Windows 自己的）。
- **点任务栏图标最小化，现在也走同一套缩放动画**：实测**系统根本不给这种窗口跑动画**
  （无边框 + 半透明是分层窗口，`ShowWindow(SW_MINIMIZE)` 之后窗口矩形一采样就跳到
  -32000，一帧过渡都没有），所以那条路改成在原生消息层把 `WM_SYSCOMMAND / SC_MINIMIZE`
  拦下来（任务栏按钮、系统菜单里的「最小化」发的都是这一条），改放我们自己的动画；
  另外 `changeEvent` 里留了一层兜底。现在两条路看到的动画一模一样。
- **最小化动画的落点可以校准**：终点默认落在任务栏中间。想对准任务栏上自己那个图标，
  去「通用 → 最小化动画 → 校准…」点一下，**把鼠标停到自己那个任务栏图标上按空格**
  就记现在这一点（按 Enter 也行；不想按就 8 秒后自动记当时鼠标在哪，
  提示里会实时显示鼠标当前坐标）。再点一次那颗按钮也等于"就记这儿"。
  （为什么不做成自动：那个按钮是 Windows 11 用 XAML 画的，拿不到矩形 ——
  .NET UIA 查询要 15~50 秒而且查不到，MSAA 命中测试在任务栏上返回的是前台窗口的元素。）
- **从任务栏点回来也有动画了**（反向放一遍）：最小化时留下的那张整窗截图没有丢掉，
  当成"还原动画"的素材 —— 窗口从落点那一点点大、一边放大一边淡进来（OutCubic，190 毫秒）。
  拦的还是同一条原生消息：`WM_SYSCOMMAND / SC_RESTORE`（最小化着再点一次任务栏图标发的就是它）。
  从右键菜单点「打开设置…」唤回最小化的窗口，走的也是这套动画，不会"啪"一下蹦出来。
- **修掉还原动画的闪烁 / 卡顿**（两处原因）：
  ① 顺序问题 —— 原来先 `showNormal()` 再挪到落点，中间有**一帧停在"原来那么大"**上，
     那就是肉眼看到的闪烁。现在改成 `hide() → 清掉最小化状态 → 隐身 → 摆到落点那么小 → show()`，
     窗口第一次露面就是"小 + 全透明"，物理上不可能闪出大尺寸。
  ② 每帧都在把上千像素的截图做**平滑缩放**（一帧十几毫秒），动画自然一顿一顿。
     动画期间改用 `FastTransformation`。
- **动画可以打断了（跟 Windows 一样是"接管"，不是忽略）**：以前连点会出问题 ——
  动画跑着的时候新请求直接被丢掉，结果卡在错的状态（该最小化的还开着、该还原的还在最小化）。
  现在改成：新请求先把正在跑的动画停掉，**从"现在这个大小 / 现在这个透明度"接着往反方向走** ——
  最小化到一半点任务栏就平滑地长回去，还原到一半再点就顺着缩回去。
- **连点那"一帧大窗口"再收一手**（三处）：
  ① 只有手里没动画时才更新"正常大小"这个基准 —— 不然连点会把动画中间那个尺寸
     当成正常大小记下来，后面还原就停在一个不上不下的大小上；
  ② 最小化收尾摆大小之前**强制把不透明度压到 0**，那一帧必然是看不见的；
  ③ `_finish_restore()` 在"正往最小化走"时直接返回 —— 别把真控件放出来闪一帧。
- **同时间内帧数翻倍（动画更顺）**：Qt 自带的动画跑在全局统一计时器上（默认 16ms ≈60fps），
  所以 190ms 只有 13 帧。现在改成自己按 `ANIM_TICK_MS = 8`（≈120fps）插值 ——
  **时长不变**、帧数翻倍。进度是按真实流逝时间算的，掉一两拍也不会变慢或跳。
  实测同一段 190ms：最小化 13 → **23 帧**（平均 8.8ms/帧），还原 13 → **24 帧**
  （平均 8.4ms/帧，没有一帧超过 20ms）。
- **淡入淡出改成"画的时候控制"**：原来每帧 `setWindowOpacity()`，对分层窗口来说等于
  每帧让 DWM 重新合成一次整个窗口 —— 那是掉帧的大头。现在动画只更新 `_anim_fade`，
  由 `paintEvent` 里的 `painter.setOpacity()` 生效，每帧少一次系统级调用，
  也从原理上消掉了"不透明度恢复太早 → 闪一帧"那一类 bug。
- **还原起手那一下**（真机逐帧采样揪出来的）：从最小化里出来时，`show()` 会让 Qt
  把窗口摆回它记住的几何，于是动画**从满尺寸起步**、再淡入 —— 看着就是"闪一下大窗口
  再重新接上动画"。现在 `show()` 之后**再把尺寸钉一次**，并且动画的起点直接传"落点那个小矩形"，
  不信 `self.geometry()`。
- **尺寸和不透明度必须同一条曲线**：几何用 `OutCubic`（一开始涨得飞快）、透明度却是线性的，
  会出现"已经长到 2/3 大、才 27% 不透明"的**大号虚影** —— 眼睛看到的也是"闪了个大窗口"。
  现在两条曲线一致，实测 343px@28% → 605px@61% → 696px@72%，尺寸和透明度同步长。

- 实测（真机、真鼠标，`F:\Codex\work\probe_console.py`）：右键桌宠 → 菜单第一块里有
  「打开设置…」→ 真鼠标点它 → 设置窗口真的弹出来（不是一闪就没）→
  点左侧「音效」→ 右侧换页、标题跟着变 → 点「通用 → 亮色」→
  整套配色换成亮的、`config.json` 里 `ui_theme` 跟着写成 `light` →
  关掉窗口后桌宠还在、菜单照旧能开，探针打印 PASS。
  截图 `shot_console_real_menu.png`、`shot_console_real_open.png`、
  `shot_console_real_page.png`、`shot_console_real_theme.png`。
- 实测（离屏，`F:\Codex\work\test_quick_menu.py`，全部通过）：瘦身菜单里**一条子菜单都没有**、
  十条、该留的全在、该挪走的全不在；完整版老菜单那些入口**一条没少**；
  设置里没有「首页」这一页、没有「查一下 / 看一眼」这类按钮；11 个页面里
  **没有一个重复的设置项**。
- 实测（离屏，`F:\Codex\work\test_console_ui.py`，全部通过）：47 个图标逐个渲染，
  没有一个画出来是空白；11 个分类页都建得出来、切页标题跟着走；亮暗两套配色的
  样式表确实不同；系统亮暗判定拿得到值（这台机器上是暗色）。另外出了 11 页 ×
  亮暗 = 22 张图 `shot_console_light_*.png` / `shot_console_dark_*.png`。
- 实测（离屏，同一套）：形象库和上传形象窗口亮暗两套都套上了配色、窗口图标也不空，
  出图 4 张 `shot_dialog_light_skinlib.png` 等。
- 实测（离屏，`F:\Codex\work\test_size_link.py`，全部通过）：五档百分比 30/40/60/80/100、
  范围 20~150（倍率夹在 0.18~1.35）、点档位滑块跟过去、拖滑块停在档位上会点亮那一档、
  停在 47% 时整排都不亮、从别处改大小后切回这一页能重新对上。
- 实测（离屏，`F:\Codex\work\test_music_app_name.py`，全部通过）：八种写法全认对；
  三级兜底逐个验过（有名字 / 只有 exe / 只有 AUMID / 不认识的播放器 / 啥都没有）。
- 实测（离屏，`F:\Codex\work\probe_scroll_perf.py`）：整窗重画 1.67ms → 0.52ms，
  最长那页滚一屏 1.40ms → 0.72ms（离屏量的是"画得动"这条线，真机手感请主人再看一眼）。
- 实测：Qt 词条 `QPlatformTheme.OK` 翻出来是「确定」、`Cancel` 是「取消」；
  侧边顺序断言写在 `test_quick_menu.py` ⑤ 里。
- 实测（离屏，`F:\Codex\work\test_quick_menu.py` ④b）：菜单里已经找不到
  「经典菜单 / 显示隐藏」；「通用」页里有「右键菜单用经典版」开关，
  打开后 `_make_menu()` 真的换成完整版（一级里有「模式」），关掉又回瘦身版。
- 实测（离屏，`F:\Codex\work\test_console_minimize.py`，全部通过）：设置窗口不带
  `WindowStaysOnTopHint`；右上角两颗按钮分别是「最小化 / 关闭」；最小化动画跑完后
  **isMinimized=True 且 isVisible=True**（任务栏图标还在，这就是跟 `hide()` 的区别）、
  尺寸与不透明度都还原、连点第二下不会起第二个动画、`showNormal()` 能从任务栏回来；
  「关闭」则是 `isVisible=False`。
- 实测（真机，`F:\Codex\work\probe_minimize_anim.py`）：窗口样式 `0x960A0000` 里
  **有 WS_MINIMIZEBOX**；动画中途抓窗口自己的画面，`shot_min_t1/t2/t3.png` 三张
  能看出是**整窗等比缩小**（不是内容重排）；收尾后 isMinimized=True、几何还原。
- 实测（真机，`F:\Codex\work\probe_native_minimize.py`）：直接 `showMinimized()`
  时窗口矩形**一采样就跳到 (-32000, -32000)** —— 证明系统没跑动画，必须自己画。
- 实测（真机，`F:\Codex\work\probe_minimize_from_taskbar.py`）：给设置窗口
  `PostMessage(WM_SYSCOMMAND, SC_MINIMIZE)`（任务栏按钮走的就是这条）之后，
  50ms 时动画已经起来、画面是那张整窗截图、窗口在缩小（不是跳 -32000），
  收尾 isMinimized=True 且几何还原，打印 PASS。
- 实测（真机，`F:\Codex\work\probe_taskbar_button.py` / `probe_taskbar_msaa.py`）：
  想自动拿到"任务栏上自己那个按钮"的矩形，两条路都不通 ——
  .NET UIA 查询 15~50 秒且按窗口标题查不到（只列得出托盘那几个按钮）；
  MSAA `AccessibleObjectFromPoint` 在任务栏那条带上返回的是前台窗口的元素。
- 实测（真机，`F:\Codex\work\probe_calibrate.py`）：两轮都 PASS ——
  ① 点校准 → 鼠标移动到目标点 → 按空格 → 配置里就记下那个坐标；
  ② 点校准 → 不按键 → 8 秒后自动记下当时的坐标。
  （第一版这里有个低级 bug：`ui_console.py` 里根本没 import `QCursor`，
  倒计时最后一拍直接 NameError，被 Qt 吞掉 —— 表现就是"校准点了没反应"。）
- 实测（真机，`F:\Codex\work\probe_minimize_from_taskbar.py`）：① 发
  `WM_SYSCOMMAND/SC_MINIMIZE` → 走我们的缩放动画（50ms 时已在缩、不是跳 -32000）；
  ② 发 `SC_RESTORE` → 还原动画起来（+60ms 时透明度已经到 0.11 在淡入），
  收尾 isMinimized=False、shell 显示回来、素材清掉、几何还原（约 400ms 完成）。
  两个 PATH 都 PASS。
- 实测（真机，逐帧采样）：还原动画"起手拍"是 **宽 115（小）+ 透明度 0.00 + 已经可见** ——
  不会有大尺寸的可见帧；整段动画里没有"已经很亮、尺寸还很小"这种不合理帧；
  整窗重画 1.11 ms/帧。
- 实测（离屏，连点打断）：最小化跑到一半 → 立刻反向还原，收尾 `isMin=False`、真控件回来、
   素材清掉、几何还原；还原跑到一半 → 立刻再最小化，收尾 `isMin=True`；
   快速交替点 6 轮之后状态依然自洽（进程正常退出，没有崩）。
- 回归（离屏，`F:\Codex\work\test_*.py` 共 32 套）：**31 套通过**。
  `test_new_features.py`（扫描应用的数量断言）和 `test_still_front.py`
  （离屏环境下的拖拽识别）这两套失败的是**跟上一版完全相同的断言**，跟这次改动无关
  （`test_still_front.py` 这轮恰好在之前那次全绿，属于它自己的老毛病）。

### 维护者相关（发版 / 仓库，不写进 Release 说明）

- 新增 **`ui_console.py`**（本版本约 1200 行），是独立的界面子系统：
  - **主题**：`THEMES` 里一套亮、一套暗（背景 / 卡片 / 边框 / 文字三档 / 强调色 /
    开关关态 / 滚动条 / 阴影），`refresh_tokens()` 按当前模式重算；
    `system_scheme()` **先问 Qt**（`QGuiApplication.styleHints().colorScheme()`，
    还连了 `colorSchemeChanged` 信号），**拿不到就退回读注册表**
    `HKCU\...\Themes\Personalize\AppsUseLightTheme`（离屏 / 老系统上 Qt 会给 Unknown）。
  - **图标**：47 个单色 SVG 放在 `assets/icons/`，命名分三档 `nav.*`（12 个分类）/
    `ui.*`（窗口与通用按钮）/ `page.*`（各页专用）。`icon_pixmap()` 把 SVG 里的
    `currentColor` 换成当前主题色再 2 倍分辨率渲染，所以一套文件同时适配亮暗；
    换主题时 `clear_icon_cache()` 清缓存重画。
  - **控件**：`Switch`（轨道 + 圆钮，paintEvent 里现取颜色）、`Segmented`（一排互斥）、
    `NavItem`（左侧分类，选中铺主题色底片）、`DragBar`（无边框窗口拖拽）、
    `Page`（卡片 / 行 / 分隔线 / 滑块 / 下拉 / 按钮）。
  - `ConsoleWindow(pet, ctx)`：`ctx` 是 `桌宠.py` 递进来的常量与函数表
    （`SIZE_LEVELS` / `LYRIC_OFFSET_LEVELS` / `PEAK_TEXT_STYLES` / `LINE_FREQ_LEVELS` /
    `DOUBLE_CLICK_CHOICES` / `SKIN_PET` / `SKIN_WIDGET` / `LAYER_LABELS` /
    `currency_symbol` / `fetch_weather` / `human_mb` / `SPRITE_DIR` / `ASSET_DIR`），
    这样界面模块不用反过来 import 桌宠，不会循环依赖。
  - 页面里的动作统一走 `_run()` 包一层：某个动作抛异常只打一行日志，不会把窗口带崩。
- `桌宠.py` 的改动（一共五处）：
  1. `ASSET_DIR` 后面 `try: import ui_console`（**失败只打印一行、`ui_console = None`**，
     绝不拖垮桌宠）；import 前把 `APP_DIR` 插进 `sys.path`，直接 exec 加载本文件时也找得到。
  2. `PetWindow.__init__` 里加 `self._console = None`（懒加载，点「打开设置…」才建）。
  3. `_build_menu()` 第一块末尾加 `m.addAction("打开设置…", defer_dialog(self.open_console))`
     —— 和别的弹窗项一样包 `defer_dialog`，否则会被菜单的关闭动作一起带走。
  4. 新增 `open_console()`：已经建过就只抬到前面；`layer == "top"` 时给窗口也加置顶，
     不然会被桌宠盖住。
  5. `quit_app()` 里顺手 `self._console.close()`。
- `assets/icons/` 里的图标来自 **Lucide（ISC 授权，可商用可再分发）**，
  文件头保留了 `@license lucide-static v1.45.0 - ISC`，同目录放了 `LICENSE-lucide.txt`。
  下载脚本的动作是：去掉 `class="lucide…"`、把 `stroke-width="2"` 改成 `1.75`。
- **`桌宠.spec` 不用改**：`datas` 里本来就是 `('assets', 'assets')` 整个目录，
  `assets/icons` 会跟着一起打进去；`ui_console.py` 是普通 import，PyInstaller 自己会收。
- **发版时要一起改**：`ui_console.APP_VERSION`（关于页和侧边栏底部都读它）。
  版本号规则也改了：**补丁位每 20 一进位**（`x.y.19` 的下一版是 `x.(y+1).0`），
  写在 `AGENTS.md` 第 5 条；v1.0.19 已发，所以这一版是 **v1.1.0**。
- 菜单拆成两份（都在 `桌宠.py`，挨着放）：
  - `_build_quick_menu()` —— **新的默认菜单**，十条、**零子菜单**，右键桌宠和托盘都用它；
  - `_build_menu()` —— **完整版老菜单**，一条没改，只有「经典菜单…」那条会用到
    （`_open_classic_menu()` 用 `defer_dialog` 包一层再 `exec()`，直接弹会被菜单的
    关闭动作吃掉）；
  - `_make_menu(classic=False)` 按参数挑一份，`classic=False` 是默认。
  - **那 1000 行子菜单补丁先留着没删** —— 完整版老菜单还在用；等老菜单整个删掉，
    连同 `MenuClickBridge` / `SubmenuPlaceFix` / flyout 那一套一起清。
- 跟着改的用例：`test_skin_library.py` 里两处 `_make_menu()` → `_make_menu(classic=True)`
  （它验的是老菜单的内容）；新增 `F:\Codex\work\test_quick_menu.py` 盯瘦身菜单；
  `test_console_ui.py` 的页数 12 → 11、出图 24 → 22。
- 设置窗口去掉的东西：`_page_home` / `_refresh_live`（改成只刷余额那张卡的
  `_refresh_balance_card`）/ `_fetch_weather` / `_check_weather_page` / `_ask_weather` /
  `_on_weather` 和 `weather_ready` 信号 —— 连带 `ctx` 里的 `fetch_weather` 也撤了，
  界面模块现在不用 `threading`。
- 踩过的坑（下次别再踩）：
  ① **离屏平台（`QT_QPA_PLATFORM=offscreen`）这台机器上没有中文字体**，
     出图时中文全是方块 —— 要出给人看的截图得用真实平台（`windows`）+ `grab()`；
  ② 批量跑 `test_*.py` 必须带 `PYTHONIOENCODING=utf-8`：重定向到文件时 Python 默认按
     GBK 编码，测试里打印带圈数字（①②…）会 `UnicodeEncodeError` 直接中断，
     表现成"一大堆用例突然全挂"甚至卡住（台词窗口那套还会因此把真窗口留在桌上）；
  ③ 主题切换要**同时改样式表和全局调色板**：`Switch` / `NavItem` 是在 `paintEvent`
     里现取颜色的，只改 `setStyleSheet()` 它们还按旧色画。
  ④ 真机探针里要先让桌宠**站住**（`win.set_mode("still")` + 清掉 `cur_speed` /
     `target`）：它在"自由散步"时会走开，算好坐标、鼠标移过去那 300ms 里就点空了 ——
     表现成"探针时好时坏"，不是菜单的毛病（这次就是被这个坑了一次）。
  ⑤ **临时脚本千万别不重定向 `CONFIG_PATH` 就跑**：这次一个"小复现"脚本忘了改
     `pet.CONFIG_PATH`，`add_pet_skin()` 直接把三条临时图写进了主人的真 `config.json`
     （指向 `%TEMP%` 里那三张红绿蓝）。发现后手工清掉了那一条（`custom_skins` 和
     `skin_library.pet` 各一条），备份在 `%TEMP%\config.before-cleanup.json`。
     以后凡是会写配置的脚本，第一件事就是把 `CONFIG_PATH` / `USAGE_PATH` 指到临时目录。
  ⑥ 另：`setWindowIcon` 一个都没有时，Windows 任务栏会给一只**空白方块**；
     现在 `main()` 里统一设成 `icon.ico`。
  ⑦ **`Set-Content -NoNewline` 会把所有换行吃掉**：我用一句 PowerShell 批量替换
     `cfg.get("size", 0.7)` 时踩了这个 —— 6822 行的 `桌宠.py` 被写成了一行
     （而且因为首行是 `#` 注释，`py_compile` 居然还"通过"了，等于整个文件变成注释）。
     好在上一次提交就是干净状态，`git checkout -- 桌宠.py` 恢复后把本轮改动重打了一遍。
     **教训**：整文件改写只用 `apply_patch`；真要批量替换，就用 `-Raw` 读、`-NoNewline`
     只用在最后那一次写，或者干脆改成逐处 `apply_patch`。

## v1.0.19

### 新增「百宝箱 → 回收内存」：把后台程序占着的内存收一收

- **右键 → 百宝箱 → 回收内存**：点一下，它就把能碰到的后台程序"已经用不着的那部分内存"
  先还给系统，完事冒个泡报个数 —— 例如「收拾完啦：22 个程序腾出 2.6 GB，可用内存多出 251 MB」。
- **不动你正在用的程序**：默认跳过前台窗口（免得你正在玩的游戏 / 正在编辑的文档被清出去、
  再点的时候要重新加载一下）。想收得更狠一点，就把子菜单里的
  「回收时不动前台程序（防卡顿）」勾掉，前台的那个也一起收。
- **不弹管理员提示**：它收的就是浏览器、聊天软件、播放器这些你自己开的程序；
  系统进程那部分本来也碰不到，所以**全程不需要管理员、不会弹 UAC 框**，安装包还是原来那个"双击就能装"的。
- **子菜单里会留一行「上次：xx 个程序腾出 xx」**，方便你对照第二次还有没有可收的（同一台机器上
  第一把收得最多，紧接着再点一下往往就只剩零头）。
- **说清楚它是干嘛的**：收出来的内存并没有消失，只是从"进程占用"挪进了系统的备用缓存，
  程序再用到时是**软缺页**（直接从内存里取回来，不用读盘）—— 所以它是"把现在用不着的内存先让出来"，
  最适合**开大游戏 / 虚拟机之前先清一把**；平时内存本来就不紧张的话，收不收差别不大。

- 实测（真机、真鼠标，`F:\Codex\work\probe_mem_box.py`）：右键桌宠 → 悬停一级菜单里的「百宝箱」→
  子菜单弹出来（回收内存 / 回收时不动前台程序 / 灰字提示，截图 `shot_mem_box_sub_box.png`）→
  真鼠标点「回收内存」→ 气泡报「收拾完啦：22 个程序腾出 2.6 GB，可用内存多出 251 MB」
  （截图 `shot_mem_after.png`）→ 再开菜单，子菜单里多出「上次：22 个程序腾出 2.6 GB」
  （截图 `shot_mem_box_sub_last.png`）→ 点「回收时不动前台程序（防卡顿）」把它勾掉 →
  `config.json` 里 `mem_skip_foreground` 变 `false`、菜单里不打勾、回话「回收时前台程序也一起收，
  可能会顿一下」（截图 `shot_mem_toggle.png`）→ 再点回来又变 `true`；整个探针打印 `PASS`。
- 实测（离屏，`F:\Codex\work\test_mem_trim.py`，11 组全过）：菜单结构对得上 / 勾选状态跟着配置走、
  点一下能写回 config / 「上次：…」只在真回收过之后才出现 / `human_mb` 与 `system_memory` 的读数 /
  门槛调大就一个都不碰 / 按进程名跳过走 `skipped_named` / 系统进程打不开（denied > 0）/
  连点只提醒一句不起第二个任务 / 结果从后台队列回到主线程后气泡会报数。另外做了一组**对照实验**：
  先给自己灌 150 MB 常驻内存再回收 —— 跳过自己时工作集 222.2 → 222.3 MB **一动不动**，
  直接对自己调 `EmptyWorkingSet` 则 222.3 → 3.2 MB 立刻掉下来（证明"没动自己"是真没动，
  不是量不出来）。
- 实测（这台机器的回收能力，`F:\Codex\work\probe_mem_optimize.py`）：311 个进程里 **141 个能碰到**
  （都是同一个用户的），工作集 **12.2 GB → 0.29 GB**，整轮 **150 ~ 850 ms**；另外 170 个
  （系统 / 提权进程）连句柄都打不开。真要把内存还给空闲列表得"清空备用列表 / 清系统文件缓存"，
  这两件**需要管理员**（实测返回 `NTSTATUS = 0xC0000061`、`GetLastError = 5` 拒绝访问），
  这一版不做，留在 TODO 里。
- 实测（菜单不会挤爆屏幕）：一级菜单总高 613px，屏幕可用高度 1152px，还富余 539px。
- 回归（离屏，`F:\Codex\work\test_*.py` 全套 29 套）：**27 套通过**。`test_new_features.py`
  （扫描应用的数量断言）和 `test_still_front.py`（离屏环境下的拖拽识别）这两套在**改动前的干净版本上
  失败的是同样两条断言**（用 `git stash` 回退后复跑确认过），跟这次改动无关。

### 维护者相关（发版 / 仓库，不写进 Release 说明）

- `桌宠.py` 新增（放在 `list_process_names()` 和 `process_exe_by_pid()` 之间）：
  - 模块级 `human_mb(nbytes)` / `system_memory()` / `trim_working_sets(skip_pids, skip_exes, min_bytes)`；
  - 常量 `MEM_TRIM_MIN_MB = 20`（比这还小的进程不动）和 `MEM_TRIM_SKIP_EXE`
    （system / csrss / wininit / winlogon / services / lsass / dwm / fontdrvhost / audiodg 这些不碰）。
- `PetWindow` 新增 `mem_trim_now()` / `_mem_trim_done(got)` / `set_mem_skip_foreground(on)`：
  后台线程只跑 ctypes，结果塞 `self._mem_queue`，主线程在 `tick()` 里取（跟天气 / 应用扫描一个套路）；
  `self._mem_last = (程序数, 腾出字节)` 只在内存里，不落盘；`_mem_trimming` 防连点。
- `config.json` 新增 `mem_skip_foreground`（默认 `true`）。
- 菜单在 `_build_menu()`：「流畅度」和「音效」之间插了「百宝箱」二级菜单；
  子菜单条目 = 回收内存 / 回收时不动前台程序（可勾）/ 分隔线 / （回收过才有）上次：… / 灰字提示。
- 踩过的坑（下次别再踩）：
  ① `EmptyWorkingSet` 在 **psapi.dll** 里，kernel32 没有这个导出（`kernel32.EmptyWorkingSet`
     直接 AttributeError）；
  ② `OpenProcess` / `QueryFullProcessImageNameW` 不声明 `restype` 的话，64 位下返回的句柄会被
     按 32 位截断；
  ③ 收别的进程要 `PROCESS_QUERY_INFORMATION(0x400) | PROCESS_SET_QUOTA(0x100)` ——
     探针第一版写成 `0x0410`（少了 SET_QUOTA、多了 VM_READ），结果"一个都收不动"，
     差点得出"不提权就做不了全局回收"的错误结论；
  ④ `ctypes.WinDLL(...)` 不加 `use_last_error=True` 时，`ctypes.get_last_error()` 拿到的是 CRT 的
     errno 而不是 `GetLastError()`，`AdjustTokenPrivileges` 那种"其实没赋上权"的失败会被误判成成功。

## v1.0.18

### 修改一下大小：原来那五档照旧，另外加了个「无级调节」

- **右键 → 大小 → 无级调节…（现在 xx%）**：弹一个小窗口拖滑块，**30% ~ 120% 想调多大就多大**
  （100% 就是原图那么大，「大」档是 90%）。**拖着的时候它当场变大变小**，松手就记住，
  不用非挑中某一档。
- **原来那五档一个都没动**：**迷你 / 特小 / 小 / 中 / 大** 还在一级菜单的「大小」里，点一下就到；
  滑块正好停在某一档上时，菜单里那一档照旧打勾。
- **变大小改成"原地缩放"**：脚底和中心不动。以前换档位是窗口左上角不动，从小档换大档会整只往下出溜；
  现在原地长个子/缩回去，拖滑块的时候不会满屏乱窜（实在贴出屏幕了还是会自动拉回来）。
- 大小跟着存进配置，下次打开还是这个大小（手动改过 `config.json`、值超出范围也只当没看见，自动钳回 30% ~ 120%）。

### 新增「锁定位置」：锁上就拖不动，专门防误触

- **右键菜单下半部分多了一条「锁定位置（拖不动·防误触）」**，勾上以后桌宠就钉在原地：
  **点它 / 双击它 / 右键菜单照旧**（按压 Q 弹、回嘴、看余额、换姿势都不受影响），**就是拖不动了** ——
  想点一下结果把桌宠拖到屏幕另一头这种事不会再有。再点一下（去掉勾）就解锁，又能拖着走。
- 锁定状态记在配置里，重启后还是锁着的。
- 注意它只管"拖不动"：**想让它连散步都停下来，用「模式 → 原地待着」**（两件事互不干扰）；
  「回到屏幕内」「救急恢复」这类救命入口也照常能把它挪回屏幕上（不然锁在屏幕外就真叫不回来了）。
- 实测（真机、真鼠标，`F:\Codex\work\probe_size_lock.py`）：右键 → 大小（子菜单 = 迷你 / 特小 / 小 / 中 / 大 /
  无级调节…（现在 70%），截图 `shot_size_submenu.png`）→ 点「小」→ `config.json` 的 `size` 变 0.55 →
  再点开「无级调节…」弹窗（截图 `shot_size_dialog.png`）→ **真鼠标按住滑块往右拖**：
  拖到一半（还没松手）就是 97%（桌宠高度 329），松手停在 114%（高度 387、窗口 422x517，
  截图 `shot_size_dragged.png`）；随后勾「锁定位置」（截图 `shot_lock_menu_only.png`）→
  **从精灵身体正中真鼠标拖 (+64,+40)**：位置 `(400,300)` → `(400,300)`，一动没动（`dragging=False`，
  内部 `_drag_blocked=True`），截图 `shot_lock_drag.png`；锁着的时候真按一下仍有 Q 弹（`press_t=0.76`）、
  真点一下仍有反应（蹦跳 1.0）；再解锁 → 同样拖 (+64,+40)，位置 `(400,300)` → `(456,336)` 跟着走了，
  截图 `shot_unlock_drag.png`。整个探针最后打印 `PASS`。
- 实测（离屏，`F:\Codex\work\test_size_lock.py`，12 组）：菜单里五档 + 无级调节那条都在；
  `set_size(0.63)` 能现补出 214px 的精灵（四个视图齐全，画得出来）；原地缩放（脚底 384→384、中心 180→180）；
  5.0 → 钳成 1.2、0.05 → 钳成 0.3；42% 时菜单里只有「特小」打勾；从 31% 拖到 121% 之后
  `self.sprites` 里只有五档 + 当前高度（每个高度 4 张图，没有越堆越多）；锁定时按住拖：位置不变、
  不算点击；锁定时单击/快速双击照常触发；解锁后能拖动；老配置（size=0.63 + locked=true）
  重开也有图、也还是锁着 —— 全过。
- 实测（滑动的手感，`F:\Codex\work\probe_size_diag.py`）：滑块每走 1%（30%→120% 来回各 91 步）
  平均 **0.52 ~ 0.55 ms/步**（第一步含读盘，之后都走原图缓存）——拖起来不卡。
- 实测（真机、主人这只桌宠）：重启后跑上新代码，窗口还在原地、大小照旧
  （42%，逻辑坐标 1784,721，物理矩形 2230,901-2440,1241），`config.json` 里已经自动多出 `locked: false`。
- 实测（自带素材有没有被"裁透明边"改样，`F:\Codex\work\probe_sprite_crop.py`）：
  正面 255x340 / 侧面 222x340 / 背面 253x340 裁完还是原尺寸（本来就没有透明边），
  所以五档的显示跟以前一模一样；挂件小鲸鱼 610x610 → 裁完 565x600（这条老代码就是这么裁的，没动）。
- 回归（真机、真鼠标，`F:\Codex\work\probe_drag3.py`）：先确认"合成鼠标"能拖动桌宠（4px 一小步，
  位移 64px ✓），锁定的对照实验才作数。
- 回归（离屏，`F:\Codex\work\test_*.py` 全套 28 套用例）：除下面两套"本来就该跟着改"的旧用例外全过 ——
  ① `test_size_levels.py` ⑦ 写死了"大小子菜单就是那五条"，现在多一条「无级调节…」→ 用例改成
  "前五条还是五档 + 第 6 条是无级调节"；② `test_lines_dialog.py` ④b 写死"类别一共 10 类"，
  而 v1.0.17 加了「快速双击说的话」之后其实是 11 类（上一版漏改的旧用例）→ 改成跟 `LINE_GROUPS` 对齐。
  两套改完重跑全过；余额 / 双击 / 换形象 / 语录 / 菜单流畅度 / 音量透明度等其余用例都没受影响。

### 维护者相关（发版 / 仓库，不写进 Release 说明）

- `config.json` 新增 `locked`（默认 `false`）；`size` 从"只存五档倍率"变成"任意倍率"（0.30 ~ 1.20），
  老配置里的五档值照旧能用，越界/乱值由 `_clamp_size()` 兜底。
- 常量在 `桌宠.py` 顶部：`SIZE_MIN = 0.30` / `SIZE_MAX = 1.20` / `SIZE_DEFAULT = 0.7`。
- 新增方法：`PetWindow._clamp_size()` / `_ensure_sprites(h)` / `_prune_sprites(h)` / `_view_base(path)` /
  `size_dialog()` / `set_locked(on)`；`set_size()` 重写（钳位 → 换高度 → 补/清精灵 → 原地缩放 → 兜回屏幕）。
- 精灵：`_load_view_pixmap()` 改成"原图裁透明边 → 按高度缩放"，原图走 `_view_base()` 的缓存
  （按"路径 + mtime"缓存，换图自动失效，最多留 8 份）；`self.sprites` 里只保留五档 + 当前高度，
  不然滑块拖一路会攒下几十个高度的位图。自带素材仍然优先用现成的 `正面_187.png` 这类文件。
- 鼠标：`mouseMoveEvent()` 在 `self.locked` 时越过 6px 门槛也不启动拖拽，只置 `_drag_blocked`；
  `mouseReleaseEvent()` 里这一下不算点击，并把 `_last_click_ms` 清掉（免得带出假双击）。
  `mousePressEvent()` 每次按下会重置 `_drag_blocked`。
- 菜单在 `_build_menu()`：大小子菜单尾部加了 `无级调节…（现在 x%）`（`defer_dialog` 弹窗，
  跟透明度/音量一个路子 —— 菜单内嵌滑块在这台机器上拖不动）；一级菜单「回到屏幕内」下面加了
  可勾选的「锁定位置（拖不动·防误触）」。托盘右键是同一份菜单，自动跟着有。
- 真鼠标探针的坑（记下来，下次别再踩）：
  ① 桌宠窗口是 `WS_EX_LAYERED`，**光标落在精灵身体外的透明像素上时，鼠标消息会被系统送给下面的窗口** ——
     合成拖拽必须 4px 一小步、起点取"精灵身体正中"（`win.y() + BUBBLE_H + MARGIN + cur_h/2`），
     窗口正中最上面那块是气泡区、是透明的；
  ② `SendInput` 之后必须 `app.processEvents()`，否则原生消息全堆在队列里，桌宠什么都收不到
     （没有 `app.exec()` 的脚本尤其容易踩，`probe_drag2.py` 就是这么得出"拖不动"的假结论的）；
  ③ `e.buttons()` 在 PySide6 里是枚举，`int()` 会抛 TypeError —— 别在事件过滤器里用 `int(e.buttons())`。

## v1.0.17

### 「今日已用」不再把余额变动算成你今天花掉的钱

主人 2026-09-13 反馈：「现在修一个余额显示bug，今日显示余额异常，我今天最多用了15元，但显示了200多」。

- 原因：这个数一直是**"今天余额掉了多少"**推出来的。平台侧让余额掉一块（**赠送额度到期/被收回**、
  退款、接口偶尔抽一下值）也会被算成"你今天用掉的"，于是出现"我只用了十几块，它显示两百多"。
- 主人 2026-09-13 06:14 用平台用量页坐实了：**今天真实消费 ¥14.83**（1,438 次请求 / 344,272,679 tokens）；
  余额从 35.94 掉到 21.11。账本里那个 `292.24` 其实等于**赠送 271.13 + 充值 21.11** ——
  **赠送额度整块到期/被收回**，平台不算用量，桌宠旧版却把它记成了 269.95。9-12 那笔 304.27 同理。
- 现在账本先判断这次下降像不像用量，不像就不算：
  - **一次刷新掉得太多**（60 秒掉 20 元以上；桌宠关了几小时再开按每小时 5 元放宽）→ 记成「余额变动」；
  - **赠送额度整块消失**（接口里 `赠送余额` 从有到 0，掉的钱正好是那一笔）→ 同样记成「余额变动」。
- 真发生时桌宠会说明一句：`余额少了 ¥269.95，看着不像用掉的（赠送额度到期/被收回），这次没算进今日已用`；
  一开始还在右键菜单「查看余额」下面加了一行灰色的 `今天另有 ¥x 余额变动，没算进今日已用`，
  **主人 2026-09-13 看过之后说「把这个灰色小字删了，奇奇怪怪的」→ 这一行已经去掉了**
  （钱去哪了照样看得到：真发生变动时气泡会解释一句）。
- **换 Key（换账号）/ 换币种不再产生假账**：账本记 Key 指纹，账号一换只重置基准，
  不会把两个账号的差额记成用量。
- 账本落盘改成"先写临时文件再替换"：中途出错也不会留下半截 JSON（半截会被当空账本）。
- 实测（离屏，`F:\Codex\work\test_balance_logic.py`）：正常下降累计、充值不抵扣、币种/Key 变化只重置基准、
  赠额到期（292.24 → 22.29）判为余额变动、一次掉 270 判为余额变动且之后小额照常累计、跨天归档 —— 全过。
- 实测（界面，`F:\Codex\work\probe_balance_guard.py`）：模拟"赠额整块消失"→ 气泡里今日已用仍是 0.00、
  账本写 `todayAdjust 269.95`、冒的那句话带"没算进今日已用"；随后正常掉 0.49 元照常累计。
- 实测（真机、真鼠标，`F:\Codex\work\probe_menu_balance_info.py`）：加那行灰色说明时，右键菜单第一块出现
  `今天另有 ¥270.67 余额变动，没算进今日已用`（灰色不可点），截图 `shot_menu_balance_info.png`。
  主人让删掉之后又用真鼠标验了一遍（`F:\Codex\work\probe_menu_no_adjust_line.py`）：账本里
  `todayAdjust` 是 281.99（正是会出现那一行的条件）时，一级菜单里已经**没有**任何带「余额变动」的条目，
  第一块就剩 查看余额 / 查看天气 / 看一眼在放什么 / 快速双击… —— 截屏 `shot_menu_no_adjust_line.png`。
- 实测（真机、主人这只桌宠）：今天的账本从 `todayUsage 270.67` 改判成余额变动（旧账备份
  `F:\Codex\work\usage-bak-20260913-060534.json`），余额 21.57 → 21.19 这段真实消耗照常记（0.38 元），
  与余额下降一致；跑完 25 套自测之后账本只多了 0.04 元（真实消耗），不再被假数据污染。
- 实测（按平台数校准后）：今天显示 `今日已用 ¥14.87 / 余额 ¥20.90`（平台 14.83 + 之后真实用的 0.04），
  截图 `shot_today_after_fix.png`；校准脚本 `F:\Codex\work\calibrate_usage_today.py`
  （只改 `todayUsage`，不碰 `todayAdjust`，并在账本 `calibrateLog` 里留一笔）。

### 余额菜单里新增「校准今日已用…（按平台用量页）」

- **右键 → 余额 → 校准今日已用…**：小窗口里填平台「用量信息」页今天显示的消费金额（元），
  窗口里直接写着"桌宠现在记的是 ¥x"，填完回车就把「今日已用」对齐成平台那个数。
- 什么时候用：赠送额度到期、桌宠关着漏采了一段 —— 这种时候账本自己算不出当天真实用量，
  平台那个数才是准的。只改「今日已用」，**不动余额基准、也不动「余额变动」记录**，
  之后余额再降还是会照常累计；每次校准都在账本 `calibrateLog` 里留一笔（谁改成多少、为什么）。
- 填错字会提醒（"这个数我没看懂，填个数字就行（例如 14.83）"），负数不让填。
- 实测（真机、真鼠标，`F:\Codex\work\probe_calibrate_menu.py`）：右键 → 余额（子菜单里就多了这条）→
  点「校准今日已用…」→ 窗口弹出并带出当前值 → Ctrl+A 改成 12.34 → 回车 →
  账本 `todayUsage` 变 12.34、`calibrateLog` 多一条 `{from: 15.02, to: 12.34}`、气泡跟着变。
  截图 `shot_calibrate_dialog.png` / `shot_calibrate_done.png`。
- 实测（离屏，`F:\Codex\work\test_balance_logic.py` 新增一组）：校准成 14.83 → 后续下降接着累计成 15.33；
  校准不动 `lastBalance`；也能清成 0。

### 快速双击弹什么，现在自己挑（没配 Key 不会再弹余额）

主人 2026-09-13 的原话：

> 「更新一个功能，如果用户未设置apikey则快速双击不会触发余额窗口，在一级菜单加一个选项可以选择快速双击触发的窗口
> （天气，余额（未配置api则不可以选择），看一眼在放什么，或者自定义语录）」

- **右键 → 一级菜单最上面多了一条「快速双击（现在：…）」**，点开就四项，挑一个：
  **看一眼余额（5 秒）** / **看一眼天气** / **看一眼在放什么** / **说一句我写的台词**。
  点完立刻生效，这一条的标题会跟着显示现在选的是哪个。
- **没配 API Key 时快速双击不会弹余额窗口**：菜单里的「看一眼余额」是灰的（后面直接写着「要先配 Key」），
  点了也不给存；这种状态下双击还是原来的"换姿势"，并且只在本次启动里提醒一次怎么配 Key。
- 选 **「说一句我写的台词」**：双击冒的是**你自己写的那几句**（默认给了 4 句）。
  改写有两个入口：子菜单里的 **「改写这几句…」**，或者 **台词内容…** 窗口里那一类
  **「快速双击说的话（自己写）」** —— 一行一句，跟其它台词一样留空就恢复默认。
- **双击之后那几秒，它不再自己冒闲话**：以前刚双击看完余额，它可能顺手来一句"碎碎念"把
  余额盖掉（点它 / 拖它照旧有反应，只是"自言自语"先让位）—— 现在这几秒先憋着，
  选好的那一眼能安安稳稳看完。
- 顺手改掉一句会让人误会的话：闲着时冒的那句 **「双击我一下，余额马上给你看」**，
  在双击被改成别的（或者没配 Key）时就不再冒了。
- 原来那种行为没动：默认还是「看一眼余额（5 秒）」，配了 Key 的话双击照旧；
  放歌时双击看到的余额也照旧盖在歌词上面 5 秒。
- 实测（真机、真鼠标，`F:\Codex\work\probe_double_click_menu.py`）：右键 →「快速双击」—— 没配 Key 时
  「看一眼余额（5 秒）（要先配 Key）」灰掉不可点、当前项照样打着勾（`shot_double_click_menu_nokey.png`）；
  点「说一句我写的台词」→ `config.json` 里 `double_click = lines` → 真双击桌宠，气泡冒的就是那句台词
  （`shot_double_click_lines.png`）；再配上 Key → 余额项变回可点（`shot_double_click_menu_key.png`）→
  点「看一眼余额（5 秒）」→ 真双击，余额顶上来显示 5 秒（`shot_double_click_balance.png`）。
- 实测（真机、真鼠标，`F:\Codex\work\probe_dc_edit_lines.py`）：子菜单里点 **「改写这几句…」** →
  台词窗口弹出，**「改哪一类」已经停在「快速双击说的话（自己写）」**、框里就是那 4 句 ——
  截图 `shot_dc_edit_lines.png`。
- 实测（离屏，`F:\Codex\work\test_double_click_choice.py`，15 组）：四种选择各自走对窗口；
  没配 Key 双击不弹余额（`bal_until` / `_bal_peek_until` 都保持 0）；菜单里余额项灰掉且没勾上；
  没配 Key 时选余额不给存（配置不变 + 说明原因）；配置被手改坏（不存在的值）退回默认；
  双击不看余额后那句"双击我给你看余额"从闲话池里消失（改回看余额又回来）；
  双击后那几秒闲话不插嘴 —— 全过。
- 回归（离屏，`F:\Codex\work\test_double_click_peek.py`，原来那 6 组）：配了 Key 双击看 5 秒余额、
  放歌时不被歌词盖住、还没取到余额时先挂待办、余额回来立刻冒 —— 照旧全过。

### 维护者相关（发版 / 仓库，不写进 Release 说明）

- `config.json` 新增 `double_click` 键（`balance` / `weather` / `music` / `lines`，默认 `balance`），
  老配置缺这个键会自动补上；取值不认识时 `_double_click_choice()` 退回默认。
- 常量在 `桌宠.py` 顶部：`DOUBLE_CLICK_CHOICES`（四项，含菜单文字）、`DOUBLE_CLICK_DEFAULT`、
  `DOUBLE_CLICK_LINES`（"说一句我写的台词"的默认台词，已加进 `LINE_GROUPS`）、
  `DOUBLE_CLICK_HOLD_SEC`（双击后"闲话让位"几秒）、`DOUBLE_CLICK_HINT_LINE`（那句会被过滤的闲话）。
- 方法：`PetWindow._on_double_click()` 按选择分发 → `_double_click_balance()`（原来那段原样搬过来）、
  `_get_weather()`、`check_music_now()`、`say(lines_for("DOUBLE_CLICK_LINES"))`；
  菜单/说明文字用 `_double_click_menu_title()`、`_double_click_effect_label()`；
  设置走 `set_double_click()`（余额项没配 Key 时直接拒绝并说明）。
- `edit_lines_dialog()` 多了 `initial_key=None` 参数（菜单里点「改写这几句…」直接停在那类上）。
- 写探针时踩到一次坐标坑，记一下：`mapToGlobal` / `win.x()` 是 **Qt 逻辑像素**（2048×1152），
  `SendInput` 要 **物理像素**（2560×1440，DPR 1.25）—— 换算漏了，右键菜单能开、但鼠标永远落在别处，
  子菜单看着"点了没反应"（`F:\Codex\work\probe_dc_menu_geom.py` 查出来的）。以后写真鼠标探针先乘 DPR。

- 账本 `usage.json` 新增字段：`lastGranted` / `lastToppedUp` / `lastKeyId`（Key 指纹）/ `lastSampleAt` /
  `todayAdjust` / `lastAdjustWhy` / `lastAdjustAt` / `adjustLog`（最近 20 条变动）。
  老账本没有这些字段也能读：`lastAdjustWhy` 会从 `adjustLog` 兜底补。
- 判据都在 `balance_drop_reason()`；阈值是 `USAGE_JUMP_LIMIT = 20.0` 与 `USAGE_JUMP_RATE = 5.0`（元/小时）。
  采样间隔取 `lastSampleAt`，没有就用文件修改时间兜底。
- `record_balance_usage()` 返回值从"一个数字"改成
  `{"today", "adjust", "amount", "reason", "adjust_why", "adjust_at"}`，调用点在 `PetWindow._apply_balance()`。
- 这次只修了"今天"这一笔；`history` 里 9-11（26.63）/ 9-12（304.27）两个老数字没动（同一个成因，参考价值）。
- 离屏自测会把假余额（9.87）喂给 `_apply_balance`，以前直接写进主人的 `usage.json`（这次真踩到：
  多出 11.42 元假账，已按 `usage-bak-20260913-061004.json` 备份后改判）。`test_double_click_peek.py`
  已改成把 `pet.USAGE_PATH` 指向临时目录 —— 以后哪个用例要喂假余额也得这么干。
- 复核用的旁证脚本：`F:\Codex\work\probe_today_tokens.py`、`probe_key_fingerprints.py`
  （全机只有一把 DeepSeek Key，`cf2a9bed`，所以这次不是换账号引起的）。
- 也试过有没有官方用量接口：`/user/usage`、`/user/billing/usage`、`/user/bill`、`/usage` 全 404，
  只有 `/user/balance` 能用 → 「今日已用」只能继续靠余额差值算，所以判定"这笔下降不是用量"的规则才是关键。
- 账本写回改成"以老文件为基础再覆盖自己管的字段"，手工写进去的 `calibrateLog` 之类不会被抹掉。

## v1.0.16

### 换形象：有了「我的形象库」，而且是两本分开的

- 右键 → **形象 → 我的形象库…**：上传过的形象都在这儿，**带名字、能预览**，
  以后换形象直接从库里挑，不用再去硬盘里翻文件。
- **三维形象（大肥鱼）= 正面 / 侧面 / 背面三张一套** —— **三张都上传齐了才算一个形象**
  （少一张「保存」是灰的），换上时三张**整套**换；**挂件形象（小鲸鱼）= 一张图**。
  两本库各存各的，互不影响。
- 窗口里：左边列表（缩略图 + 名字 + 「正在用」/「⚠ xx 那张找不到了」），右边预览 + 三张小图 + 详情；
  按钮是 **「用这个形象」「换图…」「改名…」「从库里删掉」「上传新形象…」**，
  右上还有个小按钮 **「这套恢复默认」** —— 只把**这一本**退回自带的，另一本和库里条目都不动。
- **三维形象默认显示「正面」那张**（列表图标和大预览都用它），下面并排三张小图，缺哪张一眼看出来。
- 上传窗口：三维那本三行（正面 / 侧面 / 背面）、挂件那本一行，每行「选图片…」+ 缩略图 + 文件名和所在文件夹；
  名字填在上面（挑完图自动填文件名，可以改）；底下实时写着「还差：背面（三张齐了才能保存）」。
  **挑了一两张就取消也不白挑**：下次点上传自动带上，接着把剩的补上。
- 老配置自动升级：以前"一个视图一张图"的写法，启动时分装进两本库（三张都有的收成一个三维形象）。

### 形象菜单精简成四条

- `形象` 里就四条：**大肥鱼（三视图） / 小鲸鱼挂件（单张） / 我的形象库… / 全部恢复默认形象** ——
  前两条点一下就切皮肤（打勾的是现在用的）。
- 原来那两套**三级菜单整个去掉**（用这个形象 / 上传新图片 / 恢复默认三视图 / 恢复默认小鲸鱼）：
  上传、换图、单套恢复都在形象库窗口里，菜单上留着是重复。

### 原图别删、别挪（找不到会明确告诉你）

- 上传窗口里橙色两行提醒：**「别把源文件删掉」**、**「别改它的路径 —— 改名、挪文件夹、
  换盘都会让桌宠找不到」**；上传成功后还会冒一句「…（别删原图哦）」。（桌宠记的是原图路径，不复制文件。）
- 真找不到时不再是"默默变回自带形象"：启动冒泡提醒一次、库里那条标 `⚠ 侧面那张找不到了`、
  「用这个形象」灰掉而「换图…」照旧能点；硬要换上会弹说明窗口，写清缺哪张、原来在哪儿。
- 缺的那一张退回自带，**另外两张照旧用你自己的图**。
- 实测（离屏，`F:\Codex\work\test_skin_library.py`，12 组断言全过）：两张图时保存是灰的、
  三张齐了才收录（按精灵中心像素颜色确认三张画出来的真是那三张）、两本库互不影响、门面 = 正面、
  换图/改名不动编号、缺一张只退一张、「这套恢复默认」只退这一本、老配置迁移两种情形、草稿续传。
  截图 `shot_skinlib_pet_offscreen.png` / `shot_skinlib_widget_offscreen.png`。
- 实测（真机、真鼠标，`F:\Codex\work\probe_skin_library.py`）：右键 → 形象（就四条、无三级）→
  点「我的形象库…」窗口真的打开；真鼠标点两条外观，皮肤跟着切；上传窗口没齐时保存是灰的。
  截图 `shot_skinlib_menu.png` / `shot_skinlib_dialog.png` / `shot_skinlib_widget.png` /
  `shot_skinlib_edit.png` / `shot_skinlib_missing_warn.png`。
- 回归：`F:\Codex\work` 下 25 套自测（形象 / 音效、菜单性能、原地面朝、歌词、天气、余额…）全过。

### 维护者相关（发版 / 仓库，不写进 Release 说明）

- 微信版 zip **只留本地** `outputs\`、不再上传；里面就两样：**Windows 安装版 exe + 使用说明.txt**
  （教程按"先装再用"重写）。
- GitHub Release **只放 `dafeiyu-pet-setup.exe`（安装版）**：v1.0.13~v1.0.16 已经删掉绿色版附件，
  v1.0.12 及更早本来就只有绿色版 → 原样不动；`publish-pet.ps1` 以后也只传安装版
  （真没编出安装包才兜底发绿色版并警告）。
- README / README.en.md 顶部"下载最新版"的按钮从 `dafeiyu-pet.exe` 换成 `dafeiyu-pet-setup.exe`
  （不然点进去 404），文案也按安装版改写了。
- `publish-pet.ps1` 新增 `-ZipOnly -Version X.Y.Z`（没发版也能补做本地 zip）。

## v1.0.15

- **「原地待着」模式：三维外观一直维持正面**（主人要求）
  - 以前这个模式会"偶尔转头看鼠标"：鼠标在它左边/右边就换成**侧面**图（靠到 30px 以内才回正面）。
    换成自定义形象之后更显眼 —— 正面是你的图，一转头就变成另一张。
  - 现在**选了「原地待着」就固定正面**：`_still_face_front()` 每一帧对账，`set_mode("still")`
    切过来的当下就立刻站正（不用等下一帧）；原来那个"转头看鼠标"的老逻辑（只分左右、不会背过身）
    换成了 `_face_cursor()`（跟「跟随鼠标」同一套朝向算法），默认不调用。
  - **拖动时的朝向逻辑一点没动**：按住左键拖到左边照样给侧面 + 左翻，拖动期间主时钟继续跑也不会被
    拽回正面，松手回正面（还是原来那条 `mouseReleaseEvent`）。
  - 小鲸鱼挂件形象同理：静止时不左右翻面（它只有一张图，"正面"就是不镜像）。
  - 实测（离屏，`F:\Codex\work\test_still_front.py`，10 项断言全过）：
    把状态摆成侧面 / 背面 → 下一帧全被拉回正面；**渲染取图记录里只有「正面」**（换成背面渲染画面确实不同
    → 证明测到的是真图）；拖动往左 → 侧面，拖动期间主时钟继续跑仍是侧面，松手回正面；
    自由散步往左走仍然给侧面（别的模式没被改坏）。
  - 实测（真机，`F:\Codex\work\probe_still_front.py`：真窗口 + 真鼠标甩到左/右/上三处各停 2.5 秒）：
    **7.5 秒里朝向变更 0 次**、`_face_cursor` 调用 0 次，全程正面；截图
    `F:\Codex\work\shot_still_对比.png`。（拖动那一段因为主人当时也在用鼠标，我发的模拟按键被真人的松手
    打断，所以拖动这条以离屏那条为准。）
- **「原地待着」时也跟着鼠标转：菜单里多了个开关**（主人要求，规则也是主人定的）
  - 菜单位置：**模式 → 原地待着时也跟着鼠标转**（默认**关**）。配置存在 `config.json → still_face_cursor`。
  - 关着（默认）：原地待着就是一直正面，和上面那条一样。
  - 打开：**当前形象**每秒 50 帧实时盯着鼠标（`_face_cursor`），**只转头、绝不移动窗口**：
    - 鼠标在**右边** → 往右转（侧面 + 镜像）
    - 鼠标在**左边** → 往左转（侧面）
    - 鼠标在**身上/附近**（70px 这个圈里）→ 正对着你
    - **没有背影**：鼠标跑到上方也只是正面 —— 一开始我按"跟跟随鼠标一样"做成上方给背影，
      主人明确不要这个（"不存在背面情况"），已改掉
  - **大肥鱼和小鲸鱼挂件都听这个开关**：三视图会转成侧面（大肥鱼看得最明显），挂件就是左右镜像。
    默认关着，所以不动这个开关的话，原地待着还是一直正面。
  - 实测（`test_still_front.py` ⑥）：左/右/右上/左上/右下/正上/正下/很远的正上/压在身上/贴身圈
    10 种位置朝向全对；鼠标乱晃 15 次窗口坐标一点没动；关掉开关 → 一直正面；
    三视图在这个开关打开时左边给侧面、正上方仍是正面（不背过身）✓
  - 真机（`probe_face_cursor.py`，真鼠标 + 真窗口）：10 个位置逐个对（含"正上方要正面"、
    "右上斜该右转"），朝向全部符合预期，窗口坐标没变；关掉开关后全部保持正面 ✓
    （用主人现在的形象「大肥鱼」跑的）
- **修：背影（背面形象）几乎看不到了**（主人反馈"我这么久没见过背面了"）
  - 原因是我上一版为了修"背过身去不转回来"，把朝向规则写成了"横向超过 4px 就侧面、
    **只有几乎笔直向上**才给背影"。而散步是**随机挑点**，几乎不可能笔直向上 ——
    实测老规则下背影只占 **0.165%** 的行程（按每分钟 3~4 趟算，差不多一小时才见一次），等于没有。
  - 现在改成按**方向**给（新函数 `_face_travel`）：
    - 横向位移 ≥ 纵向的 **2 倍** → 侧面（自动镜像）
    - 其余只要**往上走** → 背影（等于往屏幕里走、背对着你）
    - 往下走 → 正面
  - 阈值取 2 倍、不是对半分：屏幕是宽屏，横向位移天然比纵向大一截。按对半分算的话
    背影在 2560×1440 上只剩 14%（实机跑 90 秒一次都没等到）；取 2 倍后是 ~28%。
  - 实测（`test_back_view.py`，全过）：
    - 8 个方向 + 分界线两侧（横向=纵向×2）都符合预期
    - 4 万个随机目标（2560×1440）：**侧面 44.4% / 背影 27.9% / 正面 27.7%**；老规则 0.165%
    - 真跑 6 万帧散步（≈20 分钟）：**背面出现 31 次**、1103 帧
    - 真机 `probe_back_view.py`（真窗口自由散步）：180 秒那次 23 次朝向切换里有 1 次背面（t=104.1s）；
      第二次等到就提前收工，抓到了真实截图 `shot_back_背面.png` / `shot_back_侧面.png`
    - 真机 120 趟散步统计：侧面 52.5% / 背影 20.8% / 正面 26.7%（桌宠停在平时那个位置）
  - 另外：卡住保护还在（贴着屏幕边 2 秒不动就换目标并转回正面），所以不会再出现"一直背对着卡住"。
- **修：打开「ChatGPT - DeepSeek」启动器会把桌宠一起关掉**（主人反馈；改的是启动器，不在桌宠代码里）
  - 第一次查出来的坑：`%LOCALAPPDATA%\CodexTools\codex-health.ps1` 启动前会"清理残留的 Codex
    沙箱进程"，过滤条件是**按可执行文件路径**匹配 `*\codex-runtimes\*` 等目录。而桌宠的
    `.venv\Scripts\pythonw.exe` 只是个壳，真正在跑的那个进程路径就是
    `…\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\pythonw.exe`
    —— 于是被当成"残留"杀掉。已改成用 CIM 取命令行过滤：带 `dafeiyu-pet` / `桌宠.py` 的一律放过
    （实测：老过滤条件命中桌宠、新条件 0 命中）。
  - 但改完还会没（主人又反馈一次），**真因是进程树**：桌宠是从 Codex 里被拉起来的，就挂在 Codex 的
    进程树里；Codex 一重启（启动器 taskkill / 应用自动更新 / 崩溃重开），Windows 把整棵树一起带走，
    桌宠就消失了。实测祖先链（2026-09-13 02:04）：
    从 Codex 里起 → `pythonw <- pwsh <- codex.exe <- ChatGPT.exe`；
    从资源管理器起 → `pythonw <- explorer.exe`（不受影响）。
  - 现在的处理（`codex-health.ps1`）：重启前记下桌宠的启动命令行，新实例起来后若桌宠没了，就把它
    拉回来 —— 三种方式按顺序试：① **计划任务**（没有就建一个「大肥鱼桌宠」，由任务计划服务拉起，
    桌宠的父进程就是资源管理器/任务计划服务，跟"开机自启"同一条路）② **WMI**
    （`Win32_Process.Create`，父进程 WmiPrvSE）③ 资源管理器打开桌面快捷方式。
    另外加了 `-PetOnly`（只把桌宠拉起来，不动 Codex）。详见 `%LOCALAPPDATA%\CodexTools\README.txt`。
  - 实测：`-PetOnly` 在"桌宠没在跑"时成功拉起（祖先链 `pythonw(会话1) <- svchost/services(会话0)`，
    窗口正常显示）、"桌宠在跑"时不重复开；WMI 起的进程照样带 `DEEPSEEK_API_KEY` 这类用户环境变量；
    脚本语法检查通过；新代码整段包了 try（桌宠的事不会耽误 Codex 启动）。
    完整的"启动器重启 Codex"整条流程没在这台机器上跑 —— 它会把正在运行的这个 Codex 一起关掉。
  - 给以后的我：要重启桌宠时，走 `codex-health.ps1 -PetOnly`（它用 WMI 起，进程挂在 WmiPrvSE 下），
    **不要**直接 `Start-Process pythonw.exe` —— 那样起的桌宠挂在 Codex 进程树里，下次 Codex 重启
    就被一起带走。（用 `explorer.exe` 打开快捷方式也行，但偶尔会由"临时 explorer 进程"代劳，
    不保证脱离，所以 WMI 更稳。）

## v1.0.14

- **一级菜单最上面多了一块「快速查看」独立区域**（主人要求）：把平时最常用的三个"看一眼"
  从各自的子菜单**挪到一级菜单**，单独一块（上下都有分隔线），不用再一层层点进去：
  - **查看余额**（原来在 余额 → 查看余额）
  - **查看天气**（原来在 天气 → 查看天气）
  - **看一眼在放什么**（原来在 音乐联动 → 立刻看一眼在放什么）

  子菜单里对应项已移除，但各自的设置项都还在（余额的 Key / 来源 / 常显 / 每轮消耗、
  天气的城市列表与设置、音乐联动的开关 / 歌词对时 / 微调）。
- **歌词气泡两处按主人要求改了**：
  - **"下一句"永远显示**（QQ音乐等有歌词的）：以前这一句折成多行就把"下一句"藏起来了，
    现在不管这一句多长，下一句都照常显示在下面（多一行字，气泡高度会跟着变，走 0.15 秒过渡）
  - **信息缺失时用固定占位**：缺歌名 → 「**无题**」，缺歌手 → 「**未知歌手**」，
    缺应用名 → 「**未知应用**」，命名风格统一（`TITLE_PLACEHOLDER` / `ARTIST_PLACEHOLDER` / `APP_PLACEHOLDER`）；
    气泡顶部固定显示「♪ 应用 · 歌名 —— 歌手」
  - **放歌期间这块信息一直显示**：以前只有"真的有歌词"时才显示，现在**只要在放歌就显示**
    （没有歌词 / 网易云不报进度 → 显示「♪ 应用 ·《歌名》—— 歌手」），
    直到这首歌放完或切歌才消失/换掉
  - 实测：全缺 → 《无题》——未知歌手；只缺歌手 → 《某首歌》——未知歌手；
    网易云播放中气泡模式 = lyric（显示歌名）；长句 + 下一句 → 4 行、下一句在里面 ✓
- **网易云改成"只显示歌名 + 歌手"**（主人定的方案）：实测网易云**从不报播放进度和时间轴**
  （歌曲播放中也一样），所以对它的歌**不再抓歌词、也不猜进度**，气泡里只显示
  「♪ 网易云音乐 ·《歌名》—— 歌手」，一直保持到切歌；切到 QQ音乐这种正常报时间轴的播放器，
  歌词照常显示。判定依据是"播放器有没有给时间轴"（`has_timeline`），不是写死播放器名字 ——
  以后网易云要是改好了，会自动恢复歌词
  - 实测：网易云 → 0 行歌词、0 次抓歌词；同一首歌换到 QQ音乐 → 歌词 2 行恢复；
    网易云换下一首 → 仍只有歌名 ✓
- **修：放歌时拖动桌宠/拖动别的窗口会卡顿掉帧**（主人反馈）。查下来主要两个来源：
  - **每 2 秒一次的"把自己顶到最前面"**（为了不被 Codex 那种置顶窗口压住）：每次 `SetWindowPos`
    都会让 Windows 合成器重排一次 Z 序，**按住鼠标拖动时正好撞上就会卡一下**。现在加了两道闸：
    ① **左键按着（正在拖窗口/拖文件）时什么都不做**；② 只有真的被另一个**置顶窗口**压住时才动，
    平时不再白折腾
  - **歌词排版每帧重算**：`wrap_text` 一次 ~1ms，100 帧/秒的过渡动画等于每帧白烧 1ms；
    现在**排版结果缓存**（换句/换宽度才重算），并且过渡动画**只重画气泡那一块**，不再整窗重画
  - 实测（同一台机器）：空转 12 秒 CPU 1.73 秒 → **1.27 秒**；放歌+歌词 1.81 秒 → **1.44 秒**；
    主时钟抖动（20ms/帧）基本不变（中位 20.0ms、p99 ~21ms）；
    按住左键模拟拖动时，10 次"顶置"调用里真正动 Z 序 **0 次** ✓
- **修：网易云暂停 / 恢复播放会把进度清零、拖动进度条跟不上**（主人反馈）：
  - **根因**：网易云一暂停**可能把整个媒体会话收掉**，而代码在"没歌"时会清空歌词和进度
    → 恢复播放就从头开始了。现在**会话消失不清空**（只把气泡收起来），恢复播放接着走；
    消失超过 30 秒才当成重新开始
  - 新增菜单「音乐联动 → 这首歌对不上？微调 → **按播放器显示的时间对齐…**」：
    输入播放器上显示的进度（比如 `1:35`，`01:35.5`、`95` 也认），桌宠立刻按这个时间对齐整首歌 ——
    这是网易云这边**最准**的办法（它不报进度，拖动进度条后用它对一次就行）
  - 另外开了「记菜单日志」后，每轮读取的"播放器报的进度"都会写进日志（`[音乐] … position=…`），
    以后对不上可以直接看日志确认是播放器没给数据、还是程序算错
  - 实测：会话消失再回来歌词还在、进度仍是 45.0 秒；对表到 1:35 → 立刻 95.0 秒；
    消失 60 秒后重新开始 → 归零 ✓
- **逐字高亮关掉了**（主人说不需要）：现在整句一个颜色，不再有"唱到哪变蓝到哪"。
  代码还留着（`LYRIC_KARAOKE_ON = False`），以后想要可以再打开
- **网易云歌词对不上，查到根因了**：实测系统媒体会话里，
  **网易云根本不报播放进度**（`position` / 总时长 / 时间轴全是 0，`last_updated` 是"从没设过"的默认值），
  而 **QQ音乐是正常报的**（实测 position=78.63s、总时长 242.33s）—— 所以"只有网易云对不上、切回 QQ 就正常"。
  网易云这边我们只能自己计时（从"看到它在放"开始累加），因此：
  - **放歌时轮询从 1.5 秒缩到 0.8 秒**（看得越勤，"从哪一刻开始算"的误差越小）
  - 新增菜单「**音乐联动 → 这首歌对不上？微调**」：
    `歌词太慢，往前赶 0.5 秒 / 往前赶 2 秒 / 歌词太快，往后压 0.5 秒 / 往后压 2 秒 / 这首歌的微调清零` ——
    **只影响这一首歌，并且按歌记住**。中途才开始看（或拖过进度条）时，用它把整首歌的歌词挪到对得上的位置
  - 实测：原 30.0s →「往前赶 2 秒」→ 32.0s →「往后压 0.5 秒」→ 31.5s →「清零」→ 30.0s，且按歌记住 ✓
- **修：歌词不显示、要切歌才恢复**（主人反馈）：抓到空歌词时以前会把正在显示的歌词**擦掉**，
  而且这首歌再也不会重试。现在：
  - 抓失败**不清空**已在显示的歌词
  - 这首歌没歌词时，**每 15 秒自己重抓一次**（不用切歌）
  - 歌词缓存加了**版本号**：旧缓存（以前只存了行级歌词）会在开歌时**自动重抓一次**，
    把有逐字歌词的歌升级成"唱到哪高亮到哪"
- **修：暂停网易云 → 放一下 QQ音乐 → 再切回网易云，歌词还是不同步**（主人反馈）：
  根因是"换歌时记位置"那步算错了对象 —— 覆盖 `now_playing` 之后才算旧位置，结果把**新歌的位置**
  记成了旧歌的（实测：A 歌放到 90 秒，切走再切回来变成了 3 秒）。现在改成覆盖之前先算，
  并且每首歌都记"上次放到哪"，播放器不报进度时接着走 —— 实测切回来还是 90.0 秒
- **逐字歌词（唱到哪高亮到哪）确认可用，但要看歌**：我用**真实接口**试了 10 首网易云的歌，
  其中 3 首返回了逐字歌词（`yrc`，形如 `[0,6800](0,480,0)OP(480,480,0)：...`），
  解析后每个字都有自己的时间（多字词比如 "OP" 会在词内均分），高亮能贴合真人演唱；
  **没有 yrc 的歌**（比如《晴天》这种老歌）只有行级时间戳，只能匀速推 —— 这是**数据本身的限制**。
  QQ音乐的逐字歌词是加密的（qrc），本次没做。
- **插耳机切换更快了**（主人反馈"要等近 2 秒"）：原来只靠 2 秒一次的轮询，现在
  - 直接接上 `QMediaDevices.audioOutputsChanged` **信号** —— 插上耳机/切蓝牙音箱会**立刻**触发，
    缓 150ms（等设备就绪）就重开音频流
  - 兜底轮询从 **2 秒缩到 0.5 秒**（有些设备不发信号）
  - 新设备一时开不了会**自动重试 3 次**（每 400ms），不再立刻退回 QSoundEffect
  - 实测：信号已接上、轮询 500ms、收到"插耳机"信号后 0.5 秒内音频流已切好（ok=True）
- **修：网易云中途暂停后歌词从头开始 / 拖动进度条歌词不跟着跳**（主人反馈）：
  - **暂停时播放器会短暂不给歌名**（网易云就这样），以前这被当成"换歌" → 清空歌词、进度归零，
    于是"从头开始显示"。现在**没有歌名时不当成换歌**，歌词和进度都原地保留
  - **暂停时进度停住**：以前算进度时不管放没在放都在按墙上时间往后跑，暂停一会儿再回来就整体偏了；
    现在暂停期间就用播放器报的那个进度（不再累加）
  - **拖动进度条**：同一首歌里如果播放器报的进度和我们自己推的差 2.5 秒以上（跳转 / 回退），
    就以播放器为准重新对表 —— 歌词立刻跟过去
  - 实测：暂停时"不给歌名"的更新不会清歌词/归零（进度仍 45.0 秒）、暂停 6 秒后算出来还是 45.00 秒、
    拖回 5 秒时内部进度重置到 5.0 秒
- **"唱到哪、字变到哪"现在能贴合真人演唱**（主人反馈：匀速推跟不上）：
  - 抓歌词时多要一份**逐字时间戳**（网易云的 `yrc`），解析后每个字都有自己的开始时间，
    高亮按**每个字的真实时间**推进（还会在当前字里平滑走完），不再假设"这一句匀速唱完"
  - 也支持带行内时间戳的增强 LRC（`[00:16.25]有<00:16.68>些<00:17.11>话`）
  - 只有行级时间戳的歌（比如 QQ音乐）没有逐字数据，只能退回匀速高亮 —— 这是**歌词数据本身的限制**，
    不是程序的问题（QQ音乐的逐字 `qrc` 是加密的，没去碰）
  - 实测：逐字歌词下 5.3 秒时"已唱 1.60 个字"（「你」5.0 唱完、「好」5.5 还没到）
- **气泡大小变化也有过渡动画**（主人补充）：文字变多/变少导致气泡要变大变小时，
  宽高同样用 **0.15 秒、100 帧/秒**平滑过渡，不再"啪"地换成另一个框
- **歌词过渡更顺、更快，而且只动歌词本身**（按主人三条反馈改的）：
  - 过渡时长 **0.30 秒 → 0.15 秒**，并且过渡期间改用**专用 100 帧/秒**的计时器重绘
    （主时钟在"休闲模式"只有 25 帧，0.15 秒里才 4 帧，看着就是一顿一顿；现在是 10ms 一帧、约 15 帧）
  - **只有歌词文字在过渡**：气泡（那个"窗口"）本身不再突然变大变小 ——
    宽高在过渡期间从上一句**平滑过渡**到这一句（`_lyric_prev_w/_lyric_prev_h`），
    不会出现"窗口先消失、又弹出一个新的"那种感觉
  - **"唱到哪、字就变到哪"**：当前这句里**已经唱过的部分**换成 DeepSeek 蓝（`LYRIC_KARAOKE_COLOR`），
    按"这一句的起止时间"算进度（`_lyric_progress()`），再按字数摊到每一行（`_karaoke_split()`），
    绘制时用裁剪把已唱部分重画成高亮色 —— 多行歌词也会一行一行地跟着变蓝
  - 实测：过渡 10ms/帧共 16 帧（0.15 秒）、进度 50% 时该句 33 字里 16 字变蓝（分字 [10, 6, 0, 0]）、
    唱到 2 秒 / 一句 4 秒时进度正好 50%
- **修：歌词显示不全**（主人反馈）。原来长句折行后**只画前两行**，多出来的部分直接丢掉；
  折行宽度也只有 260px。现在：
  - 折行宽度放宽到 **300px**（还受窗口宽度约束）
  - 一句话太长时**先把字号往下缩**（11 → 7 磅），缩到底还装不下就**多铺几行**
    （最多 8 行），同时把顶部那行「♪ 应用 · 歌名」让出来腾高度 —— 宁可气泡高一点，也不丢歌词
  - 实测：**5 档尺寸（迷你…大）× 中文长句 / 76 字符英文长句 / 短句** 全部 100% 显示、不截断
    （最极端那档会缩到 7 磅、7 行）
- **歌词切换加了过渡动画**（主人说"太生硬"）：换句时用 **0.3 秒**做过渡 ——
  新的一句从下方 8px 滑进来并淡入，旧的一句向上滑出并淡出，像歌词条滚动那样
  - 实现：`_lyric_rows()` 负责折行 + 取行；`_lyric_shown / _lyric_prev / _lyric_anim_t`
    在 `tick()` 里跟着进度更新，绘制时用 `setOpacity` + 位移做过渡
- **修：插上耳机以后，点击音效还在笔记本外放里**（主人反馈）。根因：点击音走的是
  "常开音频流"（`QAudioSink`），这条流在**启动时**就绑定了当时那个默认输出设备，
  之后系统换了默认设备它也不会跟着变。现在：
  - `ClickPlayer.rebuild_output()`：把常开流关掉、按**当前**默认设备重开一条
  - 这个 PySide6 没有 `defaultAudioOutputChanged` 信号，所以**每 2 秒自己查一次**默认输出设备，
    发现变了就重开（第一次只是记下来，不白折腾）；`QSoundEffect` 兜底那套也会跟着重建
  - 实测：设备的 id 记录/刷新、变化后重开流并保持 `ok=True` 都正常
- **修：放歌时歌词总是比声音快一点**（主人反馈）。对不上主要来自**输出设备的延迟**
  （笔记本外放几乎没延迟，蓝牙耳机/音箱能差 0.3~1 秒）。现在菜单「音乐联动 → **歌词对时**」
  六档可选：`文字提前 0.5 秒 / 提前 0.2 秒 / 刚好同步 / 延后 0.2 秒 / 延后 0.5 秒 / 延后 1.0 秒`，
  **默认"文字延后 0.2 秒"**（正是为了不再抢在声音前头）；菜单标题会显示当前档位，
  换档立刻生效、写进 `config.json`
  - 代码上是 `_current_lyric_pair()`：用「当前进度 − 对时偏移」去挑歌词行，方便以后调整
  - 实测：进度 10.2 秒时，"刚好同步"显示第二句、设成"延后 0.5 秒"就退回显示第一句（说明文字确实在等声音）
- **修：透明度和音效音量调不了**（主人反馈）。根因：这两个原来都是**塞在弹出子菜单里的滑块**，
  而这台机器上 Qt 的弹出子菜单**收不到鼠标消息**（我们早知道这个毛病，之前菜单高亮就是因此要自己喂），
  所以滑块根本拖不动。现在改成**点开一个小窗口拖滑块**：
  - 右键菜单 →「**透明度…（现在 xx%）**」→ 小窗口里拖滑块，**拖的时候立刻生效**（20%~100%）
  - 右键菜单 → 音效 →「**音量…（现在 xx%）**」→ 同样的滑块，另配一个「**试听一声**」按钮
  - 菜单项上直接写着当前数值（100% / 90% 这种），一眼能看到现在是多少
  - 实测（真鼠标）：两个入口都能弹出小窗口，拖动后 `win.opacity` / `windowOpacity()` /
    `volume` / 常开音频流音量 / `config.json` 全部同步
- **顺带把"菜单项要弹窗口"这条路径改稳**：以前直接在菜单回调里弹窗口，
  偶尔会被菜单自己的关闭动作一起带走（窗口一闪就没）。现在**等菜单收完再弹**
  （根菜单项延迟一拍执行；二级菜单项改成"先把菜单收掉，下一拍再执行那一项"）。
  这条也顺手让「添加我的音效…」「删掉我加的音效…」「台词内容…」更稳

## v1.0.13

- **多了 Windows 安装包（中文安装向导）**：附件 `dafeiyu-pet-setup.exe`
  - 双击运行 → 中文向导，**中间那一步可以自己选安装位置**
    （默认装到当前用户目录 `%LOCALAPPDATA%\Programs\大肥鱼桌宠`，不弹 UAC）
  - 自动建桌面 / 开始菜单快捷方式，装完**安装包本身就能删**
  - 卸载走「设置 → 应用 → 大肥鱼桌宠」；**配置在 `%APPDATA%\大肥鱼桌宠\` 不会被删**，
    重装还能接着用（想彻底清就手动删那个目录）
  - 脚本在仓库里 `installer.iss`（Inno Setup 6 编译），以后双击「发布到GitHub.bat」
    会自动打包 exe + 安装包一起上传
  - 免安装绿色版 `dafeiyu-pet.exe` 照旧提供，两个都放在 Release 里
- **台词窗口一起升级了**（右键菜单「文案 → 台词内容…」）：
  - 窗口顶部多了「**说多勤（语录频率）**」下拉，和菜单里那一项是同一个设置 ——
    改台词、调频率在同一个窗口里就能搞完，改完立刻生效
  - **「说多勤」那几句话自己也变成可改的了**：台词窗口的类别里多了
    「说多勤·安静 / 正常 / 话多 / 话痨（切到这一档时说的）」四类，
    每类默认 2 句，可以自己写、也能一键恢复默认（改完切档时立刻用你写的那句）
  - 这四档的**内置话术也重写了一版**，跟原来那套语录的味道对齐了，例如
    「行行行，我闭麦了，省点电给模型训练」「话多的开关被你按了，你自找的」
    「碎碎念模式启动，接下来求你别嫌吵」（原来那两句"好，我尽量闭嘴"那种风格太正经，换掉了）
  - 说明文字也写清楚了：一行一句（空行忽略）、放歌那两类可用 `{song}`、
    **保存时留空 = 恢复这一类默认**、说多勤只管"自言自语"（点它/拖它/换歌的反应不受影响）
  - **内置台词补了几条新的**（跟着现在的功能走），比如日常类加了
    「又来看余额了？省着点花，别把我饿着」「双击我一下，余额马上给你看」
    「嫌我吵就把『语录频率』调成安静，我立刻闭嘴」；点击回嘴、心声、放歌/换歌那几类也各补了 1~2 条。
    自己改写过的类别不会被覆盖（自己写的优先）
- **语录频率可以自己选了**：右键菜单「文案 → 语录频率」，四档（由少到多）

  | 档位 | 大概多久冒一句 |
  |---|---|
  | 安静 | 约 30 秒 |
  | 正常 | 约 12~17 秒（默认，和以前一模一样） |
  | 话多 | 约 7~8 秒 |
  | 话痨 | 约 4~6 秒 |

  - 管的是"闲着时自己冒话"的频率（点它、拖它、换歌这些反应不受影响）
  - 菜单里当前档位打勾，换档立刻生效、写进 `config.json`
  - 放歌时照旧不插嘴（把位置让给歌词）；「正常」这一档就是原来的参数，选了不会变味
  - 实测：20 万帧（约 66 分钟）里四档分别说了 125 / 280 / 474 / 781 句
    （平均 32 / 14 / 8.4 / 5.1 秒一句），相邻两句的最小间隔也都满足各自设定（1500 / 600 / 300 / 150 帧）

## v1.0.12

- **大小多了两档更小的**：现在一共 **迷你 / 特小 / 小 / 中 / 大** 五档
  （精灵高度 102 / 142 / 187 / 237 / 306 px，原来只有小 / 中 / 大）
  - 精灵图、吸附、走路、拖拽都按新档位正常工作（每一档都有自己的缩放图）
  - 顺手加了个**最窄窗口宽度（168px）**：小档位精灵很窄，如果窗口跟着一起变窄，
    余额气泡（余额 / 金额 / 今日已用 / 峰谷四行）会被挤成一列竖字；
    现在气泡保持能看的宽度，小精灵在中间显示
  - 菜单「大小」里五档依次排开，勾选状态照旧

## v1.0.11

- **快速双击 = 看一眼余额（显示 5 秒）**：
  - 配了 Key 才启用（`config.json` 的 `ds_api_key` / 环境变量 `DEEPSEEK_API_KEY` / 自己加的其他余额来源）
  - **没配 Key 就不启用**：双击还是原来的"换姿势"，并且**每次启动只提醒一次**
    「双击看余额要先填 Key：右键 →「余额 → 设置 Key」」
  - 放歌时双击也是余额（优先于歌词），看完自动回到歌词；余额还没取到时会先拉一把、回来立刻冒 5 秒
  - 顺带修一个老毛病：双击判定原来用**桌宠自己的动画时钟**（`self.t × tick_ms`），
    动画一卡这个钟就走得比真实时间慢 —— 实测把相隔 0.8 秒的两次单击也误判成"快速双击"。
    现在改成**按真实时间**判（墙上时钟，阈值仍是 380ms）
## v1.0.10

- **菜单里"选中哪一条"的高亮现在跟着手走了**（`_highlight_menu_item`）：
  这台机器上 Qt 的弹出菜单收不到鼠标移动，所以高亮一直不更新 —— 快速上下滑一级菜单时
  看着就是"卡住"。现在每一帧原生移动 + 40ms 对账都会把"光标下这一条"点成选中，
  实测一级菜单快速上下走 10 次**全部跟得上（0/10 卡住）**
- **从「添加我的音效…」往旁边移，选中不再卡在「音效选择 / 音量」上**：
  原因是光标落到同菜单的**另一条**上时，老的子菜单还在被"顶回来"，
  顺手把高亮又拽回旧的那一条。现在这种情况直接按"换条"处理（旧的收掉、高亮给新的一条），
  实测二级菜单 8 次移动**全部正常（0/8）**
- **可以加自己的音效了**：右键菜单「音效 → 添加我的音效…（自己挑 wav）」，
  挑一个 wav 就自动上架、立刻生效、顺手试听一声
  - 会自动转格式：**立体声 → 单声道、任意采样率 → 44100Hz、位深 → 16 位**
    （常开音频流那个播放器只吃这种格式，不转的话要么没声、要么开头被吞）
  - **会提醒最合适的时长区间**：点击音最适合 **0.15~0.60 秒**（太短听不清、太长连点会叠成一团），
    太短会问"还是要用吗"，太长会问"要不要帮你裁到 0.60 秒（只保留开头）"
  - 自己加的音效排在「音效选择」里（和内置的用一道分隔线隔开），
    不想要了用「删掉我加的音效…」删掉（内置两套动不了）
  - 文件存在 `sounds/`（安装版在 `%APPDATA%\大肥鱼桌宠\sounds\`），路径记在 `config.json`，
    下次启动自动带上
- **子菜单重叠再减 25%**：12px → **9px**（主人觉得还是压得多）。
  实测 3 个位置 × 20 条带子菜单的项共 60 次：重叠 **9~10px**（取整误差 1px 以内）、
  顶部错位 0、实际落点和我们算的一致

- **二三级菜单（右键菜单里的子菜单）这次是真修好了**，按主人定的「方案 2」做：
  子菜单和上一级菜单贴着弹出（`MENU_OVERLAP`：16 → 12 → **9**，主人两次都说"压得有点多"，
  各减 25%），鼠标从一级移到二级几乎不用走空档；
  位置在**弹出之前**就算好，不会再出现"先出现在老地方、再往里跳一下"
- 方案 1（把一级菜单的触发范围横向放大）按主人的意思**弃用**（开关留在代码里，值为 0）
- 顺手修掉真机测试揪出来的三个问题：
  - **光标移到三级菜单上、二级菜单就消失** —— 根因是"换条时把上一个子菜单收掉"会把
    光标脚下这一层也收走（三级菜单正是二级那一项的子菜单）。现在收之前先判断
    "要收的这层是不是光标现在所在层或它的上层"，是就不收
  - **子菜单压住一级菜单的条目**（「层级 / 透明度」悬停半天不出子菜单就是这个原因）：
    子菜单往左弹时，现在会**一层一层同样往左**，三级菜单不会绕回来盖在爷爷菜单头上
  - **上一支子菜单残留在屏幕上**：每 120ms 对账，**一次只留光标所在的那一支**（跟 Windows 原生一致）
- 验证（真鼠标、拟人速度）：20 条带子菜单的菜单项**左右两侧各扫一遍，全部正常、闪烁 0 次**；
  光标停在三级菜单里 2.5 秒，二级/三级菜单 **100% 可见、不闪**；点三级菜单里的「用这个形象」能真的切换形象；
  11 个自动化测试套件全过
- **托盘菜单（右键任务栏小图标）一起修好**：
  - 托盘那条路是 Qt 自己弹菜单、而且每次右键都会**重建一份**菜单，万一弹的是上一份，
    程序记着的那棵树就跟屏幕上的对不上（悬停兜底、收子菜单、点击摆渡全部落空）。
    现在每 120ms 认一次"屏幕上真正开着的那棵菜单"，认错了就换过来接管
  - 子菜单被 Qt 收掉、而光标还停在它原来那一片时，现在会**分三步往回捞**
    （让 Qt 重开 → 替 Qt 往那一条塞一个鼠标移动 → 都不行才自己补弹）
  - 实测：托盘菜单按**真实托盘位置（屏幕右下角，菜单要往上/往左翻）**扫 20 条，
    20/20 正常、闪烁 0 次；连"Qt 弹了上一份旧菜单"这种最坏情况也是一样
- **重叠像素量过了**：多个位置 × 20 条带子菜单的项（含二级、三级）逐条量，
  **横向重叠每次都等于设定的值**（16 时 80/80 都是 16；改成 12 后 60/60 都是 12）
  （顶部和条目对齐，固定差 4px，误差 ±1px），而且**子菜单实际落点和我们算的完全一致（0 处偏差）**。
  顺手揪出并修掉三个会让"看着重叠忽多忽少 / 位置不对"的真问题：
  - **残留的子菜单会把下一次右键挡掉**（最要命的一个）：Qt 偶尔不跟着根菜单一起收子菜单，
    屏幕上留一个"孤儿"弹窗；它挡在那儿，下一次右键菜单干脆弹不出来。
    现在每 120ms 对账，根菜单一关就把残留子菜单收掉（实测：修前"再右键打不开"，
    修后 4/4 轮都正常打开）
  - **"认屏幕上那棵树"的逻辑会把孤儿子菜单误认成整棵树的根** → 改成在**所有**菜单里找父关系
  - 子菜单被 Qt 收掉、光标还停在它原位时，分三步往回捞（Qt 重开 → 塞一个鼠标移动 → 才自己补弹）
- **子菜单位置"自愈"**：每 120ms 对一次账，只要某个子菜单没贴在"你正悬停的那一条"上
  （偏了 2px 以上），就把它挪回去 —— 不管是 Qt 自己摆歪的、弹出时尺寸还没算准，
  还是被别的动作挪过。实测：故意把子菜单挪歪 37/63 像素，**0.45 秒内自己回到原位**
  （横向重叠回到 16px、顶部错位回到 0），一级和三级都验过
- **新增「记菜单日志（菜单出问题时打开）」**（右键菜单 → 救急恢复下面那一项）：
  打开后菜单的展开/换条/位置自愈都会记到 `menu-debug.log`
  （源码版在程序目录、安装版在 `%APPDATA%\大肥鱼桌宠\`）。
  以后主人再遇到"位置不对"，打开它复现一次，把日志给我就能精确定位
- **"先小部分重叠、再往里移动"修掉了**：Qt 自己弹子菜单时用的是它那套摆法
  （贴着条目边、不重叠、还可能高一截），我们下一拍才发现位置不对再挪过去 —— 看起来就是
  "先摆一版再往里移"。现在给每层子菜单装了"**一显示就摆正**"的过滤器：
  摆正发生在**同一帧**里（实测：故意按 Qt 的摆法弹出来，8/8 都是同一帧就摆到位，看不出挪动）
- 菜单开着时对账间隔从 120ms 提到 **40ms**（`MENU_HOVER_FAST_MS`），万一还有哪层被摆歪，
  下 40ms 就纠回来；菜单关掉自动回到 120ms，不白费电
- 光标还停在原来那片、子菜单却被 Qt 误收时，**自动原地弹回来**（以前会在 0.35 秒宽限后放弃，
  表现就是"偶尔进去一条就没了"）

## v1.0.9

- **台词可以自己写了**（换了形象之后默认台词不搭的场景）：右键菜单「文案 → 台词内容…」
  一个窗口里改所有台词，分六类：日常台词 / 点击回嘴 / 心声 / 拖拽台词 / 放歌时点击 / 换歌时
  - 直接编辑，一行一句（空行自动忽略）；**既能自己写新的，也能直接改写内置的**（编辑框里就是当前生效的那套）
  - 放歌那两类里可以用 `{song}` 代表「《歌名》——歌手」
  - 每一类旁边都能「恢复这一类默认」；菜单项会显示「已改 N 类」
  - 菜单层级和以前完全一样（就挂在原来的「文案」下面，没有多一层）
- **闲着的时候更爱说话了**：触发概率提高（0.01 → 0.03），并且"想说话"那一档的占比从 10% 提到 30%，
  说话冷却从 30 秒缩短到 12 秒 —— 实测平均 **约 17 秒冒一句**（以前约 30 秒），
  配上自己写的台词更有存在感

## v1.0.8
- **修复：桌宠会"卡在鼠标穿透"回不来**。解除穿透原来只靠 Qt 的 `aboutToHide` 信号，
  实测这个信号会漏：菜单早关了、信号没来，桌宠就一直停在"鼠标穿透"状态，怎么点都没反应
  （去看 `config.json` 会发现 `passthrough=false`，但窗口其实还带着穿透位）。
  现在改成每 120ms 按"**现在到底还有没有菜单/对话框开着**"对账，漏信号也能自己恢复
- 开发过程备注：「菜单打开期间把桌宠藏起来」这个思路试过又回退了 —— 藏桌宠会把菜单
  一起带崩（菜单闪一下就自己消失，快速点击还可能让桌宠一直不出现）。现在菜单开着时
  桌宠保持显示，只用"临时取消置顶 + 鼠标穿透"让位
- **修复：二级菜单在有些位置点不中**（靠屏幕右边、子菜单往左弹到桌宠身上时最明显）。
  实测发现根因是**系统根本没把鼠标移动消息送给弹出菜单**（消息全发给了桌宠窗口），
  于是 Qt 收不到 hover、二级菜单一直不弹，自然就"点不中"。现在：
  - 菜单打开期间桌宠会临时退到非置顶层，菜单天然压在它上面（**不要**给桌宠加鼠标穿透，
    否则右键桌宠会穿到桌面，弹出 Windows 桌面那个右键菜单）
  - 加了**悬停兜底**：菜单开着时自己盯着光标位置（`QCursor.pos()` 一直是准的），
    在带子菜单的项上停一下（约 0.22 秒），就替 Qt 把子菜单弹出来
  - **二三级菜单不容易消失了**：光标离开子菜单后要等 **0.5 秒**才收（斜着划过去也不会误关）；
    万一 Qt 因为收不到鼠标消息把子菜单自己关了，只要光标还在它原来的范围内，就**自动补弹回来**
  - 桌宠收到的鼠标移动会**转发给菜单**，让 Qt 的悬停高亮、展开、收起都按正常逻辑走
  - **关键修复：子菜单改用 Qt 自己的 `setActiveAction` 展开**。之前是悬停兜底自己
    `popup()` 一个子菜单窗口 —— 窗口是弹出来了，但 Qt 内部并不知道它是"这一项的子菜单"，
    于是**点击不会被路由到子菜单上**，表现就是二级、三级菜单"点不了"（一级菜单却正常）
  - **最终结论：二三级菜单回滚成 1.0.7 的原生行为**。这一轮为了修它试过很多方案
    （悬停兜底、拦截/转发事件、延时收起、自动补弹、点击摆渡、原生消息层接管……），
    根因也查清了（这台 Windows 不把鼠标移动/点击送给**弹出式子菜单**，点击会被送给
    持有捕获的上层菜单 —— 抓原生消息确认过），但每次都只能改善一半，反而引入新毛病。
    所以按主人的意思**不再折腾**：子菜单完全交回 Qt，我们只保留与菜单无关的那两个修复
    （穿透状态自愈、不再给桌宠加穿透/不再隐藏桌宠）
- **修复：在桌宠上重复右键会弹出 Windows 桌面的右键菜单**。上一版为了让点击不被桌宠截住，
  菜单打开期间把桌宠设成了鼠标穿透 —— 结果右键桌宠时点击直接穿到桌面上去了。
  现在菜单期间**不再给桌宠加穿透**，只把它降到非置顶层（实测二级菜单依旧能悬停展开、点得中）
- **天气**：放歌时点「查看天气」，结果气泡和余额一样停 **5 秒**再让位给歌词
  （计时是从天气结果查回来、显示出来那一刻开始算的）
- **城市列表**：添加过的城市会记进列表，菜单「天气」里直接列出来，点一下就切过去（当前城市打勾），
  还能「从列表里删掉城市…」（至少留一个）
- **新增「流畅度」菜单：性能模式 / 休闲模式**
  - **性能模式**（默认）：动画优先，**打开设置菜单也不掉帧**。以前菜单一开，桌宠就降到
    约 5 帧、并且不再刷新，看着像卡住
  - **休闲模式**：帧间隔由 20ms 放到 40ms（50 帧 → 25 帧），菜单打开时降到约 5 帧，更省 CPU。
    动作都按同一个内部时钟换算，所以**走的速度、气泡停留时间都不变**，只是画面更"省"
- **音乐联动（QQ音乐 / 网易云音乐）**：放歌时头顶气泡变成歌词 —— 大字当前这一句、
  下面淡一行下一句，顶上小字标「♪ 应用 · 歌名」。歌名 / 歌手 / 播放状态 / 播放进度
  直接读 **Windows 系统媒体会话**（不用登录、不用填 Key），暂停和切歌都能立刻跟上
  - 歌词联网找：先网易云、再 QQ音乐（两个接口都不用 Key），找到的缓存进 `lyrics_cache.json`，下次秒开
  - **放歌时点它**：单击 → 冒一句「放歌ing：《歌名》——歌手」；
    **快速双击** → 立刻看 5 秒余额，看完自动回到歌词
  - **余额常显也不冲突**：放歌时歌词优先，余额双击就能看；定时刷新冒出来的余额气泡也不会打断歌词
  - 菜单新增「音乐联动」：整个关掉 / 只报歌名不显示歌词 / 立刻看一眼在放什么
  - 读不到系统媒体会话时（老系统、精简系统），自动退化成读播放器的窗口标题
- 双击桌宠现在有明确动作：平时换姿势，放歌时看 5 秒余额（以前说明书里写了但没实现）
- 麻烦的依赖：打包版体积从 60MB 涨到约 72MB（多了读系统媒体会话的 `winsdk`）

## v1.0.7

- **修复：托盘右键菜单打不开，导致开了「鼠标穿透」后无法解除**。
  根因是上一版为了让菜单不被桌宠窗口遮挡，改成了「无父窗口的弹出菜单」，
  但 Qt 的托盘菜单**不接管所有权** —— 菜单对象随后被 Python 回收，
  右键托盘图标就弹不出任何东西，穿透也就解不回来了。
  现在托盘菜单会被显式持有（并且每次右键重建后立刻留引用），不会再被回收。
- **新增两条「救急」通道**，避免再被锁在穿透状态里：
  - **双击托盘图标**：立刻解除鼠标穿透并把桌宠叫回来（没开穿透时 = 显示/隐藏）
  - 开启穿透时会冒泡说明解除方法；如果开机时就处于穿透状态，2.5 秒后也会提醒一次
- 穿透开关会立刻写入 `config.json`，重启后状态与你的选择一致（以前只在退出时保存）

## v1.0.6

- **内置应用在列表里明确标记、不能重复添加**：应用扫描窗口里，默认就自带台词的应用会显示
  `●内置默认（不可重复添加）`（改写过的显示 `✎已改写默认台词`），选中它时主按钮直接变成
  **「修改默认台词」**，不会再给你重复加一份
- **可以修改内置应用的台词**：点「修改默认台词」会带着原台词打开编辑框，改完**整组替换**内置文字
  （写入 `default_line_overrides`，不会和原版叠加）；想还原就点「恢复内置台词」
- 数据层面也堵住了重复：写给内置应用的 `custom_process_lines` 会被忽略，
  内置应用只认「改写」这一条路；菜单里的「清理自定义 / 改写的文字…」可以一次清掉两类记录

## v1.0.5

- **应用扫描升级成带图标的列表**：不再是一串纯文字让你猜。
  菜单「进程联动 → 扫描电脑应用并添加…」现在会打开一个像「设置 → 应用」那样的窗口：
  **每个应用都带自己的图标**（图标直接从 exe 里取，不用额外素材）、
  顶部能**搜索**（输 steam / edge / 微信 都行）、列表里 `●` 表示默认就有台词、`✓` 表示你自定义过；
  选中后点「添加触发文字」写它要说的话（一句一行，可多行），也能一键「删除该应用的文字」
- 扫描结果同时带上每个应用的 exe 完整路径（这台机器实测扫到 133 个，全部能取到图标）

## v1.0.4

- **菜单重新分区**：「吸附」（拖拽吸附四边 / 左吸附翻面）和「文案」（显示峰谷时段 / 三档峰谷文案）
  从「余额」里拆出来，变成各自的独立菜单
- **进程联动可自定义**：新增「进程联动 → 扫描电脑应用并添加…」，
  会扫描本机应用（正在运行的进程 + 开始菜单快捷方式，实测这台机器扫到 133 个），
  选中后自己输入打开它时要说的文字（多句用 `|` 隔开）；也能「删除自定义应用…」。
  自定义台词会和默认台词表叠加
- **每轮消耗统计支持其他 agent**：可以设置 **Agent 名称**（显示成「上一轮 Claude 消耗 ¥…」）
  和**会话日志目录**，不用 Codex 的人也能指向自己的日志；
  日志解析改成兼容多种结构（Codex 的 `payload.usage`、Claude Code 的 `message.usage`、
  OpenAI 风格的 `prompt_tokens/completion_tokens` 都认）

## v1.0.3

- **修复二级菜单在屏幕边缘失灵**：菜单原来挂在桌宠窗口下、又强加了置顶标志，而桌宠本身也是置顶窗口，
  三者叠在一起时，鼠标往子菜单方向移动很容易被判定成"离开了菜单"，于是整个菜单被收起
  （靠屏幕右侧、子菜单往左弹的时候最明显）。现在菜单改成**独立的 Qt 标准弹出菜单**（不再依附桌宠窗口、
  不再自带置顶标志），改为**菜单打开期间桌宠临时退到普通层**（关闭后按设定层级还原），
  悬停展开和贴边反向弹出都恢复正常；菜单开着时桌宠的刷新率也降下来，减少对弹出菜单的干扰
- 顺带：对话框（设置 Key / 添加城市 / 选图片）期间同样让桌宠临时让位，不会被它压住

## v1.0.2

- **修复进程联动只有 Steam 触发**：以前只认"新出现的进程"，所以早就开着的微信 / 浏览器 / QQ 永远不触发。
  改成看**前台窗口**——你切到（或刚打开）哪个应用就吐槽哪个，同一个应用 10 分钟只吐槽一次
  （ChatGPT 15 分钟，免得你在这边聊天时它一直念）。检测间隔也从 4 秒缩到 2 秒，反应更快
- **修复自由散步卡在屏幕边**：目标点原来按窗口"左上角"生成，而移动时又会把窗口钳制在屏幕内，
  靠近顶边时目标永远够不到 → 它就顶着墙一直走、一直背对着。现在目标点按窗口中心生成并夹在可达范围内，
  另外加了"卡住 2 秒自动换目标"的保护
- **修复右键菜单二级菜单异常**：二级菜单会被一级菜单遮挡、鼠标一离开一级菜单整个菜单就消失（越靠屏幕边缘越明显）。
  根因是上一版为了让菜单压在桌宠上面，用了 `popup` + 每 400ms `raise_()`，会把子菜单顶掉；
  现在改回 Qt 标准的 `exec()` 模态弹出（菜单打开期间桌宠本来就不会抢置顶），二级菜单悬停展开、贴边反向弹出都正常

## v1.0.1

- **层级可选**：置顶 / 置底（在所有窗口下面，但仍在壁纸和 Wallpaper Engine 这类桌面软件之上）/ 普通层
- **透明度可调**：菜单里一条滑块，100% → 20%
- **自由散步更灵**：只要横向有位移就走侧面，不再"背过身去不转回来"；原地待着时会转头看鼠标
- **进程联动**：检测到 Steam / WeGame / 浏览器 / 播放器 / 编辑器等被打开时，冒泡吐槽一句
  （台词表在 `桌宠.py` 顶部 `PROCESS_LINES`，可以自己加；菜单里可整体关掉）
- **单实例**：已开着一只时再点快捷方式或再跑源码都不会冒出第二只，会把它叫出来
- **自定义形象**：菜单「形象」里两套外观都能换成自己的图片 —— 挂件是单张图，
  大肥鱼三视图可以分别指定正面 / 侧面 / 背面，随时一键恢复默认
- **多余额来源**：可添加别的 API Key。DeepSeek 与 OpenRouter 能查到余额（OpenRouter 按 credits − usage 算）；
  OpenAI / ChatGPT 这类官方没有余额接口的，会直接显示"该服务不提供余额接口"，而不是报错
- 仓库首页加了醒目的下载入口；Release 附件名固定为 `dafeiyu-pet.exe`，
  所以 `releases/latest/download/dafeiyu-pet.exe` 永远指向最新版

## v1.0.0

首个发布版本（打包版已修好启动报错，exe 双击即用）：

- 桌宠本体：三视图行走（左右镜像 / 向上背面 / 向下正面）、自由散步 / 跟随鼠标 / 原地待着
- 余额挂件：余额泡泡（余额 / 今日已用 / 当前峰谷时段）、数字滚动、每 60 秒自动刷新、点击手动刷新
- 拖拽四边吸附 + 左吸附整体翻转 + 按压 Q 弹 + 按键音效（按压+松手合并成一条完整音频，常开音频流播放）
- 每轮 Codex 对话消耗统计（读会话日志，按峰谷定价换算）
- 天气：手动设默认城市 / 联网搜索 / 按 IP 定位，播报全中文，wttr.in + open-meteo 双源
- 形象切换：大肥鱼（三视图）/ 小鲸鱼挂件（cut-out）
- 按需求精简：移除 AI 对话、喂食、系统状态监控
- 打包：PyInstaller 会把本机其它运行时的 ICU（`icuuc.dll` / `icudt78.dll`）打进包，
  与 Qt 期望的系统 ICU 版本冲突，启动即报 `DLL load failed while importing QtCore`；
  `桌宠.spec` 现在会过滤这些 DLL，并补齐 PySide6 自带但会被漏收的 MSVC 运行库
- 附带英文说明 `README.en.md`，文档含打包踩坑说明与维护者发版流程
- 打包版 exe 体积约 60MB，配置写在 `%APPDATA%\大肥鱼桌宠\`

基于 [1190fasheqi/dafeiyu-pet](https://github.com/1190fasheqi/dafeiyu-pet)（MIT）改造，
余额挂件能力与素材并入自 [MeteorNOX/DeepSeek-Balance-Whale-Widget](https://github.com/MeteorNOX/DeepSeek-Balance-Whale-Widget)（MIT）。

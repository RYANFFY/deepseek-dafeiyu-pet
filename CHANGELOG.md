# 更新日志

## v1.0.14

- **修：放歌时拖动桌宠/拖动别的窗口会卡顿掉帧**（主人反馈）。查下来主要两个来源：
  - **每 2 秒一次的"把自己顶到最前面"**（为了不被 Codex 那种置顶窗口压住）：每次 `SetWindowPos`
    都会让 Windows 合成器重排一次 Z 序，**按住鼠标拖动时正好撞上就会卡一下**。现在加了两道闸：
    ① **左键按着（正在拖窗口/拖文件）时什么都不做**；② 只有真的被另一个**置顶窗口**压住时才动，
    平时不再白折腾
  - **歌词排版每帧重算**：`wrap_text` 一次 ~1ms，100 帧/秒的过渡动画等于每帧白烧 1ms；
    现在**排版结果缓存**（换句/换宽度才重算），并且过渡动画**只重画气泡那一块**，不再整窗重画
  - 实测（同一台机器）：空转 12 秒 CPU 1.73 秒 → **1.27 秒**；放歌+歌词 1.81 秒 → **1.44 秒**；
    主时钟抖动（20ms/帧）基本不变（中位 20.0ms、p99 ~21ms）；
    按住左键模拟拖动时，10 次"顶置"调用里真正动 Z 序 **0 次** ✓
- **修：网易云暂停 / 恢复播放会把进度清零、拖动进度条跟不上**（主人反馈）：
  - **根因**：网易云一暂停**可能把整个媒体会话收掉**，而代码在"没歌"时会清空歌词和进度
    → 恢复播放就从头开始了。现在**会话消失不清空**（只把气泡收起来），恢复播放接着走；
    消失超过 30 秒才当成重新开始
  - 新增菜单「音乐联动 → 这首歌对不上？微调 → **按播放器显示的时间对齐…**」：
    输入播放器上显示的进度（比如 `1:35`，`01:35.5`、`95` 也认），桌宠立刻按这个时间对齐整首歌 ——
    这是网易云这边**最准**的办法（它不报进度，拖动进度条后用它对一次就行）
  - 另外开了「记菜单日志」后，每轮读取的"播放器报的进度"都会写进日志（`[音乐] … position=…`），
    以后对不上可以直接看日志确认是播放器没给数据、还是程序算错
  - 实测：会话消失再回来歌词还在、进度仍是 45.0 秒；对表到 1:35 → 立刻 95.0 秒；
    消失 60 秒后重新开始 → 归零 ✓
- **逐字高亮关掉了**（主人说不需要）：现在整句一个颜色，不再有"唱到哪变蓝到哪"。
  代码还留着（`LYRIC_KARAOKE_ON = False`），以后想要可以再打开
- **网易云歌词对不上，查到根因了**：实测系统媒体会话里，
  **网易云根本不报播放进度**（`position` / 总时长 / 时间轴全是 0，`last_updated` 是"从没设过"的默认值），
  而 **QQ音乐是正常报的**（实测 position=78.63s、总时长 242.33s）—— 所以"只有网易云对不上、切回 QQ 就正常"。
  网易云这边我们只能自己计时（从"看到它在放"开始累加），因此：
  - **放歌时轮询从 1.5 秒缩到 0.8 秒**（看得越勤，"从哪一刻开始算"的误差越小）
  - 新增菜单「**音乐联动 → 这首歌对不上？微调**」：
    `歌词太慢，往前赶 0.5 秒 / 往前赶 2 秒 / 歌词太快，往后压 0.5 秒 / 往后压 2 秒 / 这首歌的微调清零` ——
    **只影响这一首歌，并且按歌记住**。中途才开始看（或拖过进度条）时，用它把整首歌的歌词挪到对得上的位置
  - 实测：原 30.0s →「往前赶 2 秒」→ 32.0s →「往后压 0.5 秒」→ 31.5s →「清零」→ 30.0s，且按歌记住 ✓
- **修：歌词不显示、要切歌才恢复**（主人反馈）：抓到空歌词时以前会把正在显示的歌词**擦掉**，
  而且这首歌再也不会重试。现在：
  - 抓失败**不清空**已在显示的歌词
  - 这首歌没歌词时，**每 15 秒自己重抓一次**（不用切歌）
  - 歌词缓存加了**版本号**：旧缓存（以前只存了行级歌词）会在开歌时**自动重抓一次**，
    把有逐字歌词的歌升级成"唱到哪高亮到哪"
- **修：暂停网易云 → 放一下 QQ音乐 → 再切回网易云，歌词还是不同步**（主人反馈）：
  根因是"换歌时记位置"那步算错了对象 —— 覆盖 `now_playing` 之后才算旧位置，结果把**新歌的位置**
  记成了旧歌的（实测：A 歌放到 90 秒，切走再切回来变成了 3 秒）。现在改成覆盖之前先算，
  并且每首歌都记"上次放到哪"，播放器不报进度时接着走 —— 实测切回来还是 90.0 秒
- **逐字歌词（唱到哪高亮到哪）确认可用，但要看歌**：我用**真实接口**试了 10 首网易云的歌，
  其中 3 首返回了逐字歌词（`yrc`，形如 `[0,6800](0,480,0)OP(480,480,0)：...`），
  解析后每个字都有自己的时间（多字词比如 "OP" 会在词内均分），高亮能贴合真人演唱；
  **没有 yrc 的歌**（比如《晴天》这种老歌）只有行级时间戳，只能匀速推 —— 这是**数据本身的限制**。
  QQ音乐的逐字歌词是加密的（qrc），本次没做。
- **插耳机切换更快了**（主人反馈"要等近 2 秒"）：原来只靠 2 秒一次的轮询，现在
  - 直接接上 `QMediaDevices.audioOutputsChanged` **信号** —— 插上耳机/切蓝牙音箱会**立刻**触发，
    缓 150ms（等设备就绪）就重开音频流
  - 兜底轮询从 **2 秒缩到 0.5 秒**（有些设备不发信号）
  - 新设备一时开不了会**自动重试 3 次**（每 400ms），不再立刻退回 QSoundEffect
  - 实测：信号已接上、轮询 500ms、收到"插耳机"信号后 0.5 秒内音频流已切好（ok=True）
- **修：网易云中途暂停后歌词从头开始 / 拖动进度条歌词不跟着跳**（主人反馈）：
  - **暂停时播放器会短暂不给歌名**（网易云就这样），以前这被当成"换歌" → 清空歌词、进度归零，
    于是"从头开始显示"。现在**没有歌名时不当成换歌**，歌词和进度都原地保留
  - **暂停时进度停住**：以前算进度时不管放没在放都在按墙上时间往后跑，暂停一会儿再回来就整体偏了；
    现在暂停期间就用播放器报的那个进度（不再累加）
  - **拖动进度条**：同一首歌里如果播放器报的进度和我们自己推的差 2.5 秒以上（跳转 / 回退），
    就以播放器为准重新对表 —— 歌词立刻跟过去
  - 实测：暂停时"不给歌名"的更新不会清歌词/归零（进度仍 45.0 秒）、暂停 6 秒后算出来还是 45.00 秒、
    拖回 5 秒时内部进度重置到 5.0 秒
- **"唱到哪、字变到哪"现在能贴合真人演唱**（主人反馈：匀速推跟不上）：
  - 抓歌词时多要一份**逐字时间戳**（网易云的 `yrc`），解析后每个字都有自己的开始时间，
    高亮按**每个字的真实时间**推进（还会在当前字里平滑走完），不再假设"这一句匀速唱完"
  - 也支持带行内时间戳的增强 LRC（`[00:16.25]有<00:16.68>些<00:17.11>话`）
  - 只有行级时间戳的歌（比如 QQ音乐）没有逐字数据，只能退回匀速高亮 —— 这是**歌词数据本身的限制**，
    不是程序的问题（QQ音乐的逐字 `qrc` 是加密的，没去碰）
  - 实测：逐字歌词下 5.3 秒时"已唱 1.60 个字"（「你」5.0 唱完、「好」5.5 还没到）
- **气泡大小变化也有过渡动画**（主人补充）：文字变多/变少导致气泡要变大变小时，
  宽高同样用 **0.15 秒、100 帧/秒**平滑过渡，不再"啪"地换成另一个框
- **歌词过渡更顺、更快，而且只动歌词本身**（按主人三条反馈改的）：
  - 过渡时长 **0.30 秒 → 0.15 秒**，并且过渡期间改用**专用 100 帧/秒**的计时器重绘
    （主时钟在"休闲模式"只有 25 帧，0.15 秒里才 4 帧，看着就是一顿一顿；现在是 10ms 一帧、约 15 帧）
  - **只有歌词文字在过渡**：气泡（那个"窗口"）本身不再突然变大变小 ——
    宽高在过渡期间从上一句**平滑过渡**到这一句（`_lyric_prev_w/_lyric_prev_h`），
    不会出现"窗口先消失、又弹出一个新的"那种感觉
  - **"唱到哪、字就变到哪"**：当前这句里**已经唱过的部分**换成 DeepSeek 蓝（`LYRIC_KARAOKE_COLOR`），
    按"这一句的起止时间"算进度（`_lyric_progress()`），再按字数摊到每一行（`_karaoke_split()`），
    绘制时用裁剪把已唱部分重画成高亮色 —— 多行歌词也会一行一行地跟着变蓝
  - 实测：过渡 10ms/帧共 16 帧（0.15 秒）、进度 50% 时该句 33 字里 16 字变蓝（分字 [10, 6, 0, 0]）、
    唱到 2 秒 / 一句 4 秒时进度正好 50%
- **修：歌词显示不全**（主人反馈）。原来长句折行后**只画前两行**，多出来的部分直接丢掉；
  折行宽度也只有 260px。现在：
  - 折行宽度放宽到 **300px**（还受窗口宽度约束）
  - 一句话太长时**先把字号往下缩**（11 → 7 磅），缩到底还装不下就**多铺几行**
    （最多 8 行），同时把顶部那行「♪ 应用 · 歌名」让出来腾高度 —— 宁可气泡高一点，也不丢歌词
  - 实测：**5 档尺寸（迷你…大）× 中文长句 / 76 字符英文长句 / 短句** 全部 100% 显示、不截断
    （最极端那档会缩到 7 磅、7 行）
- **歌词切换加了过渡动画**（主人说"太生硬"）：换句时用 **0.3 秒**做过渡 ——
  新的一句从下方 8px 滑进来并淡入，旧的一句向上滑出并淡出，像歌词条滚动那样
  - 实现：`_lyric_rows()` 负责折行 + 取行；`_lyric_shown / _lyric_prev / _lyric_anim_t`
    在 `tick()` 里跟着进度更新，绘制时用 `setOpacity` + 位移做过渡
- **修：插上耳机以后，点击音效还在笔记本外放里**（主人反馈）。根因：点击音走的是
  "常开音频流"（`QAudioSink`），这条流在**启动时**就绑定了当时那个默认输出设备，
  之后系统换了默认设备它也不会跟着变。现在：
  - `ClickPlayer.rebuild_output()`：把常开流关掉、按**当前**默认设备重开一条
  - 这个 PySide6 没有 `defaultAudioOutputChanged` 信号，所以**每 2 秒自己查一次**默认输出设备，
    发现变了就重开（第一次只是记下来，不白折腾）；`QSoundEffect` 兜底那套也会跟着重建
  - 实测：设备的 id 记录/刷新、变化后重开流并保持 `ok=True` 都正常
- **修：放歌时歌词总是比声音快一点**（主人反馈）。对不上主要来自**输出设备的延迟**
  （笔记本外放几乎没延迟，蓝牙耳机/音箱能差 0.3~1 秒）。现在菜单「音乐联动 → **歌词对时**」
  六档可选：`文字提前 0.5 秒 / 提前 0.2 秒 / 刚好同步 / 延后 0.2 秒 / 延后 0.5 秒 / 延后 1.0 秒`，
  **默认"文字延后 0.2 秒"**（正是为了不再抢在声音前头）；菜单标题会显示当前档位，
  换档立刻生效、写进 `config.json`
  - 代码上是 `_current_lyric_pair()`：用「当前进度 − 对时偏移」去挑歌词行，方便以后调整
  - 实测：进度 10.2 秒时，"刚好同步"显示第二句、设成"延后 0.5 秒"就退回显示第一句（说明文字确实在等声音）
- **修：透明度和音效音量调不了**（主人反馈）。根因：这两个原来都是**塞在弹出子菜单里的滑块**，
  而这台机器上 Qt 的弹出子菜单**收不到鼠标消息**（我们早知道这个毛病，之前菜单高亮就是因此要自己喂），
  所以滑块根本拖不动。现在改成**点开一个小窗口拖滑块**：
  - 右键菜单 →「**透明度…（现在 xx%）**」→ 小窗口里拖滑块，**拖的时候立刻生效**（20%~100%）
  - 右键菜单 → 音效 →「**音量…（现在 xx%）**」→ 同样的滑块，另配一个「**试听一声**」按钮
  - 菜单项上直接写着当前数值（100% / 90% 这种），一眼能看到现在是多少
  - 实测（真鼠标）：两个入口都能弹出小窗口，拖动后 `win.opacity` / `windowOpacity()` /
    `volume` / 常开音频流音量 / `config.json` 全部同步
- **顺带把"菜单项要弹窗口"这条路径改稳**：以前直接在菜单回调里弹窗口，
  偶尔会被菜单自己的关闭动作一起带走（窗口一闪就没）。现在**等菜单收完再弹**
  （根菜单项延迟一拍执行；二级菜单项改成"先把菜单收掉，下一拍再执行那一项"）。
  这条也顺手让「添加我的音效…」「删掉我加的音效…」「台词内容…」更稳

## v1.0.13

- **多了 Windows 安装包（中文安装向导）**：附件 `dafeiyu-pet-setup.exe`
  - 双击运行 → 中文向导，**中间那一步可以自己选安装位置**
    （默认装到当前用户目录 `%LOCALAPPDATA%\Programs\大肥鱼桌宠`，不弹 UAC）
  - 自动建桌面 / 开始菜单快捷方式，装完**安装包本身就能删**
  - 卸载走「设置 → 应用 → 大肥鱼桌宠」；**配置在 `%APPDATA%\大肥鱼桌宠\` 不会被删**，
    重装还能接着用（想彻底清就手动删那个目录）
  - 脚本在仓库里 `installer.iss`（Inno Setup 6 编译），以后双击「发布到GitHub.bat」
    会自动打包 exe + 安装包一起上传
  - 免安装绿色版 `dafeiyu-pet.exe` 照旧提供，两个都放在 Release 里
- **台词窗口一起升级了**（右键菜单「文案 → 台词内容…」）：
  - 窗口顶部多了「**说多勤（语录频率）**」下拉，和菜单里那一项是同一个设置 ——
    改台词、调频率在同一个窗口里就能搞完，改完立刻生效
  - **「说多勤」那几句话自己也变成可改的了**：台词窗口的类别里多了
    「说多勤·安静 / 正常 / 话多 / 话痨（切到这一档时说的）」四类，
    每类默认 2 句，可以自己写、也能一键恢复默认（改完切档时立刻用你写的那句）
  - 这四档的**内置话术也重写了一版**，跟原来那套语录的味道对齐了，例如
    「行行行，我闭麦了，省点电给模型训练」「话多的开关被你按了，你自找的」
    「碎碎念模式启动，接下来求你别嫌吵」（原来那两句"好，我尽量闭嘴"那种风格太正经，换掉了）
  - 说明文字也写清楚了：一行一句（空行忽略）、放歌那两类可用 `{song}`、
    **保存时留空 = 恢复这一类默认**、说多勤只管"自言自语"（点它/拖它/换歌的反应不受影响）
  - **内置台词补了几条新的**（跟着现在的功能走），比如日常类加了
    「又来看余额了？省着点花，别把我饿着」「双击我一下，余额马上给你看」
    「嫌我吵就把『语录频率』调成安静，我立刻闭嘴」；点击回嘴、心声、放歌/换歌那几类也各补了 1~2 条。
    自己改写过的类别不会被覆盖（自己写的优先）
- **语录频率可以自己选了**：右键菜单「文案 → 语录频率」，四档（由少到多）

  | 档位 | 大概多久冒一句 |
  |---|---|
  | 安静 | 约 30 秒 |
  | 正常 | 约 12~17 秒（默认，和以前一模一样） |
  | 话多 | 约 7~8 秒 |
  | 话痨 | 约 4~6 秒 |

  - 管的是"闲着时自己冒话"的频率（点它、拖它、换歌这些反应不受影响）
  - 菜单里当前档位打勾，换档立刻生效、写进 `config.json`
  - 放歌时照旧不插嘴（把位置让给歌词）；「正常」这一档就是原来的参数，选了不会变味
  - 实测：20 万帧（约 66 分钟）里四档分别说了 125 / 280 / 474 / 781 句
    （平均 32 / 14 / 8.4 / 5.1 秒一句），相邻两句的最小间隔也都满足各自设定（1500 / 600 / 300 / 150 帧）

## v1.0.12

- **大小多了两档更小的**：现在一共 **迷你 / 特小 / 小 / 中 / 大** 五档
  （精灵高度 102 / 142 / 187 / 237 / 306 px，原来只有小 / 中 / 大）
  - 精灵图、吸附、走路、拖拽都按新档位正常工作（每一档都有自己的缩放图）
  - 顺手加了个**最窄窗口宽度（168px）**：小档位精灵很窄，如果窗口跟着一起变窄，
    余额气泡（余额 / 金额 / 今日已用 / 峰谷四行）会被挤成一列竖字；
    现在气泡保持能看的宽度，小精灵在中间显示
  - 菜单「大小」里五档依次排开，勾选状态照旧

## v1.0.11

- **快速双击 = 看一眼余额（显示 5 秒）**：
  - 配了 Key 才启用（`config.json` 的 `ds_api_key` / 环境变量 `DEEPSEEK_API_KEY` / 自己加的其他余额来源）
  - **没配 Key 就不启用**：双击还是原来的"换姿势"，并且**每次启动只提醒一次**
    「双击看余额要先填 Key：右键 →「余额 → 设置 Key」」
  - 放歌时双击也是余额（优先于歌词），看完自动回到歌词；余额还没取到时会先拉一把、回来立刻冒 5 秒
  - 顺带修一个老毛病：双击判定原来用**桌宠自己的动画时钟**（`self.t × tick_ms`），
    动画一卡这个钟就走得比真实时间慢 —— 实测把相隔 0.8 秒的两次单击也误判成"快速双击"。
    现在改成**按真实时间**判（墙上时钟，阈值仍是 380ms）
## v1.0.10

- **菜单里"选中哪一条"的高亮现在跟着手走了**（`_highlight_menu_item`）：
  这台机器上 Qt 的弹出菜单收不到鼠标移动，所以高亮一直不更新 —— 快速上下滑一级菜单时
  看着就是"卡住"。现在每一帧原生移动 + 40ms 对账都会把"光标下这一条"点成选中，
  实测一级菜单快速上下走 10 次**全部跟得上（0/10 卡住）**
- **从「添加我的音效…」往旁边移，选中不再卡在「音效选择 / 音量」上**：
  原因是光标落到同菜单的**另一条**上时，老的子菜单还在被"顶回来"，
  顺手把高亮又拽回旧的那一条。现在这种情况直接按"换条"处理（旧的收掉、高亮给新的一条），
  实测二级菜单 8 次移动**全部正常（0/8）**
- **可以加自己的音效了**：右键菜单「音效 → 添加我的音效…（自己挑 wav）」，
  挑一个 wav 就自动上架、立刻生效、顺手试听一声
  - 会自动转格式：**立体声 → 单声道、任意采样率 → 44100Hz、位深 → 16 位**
    （常开音频流那个播放器只吃这种格式，不转的话要么没声、要么开头被吞）
  - **会提醒最合适的时长区间**：点击音最适合 **0.15~0.60 秒**（太短听不清、太长连点会叠成一团），
    太短会问"还是要用吗"，太长会问"要不要帮你裁到 0.60 秒（只保留开头）"
  - 自己加的音效排在「音效选择」里（和内置的用一道分隔线隔开），
    不想要了用「删掉我加的音效…」删掉（内置两套动不了）
  - 文件存在 `sounds/`（安装版在 `%APPDATA%\大肥鱼桌宠\sounds\`），路径记在 `config.json`，
    下次启动自动带上
- **子菜单重叠再减 25%**：12px → **9px**（主人觉得还是压得多）。
  实测 3 个位置 × 20 条带子菜单的项共 60 次：重叠 **9~10px**（取整误差 1px 以内）、
  顶部错位 0、实际落点和我们算的一致

- **二三级菜单（右键菜单里的子菜单）这次是真修好了**，按主人定的「方案 2」做：
  子菜单和上一级菜单贴着弹出（`MENU_OVERLAP`：16 → 12 → **9**，主人两次都说"压得有点多"，
  各减 25%），鼠标从一级移到二级几乎不用走空档；
  位置在**弹出之前**就算好，不会再出现"先出现在老地方、再往里跳一下"
- 方案 1（把一级菜单的触发范围横向放大）按主人的意思**弃用**（开关留在代码里，值为 0）
- 顺手修掉真机测试揪出来的三个问题：
  - **光标移到三级菜单上、二级菜单就消失** —— 根因是"换条时把上一个子菜单收掉"会把
    光标脚下这一层也收走（三级菜单正是二级那一项的子菜单）。现在收之前先判断
    "要收的这层是不是光标现在所在层或它的上层"，是就不收
  - **子菜单压住一级菜单的条目**（「层级 / 透明度」悬停半天不出子菜单就是这个原因）：
    子菜单往左弹时，现在会**一层一层同样往左**，三级菜单不会绕回来盖在爷爷菜单头上
  - **上一支子菜单残留在屏幕上**：每 120ms 对账，**一次只留光标所在的那一支**（跟 Windows 原生一致）
- 验证（真鼠标、拟人速度）：20 条带子菜单的菜单项**左右两侧各扫一遍，全部正常、闪烁 0 次**；
  光标停在三级菜单里 2.5 秒，二级/三级菜单 **100% 可见、不闪**；点三级菜单里的「用这个形象」能真的切换形象；
  11 个自动化测试套件全过
- **托盘菜单（右键任务栏小图标）一起修好**：
  - 托盘那条路是 Qt 自己弹菜单、而且每次右键都会**重建一份**菜单，万一弹的是上一份，
    程序记着的那棵树就跟屏幕上的对不上（悬停兜底、收子菜单、点击摆渡全部落空）。
    现在每 120ms 认一次"屏幕上真正开着的那棵菜单"，认错了就换过来接管
  - 子菜单被 Qt 收掉、而光标还停在它原来那一片时，现在会**分三步往回捞**
    （让 Qt 重开 → 替 Qt 往那一条塞一个鼠标移动 → 都不行才自己补弹）
  - 实测：托盘菜单按**真实托盘位置（屏幕右下角，菜单要往上/往左翻）**扫 20 条，
    20/20 正常、闪烁 0 次；连"Qt 弹了上一份旧菜单"这种最坏情况也是一样
- **重叠像素量过了**：多个位置 × 20 条带子菜单的项（含二级、三级）逐条量，
  **横向重叠每次都等于设定的值**（16 时 80/80 都是 16；改成 12 后 60/60 都是 12）
  （顶部和条目对齐，固定差 4px，误差 ±1px），而且**子菜单实际落点和我们算的完全一致（0 处偏差）**。
  顺手揪出并修掉三个会让"看着重叠忽多忽少 / 位置不对"的真问题：
  - **残留的子菜单会把下一次右键挡掉**（最要命的一个）：Qt 偶尔不跟着根菜单一起收子菜单，
    屏幕上留一个"孤儿"弹窗；它挡在那儿，下一次右键菜单干脆弹不出来。
    现在每 120ms 对账，根菜单一关就把残留子菜单收掉（实测：修前"再右键打不开"，
    修后 4/4 轮都正常打开）
  - **"认屏幕上那棵树"的逻辑会把孤儿子菜单误认成整棵树的根** → 改成在**所有**菜单里找父关系
  - 子菜单被 Qt 收掉、光标还停在它原位时，分三步往回捞（Qt 重开 → 塞一个鼠标移动 → 才自己补弹）
- **子菜单位置"自愈"**：每 120ms 对一次账，只要某个子菜单没贴在"你正悬停的那一条"上
  （偏了 2px 以上），就把它挪回去 —— 不管是 Qt 自己摆歪的、弹出时尺寸还没算准，
  还是被别的动作挪过。实测：故意把子菜单挪歪 37/63 像素，**0.45 秒内自己回到原位**
  （横向重叠回到 16px、顶部错位回到 0），一级和三级都验过
- **新增「记菜单日志（菜单出问题时打开）」**（右键菜单 → 救急恢复下面那一项）：
  打开后菜单的展开/换条/位置自愈都会记到 `menu-debug.log`
  （源码版在程序目录、安装版在 `%APPDATA%\大肥鱼桌宠\`）。
  以后主人再遇到"位置不对"，打开它复现一次，把日志给我就能精确定位
- **"先小部分重叠、再往里移动"修掉了**：Qt 自己弹子菜单时用的是它那套摆法
  （贴着条目边、不重叠、还可能高一截），我们下一拍才发现位置不对再挪过去 —— 看起来就是
  "先摆一版再往里移"。现在给每层子菜单装了"**一显示就摆正**"的过滤器：
  摆正发生在**同一帧**里（实测：故意按 Qt 的摆法弹出来，8/8 都是同一帧就摆到位，看不出挪动）
- 菜单开着时对账间隔从 120ms 提到 **40ms**（`MENU_HOVER_FAST_MS`），万一还有哪层被摆歪，
  下 40ms 就纠回来；菜单关掉自动回到 120ms，不白费电
- 光标还停在原来那片、子菜单却被 Qt 误收时，**自动原地弹回来**（以前会在 0.35 秒宽限后放弃，
  表现就是"偶尔进去一条就没了"）

## v1.0.9

- **台词可以自己写了**（换了形象之后默认台词不搭的场景）：右键菜单「文案 → 台词内容…」
  一个窗口里改所有台词，分六类：日常台词 / 点击回嘴 / 心声 / 拖拽台词 / 放歌时点击 / 换歌时
  - 直接编辑，一行一句（空行自动忽略）；**既能自己写新的，也能直接改写内置的**（编辑框里就是当前生效的那套）
  - 放歌那两类里可以用 `{song}` 代表「《歌名》——歌手」
  - 每一类旁边都能「恢复这一类默认」；菜单项会显示「已改 N 类」
  - 菜单层级和以前完全一样（就挂在原来的「文案」下面，没有多一层）
- **闲着的时候更爱说话了**：触发概率提高（0.01 → 0.03），并且"想说话"那一档的占比从 10% 提到 30%，
  说话冷却从 30 秒缩短到 12 秒 —— 实测平均 **约 17 秒冒一句**（以前约 30 秒），
  配上自己写的台词更有存在感

## v1.0.8
- **修复：桌宠会"卡在鼠标穿透"回不来**。解除穿透原来只靠 Qt 的 `aboutToHide` 信号，
  实测这个信号会漏：菜单早关了、信号没来，桌宠就一直停在"鼠标穿透"状态，怎么点都没反应
  （去看 `config.json` 会发现 `passthrough=false`，但窗口其实还带着穿透位）。
  现在改成每 120ms 按"**现在到底还有没有菜单/对话框开着**"对账，漏信号也能自己恢复
- 开发过程备注：「菜单打开期间把桌宠藏起来」这个思路试过又回退了 —— 藏桌宠会把菜单
  一起带崩（菜单闪一下就自己消失，快速点击还可能让桌宠一直不出现）。现在菜单开着时
  桌宠保持显示，只用"临时取消置顶 + 鼠标穿透"让位
- **修复：二级菜单在有些位置点不中**（靠屏幕右边、子菜单往左弹到桌宠身上时最明显）。
  实测发现根因是**系统根本没把鼠标移动消息送给弹出菜单**（消息全发给了桌宠窗口），
  于是 Qt 收不到 hover、二级菜单一直不弹，自然就"点不中"。现在：
  - 菜单打开期间桌宠会临时退到非置顶层，菜单天然压在它上面（**不要**给桌宠加鼠标穿透，
    否则右键桌宠会穿到桌面，弹出 Windows 桌面那个右键菜单）
  - 加了**悬停兜底**：菜单开着时自己盯着光标位置（`QCursor.pos()` 一直是准的），
    在带子菜单的项上停一下（约 0.22 秒），就替 Qt 把子菜单弹出来
  - **二三级菜单不容易消失了**：光标离开子菜单后要等 **0.5 秒**才收（斜着划过去也不会误关）；
    万一 Qt 因为收不到鼠标消息把子菜单自己关了，只要光标还在它原来的范围内，就**自动补弹回来**
  - 桌宠收到的鼠标移动会**转发给菜单**，让 Qt 的悬停高亮、展开、收起都按正常逻辑走
  - **关键修复：子菜单改用 Qt 自己的 `setActiveAction` 展开**。之前是悬停兜底自己
    `popup()` 一个子菜单窗口 —— 窗口是弹出来了，但 Qt 内部并不知道它是"这一项的子菜单"，
    于是**点击不会被路由到子菜单上**，表现就是二级、三级菜单"点不了"（一级菜单却正常）
  - **最终结论：二三级菜单回滚成 1.0.7 的原生行为**。这一轮为了修它试过很多方案
    （悬停兜底、拦截/转发事件、延时收起、自动补弹、点击摆渡、原生消息层接管……），
    根因也查清了（这台 Windows 不把鼠标移动/点击送给**弹出式子菜单**，点击会被送给
    持有捕获的上层菜单 —— 抓原生消息确认过），但每次都只能改善一半，反而引入新毛病。
    所以按主人的意思**不再折腾**：子菜单完全交回 Qt，我们只保留与菜单无关的那两个修复
    （穿透状态自愈、不再给桌宠加穿透/不再隐藏桌宠）
- **修复：在桌宠上重复右键会弹出 Windows 桌面的右键菜单**。上一版为了让点击不被桌宠截住，
  菜单打开期间把桌宠设成了鼠标穿透 —— 结果右键桌宠时点击直接穿到桌面上去了。
  现在菜单期间**不再给桌宠加穿透**，只把它降到非置顶层（实测二级菜单依旧能悬停展开、点得中）
- **天气**：放歌时点「查看天气」，结果气泡和余额一样停 **5 秒**再让位给歌词
  （计时是从天气结果查回来、显示出来那一刻开始算的）
- **城市列表**：添加过的城市会记进列表，菜单「天气」里直接列出来，点一下就切过去（当前城市打勾），
  还能「从列表里删掉城市…」（至少留一个）
- **新增「流畅度」菜单：性能模式 / 休闲模式**
  - **性能模式**（默认）：动画优先，**打开设置菜单也不掉帧**。以前菜单一开，桌宠就降到
    约 5 帧、并且不再刷新，看着像卡住
  - **休闲模式**：帧间隔由 20ms 放到 40ms（50 帧 → 25 帧），菜单打开时降到约 5 帧，更省 CPU。
    动作都按同一个内部时钟换算，所以**走的速度、气泡停留时间都不变**，只是画面更"省"
- **音乐联动（QQ音乐 / 网易云音乐）**：放歌时头顶气泡变成歌词 —— 大字当前这一句、
  下面淡一行下一句，顶上小字标「♪ 应用 · 歌名」。歌名 / 歌手 / 播放状态 / 播放进度
  直接读 **Windows 系统媒体会话**（不用登录、不用填 Key），暂停和切歌都能立刻跟上
  - 歌词联网找：先网易云、再 QQ音乐（两个接口都不用 Key），找到的缓存进 `lyrics_cache.json`，下次秒开
  - **放歌时点它**：单击 → 冒一句「放歌ing：《歌名》——歌手」；
    **快速双击** → 立刻看 5 秒余额，看完自动回到歌词
  - **余额常显也不冲突**：放歌时歌词优先，余额双击就能看；定时刷新冒出来的余额气泡也不会打断歌词
  - 菜单新增「音乐联动」：整个关掉 / 只报歌名不显示歌词 / 立刻看一眼在放什么
  - 读不到系统媒体会话时（老系统、精简系统），自动退化成读播放器的窗口标题
- 双击桌宠现在有明确动作：平时换姿势，放歌时看 5 秒余额（以前说明书里写了但没实现）
- 麻烦的依赖：打包版体积从 60MB 涨到约 72MB（多了读系统媒体会话的 `winsdk`）

## v1.0.7

- **修复：托盘右键菜单打不开，导致开了「鼠标穿透」后无法解除**。
  根因是上一版为了让菜单不被桌宠窗口遮挡，改成了「无父窗口的弹出菜单」，
  但 Qt 的托盘菜单**不接管所有权** —— 菜单对象随后被 Python 回收，
  右键托盘图标就弹不出任何东西，穿透也就解不回来了。
  现在托盘菜单会被显式持有（并且每次右键重建后立刻留引用），不会再被回收。
- **新增两条「救急」通道**，避免再被锁在穿透状态里：
  - **双击托盘图标**：立刻解除鼠标穿透并把桌宠叫回来（没开穿透时 = 显示/隐藏）
  - 开启穿透时会冒泡说明解除方法；如果开机时就处于穿透状态，2.5 秒后也会提醒一次
- 穿透开关会立刻写入 `config.json`，重启后状态与你的选择一致（以前只在退出时保存）

## v1.0.6

- **内置应用在列表里明确标记、不能重复添加**：应用扫描窗口里，默认就自带台词的应用会显示
  `●内置默认（不可重复添加）`（改写过的显示 `✎已改写默认台词`），选中它时主按钮直接变成
  **「修改默认台词」**，不会再给你重复加一份
- **可以修改内置应用的台词**：点「修改默认台词」会带着原台词打开编辑框，改完**整组替换**内置文字
  （写入 `default_line_overrides`，不会和原版叠加）；想还原就点「恢复内置台词」
- 数据层面也堵住了重复：写给内置应用的 `custom_process_lines` 会被忽略，
  内置应用只认「改写」这一条路；菜单里的「清理自定义 / 改写的文字…」可以一次清掉两类记录

## v1.0.5

- **应用扫描升级成带图标的列表**：不再是一串纯文字让你猜。
  菜单「进程联动 → 扫描电脑应用并添加…」现在会打开一个像「设置 → 应用」那样的窗口：
  **每个应用都带自己的图标**（图标直接从 exe 里取，不用额外素材）、
  顶部能**搜索**（输 steam / edge / 微信 都行）、列表里 `●` 表示默认就有台词、`✓` 表示你自定义过；
  选中后点「添加触发文字」写它要说的话（一句一行，可多行），也能一键「删除该应用的文字」
- 扫描结果同时带上每个应用的 exe 完整路径（这台机器实测扫到 133 个，全部能取到图标）

## v1.0.4

- **菜单重新分区**：「吸附」（拖拽吸附四边 / 左吸附翻面）和「文案」（显示峰谷时段 / 三档峰谷文案）
  从「余额」里拆出来，变成各自的独立菜单
- **进程联动可自定义**：新增「进程联动 → 扫描电脑应用并添加…」，
  会扫描本机应用（正在运行的进程 + 开始菜单快捷方式，实测这台机器扫到 133 个），
  选中后自己输入打开它时要说的文字（多句用 `|` 隔开）；也能「删除自定义应用…」。
  自定义台词会和默认台词表叠加
- **每轮消耗统计支持其他 agent**：可以设置 **Agent 名称**（显示成「上一轮 Claude 消耗 ¥…」）
  和**会话日志目录**，不用 Codex 的人也能指向自己的日志；
  日志解析改成兼容多种结构（Codex 的 `payload.usage`、Claude Code 的 `message.usage`、
  OpenAI 风格的 `prompt_tokens/completion_tokens` 都认）

## v1.0.3

- **修复二级菜单在屏幕边缘失灵**：菜单原来挂在桌宠窗口下、又强加了置顶标志，而桌宠本身也是置顶窗口，
  三者叠在一起时，鼠标往子菜单方向移动很容易被判定成"离开了菜单"，于是整个菜单被收起
  （靠屏幕右侧、子菜单往左弹的时候最明显）。现在菜单改成**独立的 Qt 标准弹出菜单**（不再依附桌宠窗口、
  不再自带置顶标志），改为**菜单打开期间桌宠临时退到普通层**（关闭后按设定层级还原），
  悬停展开和贴边反向弹出都恢复正常；菜单开着时桌宠的刷新率也降下来，减少对弹出菜单的干扰
- 顺带：对话框（设置 Key / 添加城市 / 选图片）期间同样让桌宠临时让位，不会被它压住

## v1.0.2

- **修复进程联动只有 Steam 触发**：以前只认"新出现的进程"，所以早就开着的微信 / 浏览器 / QQ 永远不触发。
  改成看**前台窗口**——你切到（或刚打开）哪个应用就吐槽哪个，同一个应用 10 分钟只吐槽一次
  （ChatGPT 15 分钟，免得你在这边聊天时它一直念）。检测间隔也从 4 秒缩到 2 秒，反应更快
- **修复自由散步卡在屏幕边**：目标点原来按窗口"左上角"生成，而移动时又会把窗口钳制在屏幕内，
  靠近顶边时目标永远够不到 → 它就顶着墙一直走、一直背对着。现在目标点按窗口中心生成并夹在可达范围内，
  另外加了"卡住 2 秒自动换目标"的保护
- **修复右键菜单二级菜单异常**：二级菜单会被一级菜单遮挡、鼠标一离开一级菜单整个菜单就消失（越靠屏幕边缘越明显）。
  根因是上一版为了让菜单压在桌宠上面，用了 `popup` + 每 400ms `raise_()`，会把子菜单顶掉；
  现在改回 Qt 标准的 `exec()` 模态弹出（菜单打开期间桌宠本来就不会抢置顶），二级菜单悬停展开、贴边反向弹出都正常

## v1.0.1

- **层级可选**：置顶 / 置底（在所有窗口下面，但仍在壁纸和 Wallpaper Engine 这类桌面软件之上）/ 普通层
- **透明度可调**：菜单里一条滑块，100% → 20%
- **自由散步更灵**：只要横向有位移就走侧面，不再"背过身去不转回来"；原地待着时会转头看鼠标
- **进程联动**：检测到 Steam / WeGame / 浏览器 / 播放器 / 编辑器等被打开时，冒泡吐槽一句
  （台词表在 `桌宠.py` 顶部 `PROCESS_LINES`，可以自己加；菜单里可整体关掉）
- **单实例**：已开着一只时再点快捷方式或再跑源码都不会冒出第二只，会把它叫出来
- **自定义形象**：菜单「形象」里两套外观都能换成自己的图片 —— 挂件是单张图，
  大肥鱼三视图可以分别指定正面 / 侧面 / 背面，随时一键恢复默认
- **多余额来源**：可添加别的 API Key。DeepSeek 与 OpenRouter 能查到余额（OpenRouter 按 credits − usage 算）；
  OpenAI / ChatGPT 这类官方没有余额接口的，会直接显示"该服务不提供余额接口"，而不是报错
- 仓库首页加了醒目的下载入口；Release 附件名固定为 `dafeiyu-pet.exe`，
  所以 `releases/latest/download/dafeiyu-pet.exe` 永远指向最新版

## v1.0.0

首个发布版本（打包版已修好启动报错，exe 双击即用）：

- 桌宠本体：三视图行走（左右镜像 / 向上背面 / 向下正面）、自由散步 / 跟随鼠标 / 原地待着
- 余额挂件：余额泡泡（余额 / 今日已用 / 当前峰谷时段）、数字滚动、每 60 秒自动刷新、点击手动刷新
- 拖拽四边吸附 + 左吸附整体翻转 + 按压 Q 弹 + 按键音效（按压+松手合并成一条完整音频，常开音频流播放）
- 每轮 Codex 对话消耗统计（读会话日志，按峰谷定价换算）
- 天气：手动设默认城市 / 联网搜索 / 按 IP 定位，播报全中文，wttr.in + open-meteo 双源
- 形象切换：大肥鱼（三视图）/ 小鲸鱼挂件（cut-out）
- 按需求精简：移除 AI 对话、喂食、系统状态监控
- 打包：PyInstaller 会把本机其它运行时的 ICU（`icuuc.dll` / `icudt78.dll`）打进包，
  与 Qt 期望的系统 ICU 版本冲突，启动即报 `DLL load failed while importing QtCore`；
  `桌宠.spec` 现在会过滤这些 DLL，并补齐 PySide6 自带但会被漏收的 MSVC 运行库
- 附带英文说明 `README.en.md`，文档含打包踩坑说明与维护者发版流程
- 打包版 exe 体积约 60MB，配置写在 `%APPDATA%\大肥鱼桌宠\`

基于 [1190fasheqi/dafeiyu-pet](https://github.com/1190fasheqi/dafeiyu-pet)（MIT）改造，
余额挂件能力与素材并入自 [MeteorNOX/DeepSeek-Balance-Whale-Widget](https://github.com/MeteorNOX/DeepSeek-Balance-Whale-Widget)（MIT）。
